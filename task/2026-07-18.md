# Task 2026-07-18

## Task 1：縮小行為變更的測試爆炸半徑（run: 2026-07-18-test-blast-radius）

- 建立時間：2026-07-18 00:15
- Tier：1（spec_inline 見 `run/2026-07-18-test-blast-radius.json`）
- 背景：使用者回報「新需求改 code 花一點 token、改 test 花更多」。診斷：合理成分（行為變更→舊契約作廢本該改）之外，大頭是設計債——同一行為被多個測試附帶斷言（訊息全文、欄位順序等非主題細節），行為一改全場陪葬；且 writer 邊寫邊被 mine/check 逐個炸出舊測試、強模型逐輪修。三處方：斷言去重（治本）、預期值集中（治結構）、測試同步獨立 item（治執行成本）。
- 驗證方式：`python3 -m unittest tests.test_docs_consistency` 綠＋全套綠。

### Items（序列執行；兩 item 均適用 Tier 1 小 prose 合併規則）

- [x] **Item 1：斷言去重＋預期值集中＋reviewer 扣分項**（`.claude/agents/code-writer.md`、`.claude/agents/code-reviewer.md`；~20 行 prose；直寫捷徑；兩檔語義同源合併一輪審）
  - code-writer 測試管轄規則加規則 7：**一個行為只在一個地方被完整斷言**——你的測試只斷言你的主題，路過的既有行為用寬鬆檢查（狀態碼對即可，不比對訊息全文／欄位順序／完整輸出形狀）；被多個測試共用的預期值收進 builder／常數／golden 單一出處（fixture single source 精神擴及 expected values），行為變更時改一處
  - code-reviewer 測試覆蓋範疇加扣分項：**重複斷言既有行為 → 🟡（爆炸半徑稅）**——diff 中的新測試若完整斷言了別的測試已擁有的行為（非本測試主題），標 🟡
  - DoD：兩檔規則語義一致（writer 禁做的＝reviewer 扣的）；`python3 -m unittest tests.test_docs_consistency` 綠
- [x] **Item 2：測試同步規則＋部署**（設計修正：原「獨立 item」有 step 5 check 時序死結——行為變更的舊測試失敗不在 baseline、script 不讀 DoD，任何獨立排序都必紅；審查後改為「實作 item 內測試同步段」：writer 交付後、check 前由主 flow 派快 model 批次同步）（`skills/task-decomposition/SKILL.md`；~15 行 prose；直寫捷徑）
  - 加規則：需求含**有意的行為變更**時，Spec／spec_inline 須明列「作廢舊行為清單」（舊契約→新契約對照）；拆分時以 `python3 .claude/hooks/test_baseline.py related --files <受影響檔>` 預算受影響測試，開**獨立「測試同步 item」**——工作＝依對照表機械批次更新舊測試（可派快 model），實作 item 的 writer 不碰舊測試（其 DoD 明寫「既有測試失敗屬測試同步 item 管轄」）；測試同步 item 的 local_test_evidence 引用對照表即滿足 test-strategy「測試過時須記依據」
  - 反模式清單加一條：有意行為變更卻無作廢清單／無測試同步 item（舊測試更新散進各實作 item，writer 逐輪踩雷）
  - 部署同步 `~/.claude/skills/task-decomposition/SKILL.md`（diff 驗證一致）
  - DoD：`python3 -m unittest tests.test_docs_consistency` 綠；部署 diff 一致

## Task: scout 蒐證 agent(run: 2026-07-18-scout-agent)

> Tier 1。目標:新增 haiku 唯讀蒐證 agent,Tier 2 前置 2/2.5 改為「scout 蒐證 → opus 分析」兩段,降低 usage/impact 兩個 opus agent 的掃碼 token。

- [x] 1. 新增 scout agent 定義
  - files: `.claude/agents/scout.md`
  - DoD: frontmatter model=haiku(附指派理由註解)、tools=Read/Grep/Glob/Write(無 Bash,可背景);產出 `scout/<run_id>.md` 四節(檔案清單/symbol 簽名/慣例觀察/呼叫端與測試位置);明文「只記事實(引用+路徑+行號),禁止分析建議結論」;不回寫 manifest(無 Edit 權限,回寫責任在主 flow)
- [x] 2. 接線四處(prose 合併一輪審)
  - files: `skills/eval-flow/SKILL.md`, `.claude/agents/usage-analyzer.md`, `.claude/agents/impact-analyzer.md`, `CLAUDE.md`
  - DoD: eval-flow 新增「前置 1.5:scout 蒐證」段(risk_done 後派、背景執行、主 flow 回寫 manifest `scout_report_path`、失敗軟性降級不擋流程、不動 hook/phase);usage/impact 兩定義各加「證據來源優先序」段(①讀 scout 報告 ②關鍵斷言抽查原檔——硬性 ③缺口自補掃);CLAUDE.md Task Principle 加 `scout/<run_id>.md` 一行;skill 檔部署同步 `~/.claude/skills/eval-flow/SKILL.md`(diff 驗證一致,於本地測試通過後執行)

## Task: 簡化 test gate——移 flaky 分類與 strike 額度(run: 2026-07-18-test-gate-simplify)

> Tier 1。背景:scoped 測試架構下 flaky 分類無存在前提(12 run 零命中);strike 兩次額度是 agent 僅存的自主裁量,改為真失敗立即 HITL(人是計數器)。**不改欄位名**(保留 stable_failures);測試同步摺進實作 item(有意行為變更,依 task-decomposition 測試同步段規則,writer 交付後 git add 前由快 model 批次同步舊測試)。

- [x] 1. test_baseline.py 行為變更 ＋ 測試同步段
  - files: `.claude/hooks/test_baseline.py`, `tests/test_test_baseline.py`
  - DoD(test_baseline.py):
    - **baseline**:移除 `--runs` 參數與「跑兩次取交集」邏輯,改**單次跑**,所有失敗記入 `stable_failures`(進場既有失敗快照);移除 `flaky` 的計算與寫入(不再產生 flaky 名單)。**保留** `find_reusable_baseline`/head_sha 沿用邏輯與 `--fresh`(沿用的舊檔含 flaky 欄位時忽略即可,不回填)
    - **check**:保留「新失敗重跑一次確認可重現」(new = fails - stable_failures;有 new 則重跑,real = new & fails2,非可重現的印警示「非確定性失敗,未阻擋」但**不持久化任何 flaky 名單**、不擋);移除 `strikes` dict 與 `--strike-key` 的計數/2 次上限訊息;real 失敗時 → 在 baseline 檔 append 一筆 `failure_log`(如 `[{"key":..., "tests":[...]}]`,供稽核「紅過就有痕」)＋ exit 2,stderr 訊息改為「真實新失敗 N 個,停止自修,回報使用者裁決」(不提 strike/次數)
    - **mine**:移除 strikes 計數與 2 次上限訊息;失敗時 → exit 2 印失敗清單(保留 `--strike-key` 參數簽名以免呼叫端壞,但不再用它計數;或若移除參數則同步所有呼叫端——擇一,寫進 evidence)
    - 移除因上述而孤立的 import/函式(如僅 flaky 用到的);docstring 同步更新
  - DoD(測試同步段,快 model 於 writer 交付後 git add 前執行):`tests/test_test_baseline.py` 中斷言舊行為的測試依新契約同步——`test_baseline_splits_stable_and_flaky`(flaky 移除)、`test_check_blocks_new_stable_failure_and_counts_strikes`(strike 移除)、`test_new_flaky_failure_passes_and_is_recorded`(改為驗「重跑確認可重現/非可重現不擋」語意)、`test_pass_resets_strike_counter`(移除)、`test_mine_two_failures_with_strike_key_prints_limit_message`(移除或改驗失敗即 exit 2);baseline 沿用/related/mine 基本行為測試保留
  - 舊契約→新契約對照(供 reviewer 核對測試改動依據):[兩次交集 stable+union flaky]→[單次全記 stable、無 flaky];[strike 累計+2 次上限訊息]→[真失敗即 exit 2+failure_log 留痕、無計數];[flaky 名單持久化]→[非可重現僅警示不持久化]

- [x] 2. stats.py baseline 走勢移除 flaky 顯示
  - files: `.claude/hooks/stats.py`, `tests/test_stats.py`
  - DoD:baseline 走勢輸出從「stable X／flaky Y」改為只顯示 stable(既有壞測試)數;讀取端 `base.get("flaky",[])` 移除或保留為向後相容(舊 baseline 檔仍有 flaky 欄,不因缺欄報錯);`tests/test_stats.py` 對應 fixture 與斷言同步(line 51 含 flaky 的 fixture 可保留欄位但斷言不再檢查 flaky 顯示)
  - 對照:[baseline 走勢顯示 flaky Y]→[只顯示 stable]

- [x] 3. test-strategy skill 重寫
  - files: `skills/test-strategy/SKILL.md`
  - DoD:description 移除「flaky 過濾、失敗四分類決策樹、兩次舉手上限」措辭;baseline 段「預設跑兩次...stable/flaky」改為「單次跑記 stable_failures」;flaky 相關段(line 63「flaky 由 script 自動處置」、失敗分類決策樹的 flaky 列)移除;「失敗分類決策樹」縮為一句「真新失敗 → script 已重跑確認可重現 → 立即回報使用者裁決」(保留「肇因非本 item→重開路徑」一列,那與 strike 無關);「兩次舉手上限」節(line 85-88)移除,改為「真失敗即回報使用者(人是計數器)」;「script 判定可稽核」段的「strike 計數」改為「failure_log 留痕」。**部署同步** `~/.claude/skills/test-strategy/SKILL.md`(本地測試通過後執行)
  - 對照:[兩次跑/flaky/四分類/兩次舉手]→[單次/無 flaky/一句/立即 HITL]

- [x] 4. 周邊 prose 同步
  - files: `skills/eval-flow/SKILL.md`, `skills/task-decomposition/SKILL.md`, `.claude/agents/code-writer.md`
  - DoD:
    - eval-flow step 5(循環步驟 5)敘述中提及 flaky/strike 的措辭同步(baseline 仍存在,只調整「flaky 由 script 過濾」「2 次真失敗」等句)
    - task-decomposition line 31 測試同步段的「baseline」引用檢查:baseline 機制仍在(仍有 stable_failures),不變量敘述基本不動,僅確認無 flaky/strike 殘留措辭
    - code-writer.md rule 5:移除「不依賴 mine 的 strike 提示(無 baseline 檔時提示不會出現)」的括號(strike 提示已移除),保留 writer 自身「同一失敗最多修 2 次、帶失敗交付」的核心(內部迴圈,不受本次改動影響)
  - **部署同步** `~/.claude/skills/eval-flow/SKILL.md`、`~/.claude/skills/task-decomposition/SKILL.md`(本地測試通過後執行)
