# Task 2026-09-07

## Task 1：外部專案實戰反饋修正（run: 2026-09-07-feedback-obs-fixes，Tier 1）

> 需求出處：外部專案 Tier 2 run 的實戰反饋（反饋項 3、項 4；項 1、2 已以 Tier 0 處理，見 run/tier0.jsonl 2026-09-07 兩筆）。
> 執行方式：主 flow 直寫捷徑（兩 item 均 ≤100 行）；審查合併一輪 checker。

- [x] item 1：批次派工加「批前 staged --stat 快照」，checker 以差集核對（跨批 staging 污染修法）
  - 檔案：`skills/eval-flow/SKILL.md`（循環 step 2、step 3）、`.claude/agents/task-verifier.md`（輸入節）
  - 背景：批間不 commit 時 staging 累積；同一檔跨批都有變更時，`git diff --cached --stat -- <files>` 的行數混入前批，checker 會把前批變更誤判為本批（外部 run 實際發生 1 次誤升級）。
  - DoD：
    1. SKILL.md 循環 step 1（批次派工）或 step 2 載明：派 writer 前，主 flow 先記「批前 staged `--stat` 快照」（`git diff --cached --stat -- <files>` 當下輸出；本 run 首批＝空快照，prompt 標明「首批」即可）。
    2. SKILL.md 循環 step 3 的 checker 輸入集增列第 6 項：批前快照（含「首批＝空快照」語義）。
    3. `task-verifier.md` 輸入節同步：輸入五項改六項；載明行數核對以「現快照 − 批前快照」差集為準；首批標註時視同空快照；多批情境未附快照且未標首批＝憑據缺席，觸發升級①。
    4. 兩檔對讀無矛盾（step 3 輸入集條目數與 task-verifier 輸入節一致）。
  - 契約：無（純流程文件變更，無可執行碼；仲裁以 DoD 為準）
  - 驗證：全套 `python3 -m pytest tests/ -q` 無新增失敗；人工對讀兩檔一致。

- [x] item 2：manifest `subagent_usage` 加選填 `main` 鍵，主 flow 用量入帳
  - 檔案：`.claude/hooks/stats.py`、`tests/test_stats.py`、`skills/eval-flow/references/formats.md`、`skills/eval-flow/SKILL.md`（step 6 子項②）
  - 背景：`subagent_usage` 只記 subagent（prep／loop），主 flow 協調成本不入帳，流程稅被系統性低估（外部反饋項 4）。
  - DoD：
    1. formats.md 增列 `subagent_usage.main` 選填鍵語義：主 flow 本 run 的 tokens（估計值可標來源，如 harness／/cost 顯示值）。
    2. SKILL.md step 6 子項②同步一句：彙總時可併記 `main`。
    3. stats.py 讀取 `main`（int 才收，非 int 寬容跳過——與既有 prep/loop 容錯同式），輸出行含 `main`，合計行加總 main。
    4. test_stats.py 新增兩案例：含 `main` 的 manifest 輸出含 main 數字；無 `main` 的 manifest 照舊輸出 prep/loop、不報錯。
  - 契約：
    | # | 輸入 | 預期 |
    |---|------|------|
    | 1 | `subagent_usage: {"prep":1,"loop":2,"main":3}` | stats 輸出該 run 行含 `main 3`，合計含 main 加總 |
    | 2 | `subagent_usage: {"prep":1,"loop":2}`（無 main） | 照舊輸出 prep／loop，不報錯、不顯示 main |
    | 3 | `subagent_usage: {"prep":1,"loop":2,"main":"x"}`（main 非 int） | main 寬容跳過，prep／loop 照常 |
  - 驗證：`python3 -m pytest tests/test_stats.py -q` 全綠；全套無新增失敗。
