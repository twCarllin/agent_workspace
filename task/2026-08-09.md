# Task — task 檔行數預算（item ≤12 行、DoD ≤5 條、指向式引用）  (created: 2026-08-09 12:00)

> run_id: `2026-08-09-task-item-line-budget`（Tier 1，需求原文與數據依據見 manifest `run/2026-08-09-task-item-line-budget.json` 的 `spec_inline`／`tier_rationale`）
> 本 task 檔自足可讀，不依賴對話上下文。

## Task 1: 拆分產出瘦身（三檔純 prose，合併一輪審）
- 依賴: 無
- 說明: 同一設計決策（task 檔行數預算＋指向式引用）投放三檔，各檔 ≤30 行、無 code → 依 eval-flow「Tier 1 小 prose item 合併」規則合併為一個 sub_task。

- [x] 1.1 task-decomposition skill 加行數預算＋自足性語義＋反模式；task-decomposer 自檢同步；CLAUDE.md 自足性條澄清  ~45行  files: skills/task-decomposition/SKILL.md, .claude/agents/task-decomposer.md, CLAUDE.md
      DoD:
      (a) skill 輸出格式節新增「item 行數預算」：每 item（含 DoD＋契約表）≤12 行；DoD ≤5 條、每條一行可驗斷言；禁止內嵌要寫進目標檔的替換全文／程式碼片段（去處：Spec 或 spec_inline）；標明兩數為校準值、實測後可調
      (b) skill 明訂自足性語義：自足＝讀 task 檔＋其引用的 spec／usage／impact 檔即可接手；情境、風險、慣例以 id／節名指向式引用，不重述內容
      (c) skill 反模式節新增 2 條 reject：「DoD 內嵌整段替換文字／程式碼片段」「task 檔重述 Spec／usage／impact 已有內容」
      (d) task-decomposer.md 交付前自檢加機械檢查（item >12 行＝實作細節誤入 DoD，重拆瘦身），Clarity 節自足性句同步 (b) 語義；CLAUDE.md 產出物自足性條補「引用其他 run 產出檔（路徑＋節次）不算依賴對話，應指向式引用、不重述」
      (e) grep 驗證：`grep -rn "≤12 行" skills/task-decomposition/SKILL.md .claude/agents/task-decomposer.md` 各檔 ≥1 命中；`grep -rn "指向式引用" skills/task-decomposition/SKILL.md CLAUDE.md` 合計 ≥2 命中；`grep -c "^- " skills/task-decomposition/SKILL.md` 反模式節條數 +2
      情境: router 已篩（Tier 1）
      契約: prose item——以上 DoD／grep 斷言替代契約表（無執行期行為）
