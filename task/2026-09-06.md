# Task — stats 三指標＋checked_by 留痕（修剪審查前置量測）  (created: 2026-09-06 00:30)

> run_id: 2026-09-06-stats-metrics（Tier 1）。需求全文與三個資料源設計見 manifest `spec_inline`（讀本檔＋manifest 即自足）。
> 測試指令：`python3 -m unittest discover -s tests`。單元測試綁進各實作 item（同 diff）。
> 全域約束：eval_gates.py 零變更；全部新欄位**選填**（缺＝無記錄，不回填舊 run、不猜）；stats 缺值語義沿既有「鍵不存在＝無記錄不計分母」原則。
> 本 run 為 **checker 新制首戰**：step 3 預設派 task-verifier；另計畫 game day——本 run 循環中將被刻意中斷、由零 context agent 純照 eval-flow-resume 恢復（中斷點與程序由主 flow 控制，不影響 item 內容）。

## Task 1: 三指標與留痕
- 依賴: 無
- [x] 1.1 eval_state.py set-review 加 --checked-by＋archive 帶入  ~80行  files: .claude/hooks/eval_state.py, tests/test_eval_state.py
      DoD: `set-review <id> <reds>` 加選填 `--checked-by <值>`，合法值 `checker`／`reviewer:①`…`reviewer:⑤`／`reviewer:manual`，寫入 sub_task 的 `checked_by` 欄（未帶＝欄位保持 null）；非法值 exit 非 0＋stderr 列合法值；archive 原樣帶入 eval.json；不變量驗證不動；tests/test_eval_state.py 補：帶值寫入、未帶保持 null、非法值報錯、archive 帶入四案
      契約: set-review 2 0 --checked-by checker → sub_task.checked_by == "checker"；未帶 → null；[邊界] --checked-by reviewer:⑥ → exit 非 0、stderr 含合法值清單、檔案不變
- [x] 1.2 stats.py 三指標  depends: 1.1  ~140行  files: .claude/hooks/stats.py, tests/test_stats.py
      DoD: 新增三節輸出——a. HITL 裁示數：manifest `hitl_rulings`（int）分佈＋平均，缺欄的 run 計「無記錄」；b. checker 升級率：eval.json sub_tasks 的 `checked_by` 統計（checker 直過 n／升級 n（附 reviewer:碼 分佈）／null＝無記錄），升級率＝升級/(直過＋升級)；c. 前置/循環成本比：manifest `subagent_usage`（{"prep": int, "loop": int}）逐 run 列＋合計比值，缺欄計「無記錄」；三節皆缺全部資料時顯示「無記錄（需要 <欄名>）」，不 crash；tests/test_stats.py 補：三指標各有值案＋無記錄案、混合新舊 run 案
      契約: 有 hitl_rulings 的 manifest → 輸出裁示數節含該值；有 checked_by 的 eval.json → 升級率正確（如 2 直過 1 升級 → 33%）；有 subagent_usage → 比值輸出；[邊界] 全部舊 run 無任何新欄 → 三節顯示無記錄、exit 0 不 crash
- [x] 1.3 eval-flow 記錄慣例三處（prose）  depends: 1.1  ~20行  files: skills/eval-flow/SKILL.md
      DoD: ①循環 step 3/4 的 set-review 操作句補「必帶 --checked-by（checker 輪＝checker；升級輪＝reviewer:<碼>；手動觸發＝reviewer:manual）」；②Tier 1 輕量 HITL 與 Tier 2 前置 2 確認句補「確認當下主 flow 把裁示條數寫入 manifest `hitl_rulings`（int，無裁示填 0）」；③step 6 收尾清單句後補「收尾時主 flow 依 Agent 回執把 subagent tokens 彙總寫入 manifest `subagent_usage`（{"prep","loop"}；Tier 1 無前置 agent 填 {"prep":0,...}）」；三處皆指向式、不重列 stats 消費細節；`python3 -m unittest tests.test_docs_consistency` 綠（helper 子命令文件↔實作一致性測試會檢 set-review）
      契約: grep 'checked-by' skills/eval-flow/SKILL.md 命中；grep 'hitl_rulings' 命中；grep 'subagent_usage' 命中；[邊界] step 6 ②的 git add 檔案清單（單一枚舉點）不動 → 原句仍命中

---

# Task — eval-flow 分層＋TODO 清理  (created: 2026-09-06 01:37)

> run_id: 2026-09-06-evalflow-layering（Tier 1）。切分方案全文見 manifest `spec_inline`（讀本檔＋manifest 即自足）。
> 測試指令：`python3 -m unittest discover -s tests`。
> 全域約束：搬移＝**逐字 cut-paste**（機械可驗：搬出段在新檔逐字存在、SKILL.md 不留重複本）；不改寫任何規則語義；eval_gates.py 零變更；phase 設 decomposed（HITL 已過，見 manifest）。

## Task 2: 分層與清理
- 依賴: 無
- [x] 2.1 格式節搬 references/formats.md  ~240行(搬移雙計)  files: skills/eval-flow/SKILL.md, skills/eval-flow/references/formats.md
      DoD: manifest 格式／eval_state 格式／events 格式／操作規則四節（:133-243 附近）逐字搬入新檔 skills/eval-flow/references/formats.md（檔頭加 R-NNN 編輯守衛句＋一行定位說明）；SKILL.md 原位置替換為 ≤5 行——操作紀律（一律用 helper 不手動 Edit、set-review 必帶 --checked-by 兩句保留）＋觸發指向句（init 填欄、resume 對賬、欄位語義查詢時讀 references/formats.md）；逐字驗證：搬出段每行（除節標題層級調整）在新檔 grep 可命中
      契約: 新檔含四節全部內容 → 抽 3 個代表句 grep 新檔命中且 SKILL.md 零命中；SKILL.md 留操作紀律 → grep 'helper' 'checked-by' 原句仍命中；[邊界] step 6 ②git add 清單句（在循環節、非格式節）不動 → grep 原句仍命中
- [x] 2.2 稀有路徑＋gate 清單搬移  depends: 2.1  ~220行(搬移雙計)  files: skills/eval-flow/SKILL.md, skills/eval-flow/references/rare-paths.md, skills/eval-flow/references/gates.md
      DoD: Tier B／Hotfix／單一 run 併發／[P] fan-out 四節逐字搬 references/rare-paths.md；Gate 硬性執行節逐字搬 references/gates.md；兩新檔頭同 2.1 掛守衛句；SKILL.md 原位置各替換為觸發指向句（判 Tier B→讀 rare-paths、使用者宣告 Hotfix→讀 rare-paths、[P] item ≥2 且各 ≥150 行→讀 rare-paths、被 gate 攔須查全貌→讀 gates）；Tier 1 精簡路徑節內對 fan-out 的既有指向改指新檔
      契約: 抽各節代表句 grep 新檔命中且 SKILL.md 零命中；SKILL.md 觸發句 → grep 'rare-paths' ≥3 命中、'gates.md' ≥1 命中；[邊界] 循環與 Tier 1 節不動（除指向改址）
- [x] 2.3 測試同步  depends: 2.1, 2.2  ~50行  files: tests/test_docs_consistency.py
      DoD: MD_FILES 加 `list((ROOT / "skills").glob("*/references/*.md"))`（R-NNN／hook 引用／skill 引用三測試自動涵蓋新檔）；GateListConsistencyTest 的掃描對象由 eval-flow SKILL.md 改為 references/gates.md（README 不動）；HelperSubcommandDocsTest 的 skill 讀取對象改為 references/formats.md；全套測試綠
      契約: 搬移後 `python3 -m unittest tests.test_docs_consistency` 綠；[邊界] 故意在新檔引一個不存在的 R-999 → RetroIdReferencesTest 應 FAIL（驗掃描面真的擴到 references，驗完移除）
- [x] 2.4 [P] TODO.md 清理  ~110行(含刪)  files: TODO.md
      DoD: 已完成節 #1-#5、#7-#10、#13 各壓成一行索引（`## N. 標題 — ✅ 完成日＋一句結論`，細目刪除）；#6（持續追蹤）、#11、#12、#14、#15（治理 v2）保留全文不動；檔頭來源說明保留；壓縮後 wc -l 減 ≥60 行（原寫 ≥80 為估算失準：與「#6/#11/#12/#14/#15 保留全文」互斥——保留內容＋9 索引行＋檔頭＝64 行下限；主 flow 依補表機制裁決修正，2026-09-06）
      契約: grep '出生證' TODO.md 仍命中（§15 不動）；#11/#12/#14 標題與細目仍在；[邊界] 九個已完成節每節恰一行 → grep -c '^## ' 節數不變

---

# Task — game day 流程修補（prose 三項）  (created: 2026-09-06 11:48)

> run_id: 2026-09-06-gameday-flow-fixes（Tier 1）。四缺口背景與逐項修法全文見 manifest `spec_inline`（讀本檔＋manifest 即自足）。
> 測試指令：`python3 -m unittest discover -s tests`。
> 全域約束：全 prose，不動任何 hook 可執行邏輯行（`__suite__` 修改屬 Tier 2 另 run）；既有規則句只增補、不改寫語義（消時間矛盾的輸入第 4 項改寫除外）。

## Task 3: 三項條文增補
- 依賴: 無
- [x] 3.1 視覺類 DoD 的 `[憑據:step5]` 記號（記號定義＋checker 🔍＋step5 收尾核對＋使用者驗收）  ~30行  files: skills/task-decomposition/SKILL.md, .claude/agents/task-verifier.md, skills/eval-flow/SKILL.md, skills/test-strategy/SKILL.md
      DoD: ①task-decomposition 的 DoD 撰寫規則區加記號規則——DoD 條目的憑據只能由 step 5 實跑產生（視覺呈現／實機行為）時，條目尾標 `[憑據:step5]`，記號定義住本 skill（單一枚舉點，Tier 1 主 flow 直建 task 檔經既有指向句同守）；②task-verifier.md 輸入第 4 項「本輪測試輸出尾段（step 5 的驗證指令與結果摘要）」改為 writer mine 自驗輸出尾段（消時間矛盾），工作流程步驟 1 加：帶記號的 DoD 條目 → 完成度節記 🔍（step 5 待驗）、不計入升級②④，記號判定以 task 檔原文機械比對、零裁量；③eval-flow 循環 step 5 加一句：主 flow 於本步逐條核對帶記號條目的憑據已補（實跑輸出或使用者驗收），未補不得通過 gate；④test-strategy 前端／UI 節加使用者驗收路徑：實機環境備不妥的視覺類條目，step 5 列「待使用者驗收」清單請使用者裁決、裁決記入 `local_test_evidence`，自動化功能測試照常硬 gate、本路徑非豁免（不觸發 `test_policy`）；⑤全套測試綠
      契約: grep -l '憑據:step5' → 四檔皆命中；grep 'step 5 的驗證指令' .claude/agents/task-verifier.md → 零命中；[邊界] 無記號 DoD 的 checker 行為不變 → 五類升級與「不確定即升級④」原句 grep 仍命中
- [x] 3.2 step 5 發現 bug 的明文回路＋r 號規則  ~15行  files: skills/test-strategy/SKILL.md, skills/eval-flow/SKILL.md
      DoD: ①test-strategy「真新失敗的處置」的三者皆非段補確定路徑：使用者裁決「是 bug、要修」→ 主 flow 把裁決補進該 item 契約表 row（沿循環 step 1 補表機制，single source 不變）→ 該 sub_task `step` 設 `fixing` → 回派修正 → 從循環 step 3 重走；②eval-flow step 3 審查落檔的 r 號定義補：r 號同 sub_task 連號遞增，不分回路來源（審查退回、step 5 打回、全套重開同一序列）
      契約: grep '補進.*契約表' skills/test-strategy/SKILL.md 命中；grep '不分回路來源' skills/eval-flow/SKILL.md 命中；[邊界] 既有「人是計數器」句原文仍命中（新路徑是裁決後的下游，不取代 HITL）
- [x] 3.3 前置 0 dirty tree 進場檢查  ~12行  files: skills/eval-flow/SKILL.md, skills/eval-flow/references/formats.md
      DoD: ①eval-flow 前置 0 加進場檢查子步（置於建 manifest 之前）：跑 `git status --porcelain`，非空 → 列檔案清單問使用者歸屬（納入本 run／擱置不動），裁決一句寫 manifest `dirty_tree_ruling`（選填欄，乾淨樹免記）；②Tier 1 精簡初始化加同守指向句（指向前置 0 的進場檢查，不重列——R-007）；③references/formats.md 的 manifest 欄位表補 `dirty_tree_ruling` 一行（選填、語義一句）
      契約: grep -c 'dirty_tree_ruling' skills/eval-flow/SKILL.md ≥1 且 skills/eval-flow/references/formats.md ≥1；grep '進場檢查' 在 Tier 1 精簡初始化節命中；[邊界] 乾淨樹 → 不問不記（欄位缺席合法，formats 註明選填）

---

# Task — baseline `__suite__` 毒化修復（重跑確認＋失明警告）  (created: 2026-09-06 13:10)

> run_id: 2026-09-06-baseline-suite-guard（Tier 2）。Spec: spec/2026-09-06-baseline-suite-guard.md；usage（情境 A–E＋HITL 裁示五條）: usage/2026-09-06-baseline-suite-guard.md；impact: impact/2026-09-06-baseline-suite-guard.md；risk: risk/2026-09-06-baseline-suite-guard.md。讀本檔＋上述引用檔即自足。
> 測試指令：`python3 -m unittest discover -s tests`。單元測試綁進各實作 item（同 diff）。
> HITL 裁示（契約硬輸入）：重跑 1 次；判斷式 `'__suite__' in fails`；沿用時不另印警告（交 B2 兜底）；不改 mine 路徑；警告文案由實作定關鍵字、測試斷 substring。
> 全域約束：不動 `_parse_fails`（:58-64，sentinel 來源，三路共用；review 須確認其 diff 為零，見 risk 技術風險#1／impact §5）；不動 mine／related／find_reusable_baseline 沿用路徑（Spec §4）；B1 收斂在 cmd_baseline 實跑路徑 :163-171、B2 收斂在 cmd_check :190 known 建立後（兩機制互不越界，勿混用既有 check 惰性重跑 :197-203）。
> 輸出管道（依 impact §2 慣例）：B1「未重現以重跑為準」屬資訊性走 stdout；B1「兩次皆崩大聲警告」與 B2 失明警告屬警示走 stderr。
> 測試 fixture 全域約束：fixture＝shell stub（沿 test_test_baseline.py `CMD_*` 慣例），逐區塊依據 `_parse_fails`（.claude/hooks/test_baseline.py:58-64）——首跑 `exit 1` 無可解析失敗→ `fails=={"__suite__"}`；重跑 `exit 0`→ 無失敗；重跑印 `FAILED …`→ 個別失敗；首跑vs重跑用計數檔 stub 模式控制（test_test_baseline.py:28-34）。stub 為測試實際載入之 single source，不另存副本。
> B3 裁量記錄：eval-flow SKILL.md:96「baseline 單次快照」**不改**——「單次快照」對正常路徑與個別失敗仍成立（B1 僅在 `__suite__` 場景多跑一次），且 `__suite__` 重跑＋失明行為單一枚舉於 test-strategy（R-007，不製造第二枚舉點）；與 eval-flow:96 語義相容（R-006），故 B3 範圍維持 Spec §2 之 test-strategy 一處。

## Task 4: baseline `__suite__` 毒化修復
- 依賴: 無
- [x] 4.1 B1 `cmd_baseline` 實跑路徑加 `__suite__` 重跑確認分支＋單元測試  ~160行  files: .claude/hooks/test_baseline.py, tests/test_test_baseline.py
      DoD: `run_tests(cmd)`（:163）回傳後、寫檔（:166）前，`'__suite__' in fails`（HITL）→ 複用 `run_tests(cmd)` 重跑 1 次（HITL），第二次結果覆蓋 `stable`；第二次含 `__suite__` → 照記＋stderr 大聲警告（含失明提示、建議 `--fresh`）；第二次不含 → 以第二次為準＋stdout 印「未重現以重跑為準」；維持 `sorted` 與內聯寫檔慣例（`_parse_fails`／沿用路徑不動見 header 全域約束）；單元測試 4 案（沿 header stub 慣例、斷 substring）  情境: A, A-edge1, A-edge2, B
      契約: 首跑 fails=={__suite__}＋重跑 rc=0 無失敗 → stable_failures 不含 __suite__、stdout 含未重現關鍵字、全套執行 2 次；首跑 fails=={__suite__}＋重跑仍 fails=={__suite__} → stable_failures 含 __suite__、stderr 含失明警告關鍵字；[邊界] 首跑解析出個別失敗（'__suite__' not in fails）→ 不重跑、全套執行 1 次、個別失敗照記為 stable；[組合] 首跑 fails=={__suite__}＋重跑解析出個別失敗（rc≠0 有可解析）→ stable_failures 為第二次個別失敗、不含 __suite__、全套執行 2 次
- [x] 4.2 B2 `cmd_check` 讀 baseline 後加失明警告行＋單元測試  ~60行  files: .claude/hooks/test_baseline.py, tests/test_test_baseline.py
      DoD: `known = set(...)`（:190）後，`'__suite__' in known` → stderr 印一行失明警告（本次判定對全套層級失敗失明、關鍵字由實作定）；不改 `new = fails - known` 判定與 exit code；警告依 `known` 判定、來源無關 → 沿用毒化（情境 C）與歷史檔（情境 E）兩路皆覆蓋；單元測試 1 案（仿 write_prev_baseline :130-138 寫 `stable_failures:["__suite__"]` baseline，沿 header stub 慣例、斷 substring）  情境: C, D, D-edge1, E
      契約: baseline stable_failures 含 __suite__＋本次無新失敗 → stderr 含失明警告關鍵字、exit 0；baseline 不含 __suite__ → 無 B2 警告、既有 PASS 行不變；[邊界/組合] baseline known 含 __suite__＋本次全套崩潰 fails=={__suite__} → new 為空、判定 PASS exit 0（原失明行為不改）＋同時印 B2 警告（失明現形）
- [x] 4.3 [P] B3 文件同步：test-strategy Baseline 節補一句  ~15行  files: skills/test-strategy/SKILL.md
      DoD: Baseline 節（:13-24，現述「跑一次，所有失敗記為 stable_failures」處，:20-22 附近）補一句 `__suite__` 套件層失敗重跑確認＋check 端失明警告的指向式描述，不重述實作細節；eval-flow:96 不改（見 header B3 裁量記錄）  情境: D（文件對映 B2／B1 行為描述）
- [x] 4.4 整合測試（B1→B2 串接）＋ mutation self-check  depends: 4.1, 4.2  ~100行  files: tests/test_test_baseline.py
      DoD: 整合涵蓋 B1→B2 串接：建 baseline 遇可重現 `__suite__`（B1 照記＋stderr 警告）→ 對同 run baseline 跑 check（B2 stderr 警告）；及暫時性路徑（B1 首崩重跑不崩→ 不記 __suite__）→ check 無 B2 警告；mutation self-check：破壞 B1 判斷式（改為永不成立）→ 4.1 案 A/A-edge2 應 FAIL、破壞 B2 警告行（刪除）→ 4.2 案應 FAIL（sabotage 後測試須 FAIL，驗完還原）；`python3 -m unittest discover -s tests` 綠  情境: A, B, D
