# Task — stats 三指標＋checked_by 留痕（修剪審查前置量測）  (created: 2026-09-06 00:30)

> run_id: 2026-09-06-stats-metrics（Tier 1）。需求全文與三個資料源設計見 manifest `spec_inline`（讀本檔＋manifest 即自足）。
> 測試指令：`python3 -m unittest discover -s tests`。單元測試綁進各實作 item（同 diff）。
> 全域約束：eval_gates.py 零變更；全部新欄位**選填**（缺＝無記錄，不回填舊 run、不猜）；stats 缺值語義沿既有「鍵不存在＝無記錄不計分母」原則。
> 本 run 為 **checker 新制首戰**：step 3 預設派 task-verifier；另計畫 game day——本 run 循環中將被刻意中斷、由零 context agent 純照 eval-flow-resume 恢復（中斷點與程序由主 flow 控制，不影響 item 內容）。

## Task 1: 三指標與留痕
- 依賴: 無
- [x] 1.1 eval_state.py set-review 加 --checked-by＋archive 帶入  ~80行  files: .claude/hooks/eval_state.py, tests/test_eval_state.py
      DoD: `set-review <id> <reds>` 加選填 `--checked-by <值>`，合法值 `checker`／`reviewer:①`…`reviewer:⑤`／`reviewer:manual`，寫入 sub_task 的 `checked_by` 欄（未帶＝欄位保持 null）；非法值 exit 非 0＋stderr 列合法值；archive 原樣帶入 eval.json；不變量驗證不動；tests/test_eval_state.py 補：帶值寫入、未帶保持 null、非法值報錯、archive 帶入四案
      契約: set-review 2 0 --checked-by checker → sub_task.checked_by == "checker"；未帶 → null；[邊界] --checked-by reviewer:⑥ → exit 非 0、stderr 含合法值清單、檔案不變
- [x] 1.2 stats.py 三指標  depends: 1.1  ~140行  files: .claude/hooks/stats.py, tests/test_stats.py
      DoD: 新增三節輸出——a. HITL 裁示數：manifest `hitl_rulings`（int）分佈＋平均，缺欄的 run 計「無記錄」；b. checker 升級率：eval.json sub_tasks 的 `checked_by` 統計（checker 直過 n／升級 n（附 reviewer:碼 分佈）／null＝無記錄），升級率＝升級/(直過＋升級)；c. 前置/循環成本比：manifest `subagent_usage`（{"prep": int, "loop": int}）逐 run 列＋合計比值，缺欄計「無記錄」；三節皆缺全部資料時顯示「無記錄（需要 <欄名>）」，不 crash；tests/test_stats.py 補：三指標各有值案＋無記錄案、混合新舊 run 案
      契約: 有 hitl_rulings 的 manifest → 輸出裁示數節含該值；有 checked_by 的 eval.json → 升級率正確（如 2 直過 1 升級 → 33%）；有 subagent_usage → 比值輸出；[邊界] 全部舊 run 無任何新欄 → 三節顯示無記錄、exit 0 不 crash
- [x] 1.3 eval-flow 記錄慣例三處（prose）  depends: 1.1  ~20行  files: skills/eval-flow/SKILL.md
      DoD: ①循環 step 3/4 的 set-review 操作句補「必帶 --checked-by（checker 輪＝checker；升級輪＝reviewer:<碼>；手動觸發＝reviewer:manual）」；②Tier 1 輕量 HITL 與 Tier 2 前置 2 確認句補「確認當下主 flow 把裁示條數寫入 manifest `hitl_rulings`（int，無裁示填 0）」；③step 6 收尾清單句後補「收尾時主 flow 依 Agent 回執把 subagent tokens 彙總寫入 manifest `subagent_usage`（{"prep","loop"}；Tier 1 無前置 agent 填 {"prep":0,...}）」；三處皆指向式、不重列 stats 消費細節；`python3 -m unittest tests.test_docs_consistency` 綠（helper 子命令文件↔實作一致性測試會檢 set-review）
      契約: grep 'checked-by' skills/eval-flow/SKILL.md 命中；grep 'hitl_rulings' 命中；grep 'subagent_usage' 命中；[邊界] step 6 ②的 git add 檔案清單（單一枚舉點）不動 → 原句仍命中

---

# Task — eval-flow 分層＋TODO 清理  (created: 2026-09-06 01:37)

> run_id: 2026-09-06-evalflow-layering（Tier 1）。切分方案全文見 manifest `spec_inline`（讀本檔＋manifest 即自足）。
> 測試指令：`python3 -m unittest discover -s tests`。
> 全域約束：搬移＝**逐字 cut-paste**（機械可驗：搬出段在新檔逐字存在、SKILL.md 不留重複本）；不改寫任何規則語義；eval_gates.py 零變更；phase 設 decomposed（HITL 已過，見 manifest）。

## Task 2: 分層與清理
- 依賴: 無
- [x] 2.1 格式節搬 references/formats.md  ~240行(搬移雙計)  files: skills/eval-flow/SKILL.md, skills/eval-flow/references/formats.md
      DoD: manifest 格式／eval_state 格式／events 格式／操作規則四節（:133-243 附近）逐字搬入新檔 skills/eval-flow/references/formats.md（檔頭加 R-NNN 編輯守衛句＋一行定位說明）；SKILL.md 原位置替換為 ≤5 行——操作紀律（一律用 helper 不手動 Edit、set-review 必帶 --checked-by 兩句保留）＋觸發指向句（init 填欄、resume 對賬、欄位語義查詢時讀 references/formats.md）；逐字驗證：搬出段每行（除節標題層級調整）在新檔 grep 可命中
      契約: 新檔含四節全部內容 → 抽 3 個代表句 grep 新檔命中且 SKILL.md 零命中；SKILL.md 留操作紀律 → grep 'helper' 'checked-by' 原句仍命中；[邊界] step 6 ②git add 清單句（在循環節、非格式節）不動 → grep 原句仍命中
- [x] 2.2 稀有路徑＋gate 清單搬移  depends: 2.1  ~220行(搬移雙計)  files: skills/eval-flow/SKILL.md, skills/eval-flow/references/rare-paths.md, skills/eval-flow/references/gates.md
      DoD: Tier B／Hotfix／單一 run 併發／[P] fan-out 四節逐字搬 references/rare-paths.md；Gate 硬性執行節逐字搬 references/gates.md；兩新檔頭同 2.1 掛守衛句；SKILL.md 原位置各替換為觸發指向句（判 Tier B→讀 rare-paths、使用者宣告 Hotfix→讀 rare-paths、[P] item ≥2 且各 ≥150 行→讀 rare-paths、被 gate 攔須查全貌→讀 gates）；Tier 1 精簡路徑節內對 fan-out 的既有指向改指新檔
      契約: 抽各節代表句 grep 新檔命中且 SKILL.md 零命中；SKILL.md 觸發句 → grep 'rare-paths' ≥3 命中、'gates.md' ≥1 命中；[邊界] 循環與 Tier 1 節不動（除指向改址）
- [x] 2.3 測試同步  depends: 2.1, 2.2  ~50行  files: tests/test_docs_consistency.py
      DoD: MD_FILES 加 `list((ROOT / "skills").glob("*/references/*.md"))`（R-NNN／hook 引用／skill 引用三測試自動涵蓋新檔）；GateListConsistencyTest 的掃描對象由 eval-flow SKILL.md 改為 references/gates.md（README 不動）；HelperSubcommandDocsTest 的 skill 讀取對象改為 references/formats.md；全套測試綠
      契約: 搬移後 `python3 -m unittest tests.test_docs_consistency` 綠；[邊界] 故意在新檔引一個不存在的 R-999 → RetroIdReferencesTest 應 FAIL（驗掃描面真的擴到 references，驗完移除）
- [x] 2.4 [P] TODO.md 清理  ~110行(含刪)  files: TODO.md
      DoD: 已完成節 #1-#5、#7-#10、#13 各壓成一行索引（`## N. 標題 — ✅ 完成日＋一句結論`，細目刪除）；#6（持續追蹤）、#11、#12、#14、#15（治理 v2）保留全文不動；檔頭來源說明保留；壓縮後 wc -l 減 ≥60 行（原寫 ≥80 為估算失準：與「#6/#11/#12/#14/#15 保留全文」互斥——保留內容＋9 索引行＋檔頭＝64 行下限；主 flow 依補表機制裁決修正，2026-09-06）
      契約: grep '出生證' TODO.md 仍命中（§15 不動）；#11/#12/#14 標題與細目仍在；[邊界] 九個已完成節每節恰一行 → grep -c '^## ' 節數不變

---

# Task — game day 流程修補（prose 三項）  (created: 2026-09-06 11:48)

> run_id: 2026-09-06-gameday-flow-fixes（Tier 1）。四缺口背景與逐項修法全文見 manifest `spec_inline`（讀本檔＋manifest 即自足）。
> 測試指令：`python3 -m unittest discover -s tests`。
> 全域約束：全 prose，不動任何 hook 可執行邏輯行（`__suite__` 修改屬 Tier 2 另 run）；既有規則句只增補、不改寫語義（消時間矛盾的輸入第 4 項改寫除外）。

## Task 3: 三項條文增補
- 依賴: 無
- [x] 3.1 視覺類 DoD 的 `[憑據:step5]` 記號（記號定義＋checker 🔍＋step5 收尾核對＋使用者驗收）  ~30行  files: skills/task-decomposition/SKILL.md, .claude/agents/task-verifier.md, skills/eval-flow/SKILL.md, skills/test-strategy/SKILL.md
      DoD: ①task-decomposition 的 DoD 撰寫規則區加記號規則——DoD 條目的憑據只能由 step 5 實跑產生（視覺呈現／實機行為）時，條目尾標 `[憑據:step5]`，記號定義住本 skill（單一枚舉點，Tier 1 主 flow 直建 task 檔經既有指向句同守）；②task-verifier.md 輸入第 4 項「本輪測試輸出尾段（step 5 的驗證指令與結果摘要）」改為 writer mine 自驗輸出尾段（消時間矛盾），工作流程步驟 1 加：帶記號的 DoD 條目 → 完成度節記 🔍（step 5 待驗）、不計入升級②④，記號判定以 task 檔原文機械比對、零裁量；③eval-flow 循環 step 5 加一句：主 flow 於本步逐條核對帶記號條目的憑據已補（實跑輸出或使用者驗收），未補不得通過 gate；④test-strategy 前端／UI 節加使用者驗收路徑：實機環境備不妥的視覺類條目，step 5 列「待使用者驗收」清單請使用者裁決、裁決記入 `local_test_evidence`，自動化功能測試照常硬 gate、本路徑非豁免（不觸發 `test_policy`）；⑤全套測試綠
      契約: grep -l '憑據:step5' → 四檔皆命中；grep 'step 5 的驗證指令' .claude/agents/task-verifier.md → 零命中；[邊界] 無記號 DoD 的 checker 行為不變 → 五類升級與「不確定即升級④」原句 grep 仍命中
- [x] 3.2 step 5 發現 bug 的明文回路＋r 號規則  ~15行  files: skills/test-strategy/SKILL.md, skills/eval-flow/SKILL.md
      DoD: ①test-strategy「真新失敗的處置」的三者皆非段補確定路徑：使用者裁決「是 bug、要修」→ 主 flow 把裁決補進該 item 契約表 row（沿循環 step 1 補表機制，single source 不變）→ 該 sub_task `step` 設 `fixing` → 回派修正 → 從循環 step 3 重走；②eval-flow step 3 審查落檔的 r 號定義補：r 號同 sub_task 連號遞增，不分回路來源（審查退回、step 5 打回、全套重開同一序列）
      契約: grep '補進.*契約表' skills/test-strategy/SKILL.md 命中；grep '不分回路來源' skills/eval-flow/SKILL.md 命中；[邊界] 既有「人是計數器」句原文仍命中（新路徑是裁決後的下游，不取代 HITL）
- [x] 3.3 前置 0 dirty tree 進場檢查  ~12行  files: skills/eval-flow/SKILL.md, skills/eval-flow/references/formats.md
      DoD: ①eval-flow 前置 0 加進場檢查子步（置於建 manifest 之前）：跑 `git status --porcelain`，非空 → 列檔案清單問使用者歸屬（納入本 run／擱置不動），裁決一句寫 manifest `dirty_tree_ruling`（選填欄，乾淨樹免記）；②Tier 1 精簡初始化加同守指向句（指向前置 0 的進場檢查，不重列——R-007）；③references/formats.md 的 manifest 欄位表補 `dirty_tree_ruling` 一行（選填、語義一句）
      契約: grep -c 'dirty_tree_ruling' skills/eval-flow/SKILL.md ≥1 且 skills/eval-flow/references/formats.md ≥1；grep '進場檢查' 在 Tier 1 精簡初始化節命中；[邊界] 乾淨樹 → 不問不記（欄位缺席合法，formats 註明選填）

---

# Task — baseline `__suite__` 毒化修復（重跑確認＋失明警告）  (created: 2026-09-06 13:10)

> run_id: 2026-09-06-baseline-suite-guard（Tier 2）。Spec: spec/2026-09-06-baseline-suite-guard.md；usage（情境 A–E＋HITL 裁示五條）: usage/2026-09-06-baseline-suite-guard.md；impact: impact/2026-09-06-baseline-suite-guard.md；risk: risk/2026-09-06-baseline-suite-guard.md。讀本檔＋上述引用檔即自足。
> 測試指令：`python3 -m unittest discover -s tests`。單元測試綁進各實作 item（同 diff）。
> HITL 裁示（契約硬輸入）：重跑 1 次；判斷式 `'__suite__' in fails`；沿用時不另印警告（交 B2 兜底）；不改 mine 路徑；警告文案由實作定關鍵字、測試斷 substring。
> 全域約束：不動 `_parse_fails`（:58-64，sentinel 來源，三路共用；review 須確認其 diff 為零，見 risk 技術風險#1／impact §5）；不動 mine／related／find_reusable_baseline 沿用路徑（Spec §4）；B1 收斂在 cmd_baseline 實跑路徑 :163-171、B2 收斂在 cmd_check :190 known 建立後（兩機制互不越界，勿混用既有 check 惰性重跑 :197-203）。
> 輸出管道（依 impact §2 慣例）：B1「未重現以重跑為準」屬資訊性走 stdout；B1「兩次皆崩大聲警告」與 B2 失明警告屬警示走 stderr。
> 測試 fixture 全域約束：fixture＝shell stub（沿 test_test_baseline.py `CMD_*` 慣例），逐區塊依據 `_parse_fails`（.claude/hooks/test_baseline.py:58-64）——首跑 `exit 1` 無可解析失敗→ `fails=={"__suite__"}`；重跑 `exit 0`→ 無失敗；重跑印 `FAILED …`→ 個別失敗；首跑vs重跑用計數檔 stub 模式控制（test_test_baseline.py:28-34）。stub 為測試實際載入之 single source，不另存副本。
> B3 裁量記錄：eval-flow SKILL.md:96「baseline 單次快照」**不改**——「單次快照」對正常路徑與個別失敗仍成立（B1 僅在 `__suite__` 場景多跑一次），且 `__suite__` 重跑＋失明行為單一枚舉於 test-strategy（R-007，不製造第二枚舉點）；與 eval-flow:96 語義相容（R-006），故 B3 範圍維持 Spec §2 之 test-strategy 一處。

## Task 4: baseline `__suite__` 毒化修復
- 依賴: 無
- [x] 4.1 B1 `cmd_baseline` 實跑路徑加 `__suite__` 重跑確認分支＋單元測試  ~160行  files: .claude/hooks/test_baseline.py, tests/test_test_baseline.py
      DoD: `run_tests(cmd)`（:163）回傳後、寫檔（:166）前，`'__suite__' in fails`（HITL）→ 複用 `run_tests(cmd)` 重跑 1 次（HITL），第二次結果覆蓋 `stable`；第二次含 `__suite__` → 照記＋stderr 大聲警告（含失明提示、建議 `--fresh`）；第二次不含 → 以第二次為準＋stdout 印「未重現以重跑為準」；維持 `sorted` 與內聯寫檔慣例（`_parse_fails`／沿用路徑不動見 header 全域約束）；單元測試 4 案（沿 header stub 慣例、斷 substring）  情境: A, A-edge1, A-edge2, B
      契約: 首跑 fails=={__suite__}＋重跑 rc=0 無失敗 → stable_failures 不含 __suite__、stdout 含未重現關鍵字、全套執行 2 次；首跑 fails=={__suite__}＋重跑仍 fails=={__suite__} → stable_failures 含 __suite__、stderr 含失明警告關鍵字；[邊界] 首跑解析出個別失敗（'__suite__' not in fails）→ 不重跑、全套執行 1 次、個別失敗照記為 stable；[組合] 首跑 fails=={__suite__}＋重跑解析出個別失敗（rc≠0 有可解析）→ stable_failures 為第二次個別失敗、不含 __suite__、全套執行 2 次
- [x] 4.2 B2 `cmd_check` 讀 baseline 後加失明警告行＋單元測試  ~60行  files: .claude/hooks/test_baseline.py, tests/test_test_baseline.py
      DoD: `known = set(...)`（:190）後，`'__suite__' in known` → stderr 印一行失明警告（本次判定對全套層級失敗失明、關鍵字由實作定）；不改 `new = fails - known` 判定與 exit code；警告依 `known` 判定、來源無關 → 沿用毒化（情境 C）與歷史檔（情境 E）兩路皆覆蓋；單元測試 1 案（仿 write_prev_baseline :130-138 寫 `stable_failures:["__suite__"]` baseline，沿 header stub 慣例、斷 substring）  情境: C, D, D-edge1, E
      契約: baseline stable_failures 含 __suite__＋本次無新失敗 → stderr 含失明警告關鍵字、exit 0；baseline 不含 __suite__ → 無 B2 警告、既有 PASS 行不變；[邊界/組合] baseline known 含 __suite__＋本次全套崩潰 fails=={__suite__} → new 為空、判定 PASS exit 0（原失明行為不改）＋同時印 B2 警告（失明現形）
- [x] 4.3 [P] B3 文件同步：test-strategy Baseline 節補一句  ~15行  files: skills/test-strategy/SKILL.md
      DoD: Baseline 節（:13-24，現述「跑一次，所有失敗記為 stable_failures」處，:20-22 附近）補一句 `__suite__` 套件層失敗重跑確認＋check 端失明警告的指向式描述，不重述實作細節；eval-flow:96 不改（見 header B3 裁量記錄）  情境: D（文件對映 B2／B1 行為描述）
- [x] 4.4 整合測試（B1→B2 串接）＋ mutation self-check  depends: 4.1, 4.2  ~100行  files: tests/test_test_baseline.py
      DoD: 整合涵蓋 B1→B2 串接：建 baseline 遇可重現 `__suite__`（B1 照記＋stderr 警告）→ 對同 run baseline 跑 check（B2 stderr 警告）；及暫時性路徑（B1 首崩重跑不崩→ 不記 __suite__）→ check 無 B2 警告；mutation self-check：破壞 B1 判斷式（改為永不成立）→ 4.1 案 A/A-edge2 應 FAIL、破壞 B2 警告行（刪除）→ 4.2 案應 FAIL（sabotage 後測試須 FAIL，驗完還原）；`python3 -m unittest discover -s tests` 綠  情境: A, B, D

---

# Task — 分級制度 v2（agentflow 理由碼制）  (created: 2026-09-06 13:19)

> run_id: 2026-09-06-tier-router-v2（Tier 1）。設計五點＋不做清單全文見 manifest `spec_inline`（讀本檔＋manifest 即自足）；agentflow 原文在 scratchpad clone（不進 git，引用處以「agentflow SKILL.md／ag.md」註明即可）。
> 測試指令：`python3 -m unittest discover -s tests`。
> 全域約束：全 prose，不動任何 hook 可執行邏輯行（AGENT_MIN_PHASE 零變更——advisor 點名排在 phase=decomposed 後）；Tier 0／Tier B／Hotfix 條文不動；「本體 vs 使用」兩段判準原文沿用（搬移措辭、不改語義）；歷史 task 檔與 usage-scenario-analysis 的既有「高風險面」字樣不回改（R-006 盤點：相容）。

## Task 5: Router v2 與同步
- 依賴: 無
- [x] 5.1 CLAUDE.md Router 節重寫（理由碼制）  ~70行  files: CLAUDE.md
      DoD: ①「難易度分級」節改兩步制——第一步 complexity_reasons 五碼表（重大不確定／跨子系統協調／公開介面／落地資料契約／信任邊界／未決重大決策，各附一句判準），附「本體 vs 使用」沿用句與「本地開發工具鏈不因檔案類別自動觸碼」句；第二步 tier 表——Tier 0 條件不動、Tier 1＝理由碼空或僅剩具名問題可由點名 advisor 回答＋預估 active time ≤120 分、Tier 2＝信任邊界／公開契約本體 OR 不確定無法收斂為具名問題 OR >120 分或請求不連貫 OR all-in、Tier B 不動；②行數退出 Tier 1／2 判級（Tier 0 門檻保留），manifest 記 estimated_active_minutes（選填）；③新增「事後降級門」條（前置 1 前、須使用者確認、執行中升級不可逆不變）與「all-in」條（使用者明說才觸發、agent 不可自行宣告或建議、觸發時直寫捷徑／合併審查／降級門全關、預設抄捷徑跳過留痕一句）；④防濫用規則同步：「高風險面的邊界」改「信任邊界／公開契約的邊界」（兩段判準原文沿用）、升級逃生門的「預估遠超 300 行」改「預估 active time 將超 120 分」；⑤工作型態前判尾句改「理由碼空 → Tier 0／1；非空 → Tier 2」；⑥Minimality 尾註一句（不做清單指向 manifest spec_inline）
      契約: grep 'complexity_reasons' CLAUDE.md 命中；grep '信任邊界' 命中且防濫用節 '高風險面的邊界' 零命中；grep 'all-in' ≥2 命中；grep '降級門' 命中；[邊界] Tier 0 行與 Tier B 行原文不動 → grep '80 行' 與 'Bootstrap' 原句仍命中；「本體 vs 使用」關鍵句（'僅呼叫／經過既有機制＝使用'）仍命中
- [x] 5.2 eval-flow＋formats 同步  depends: 5.1  ~30行  files: skills/eval-flow/SKILL.md, skills/eval-flow/references/formats.md
      DoD: ①Tier 1 節首句「不變更高風險面本體」改指理由碼空集合（指向 CLAUDE.md Router，不重列五碼——R-007）；「風險由 Router 的排除條件把關」句同步改寫；②Tier 1 輕量 HITL 步驟加「點名 advisor」機制：有具名重大問題→HITL 提報「問題原文＋advisor 名」，確認後跑該 advisor（usage-analyzer 或 impact-analyzer，產出照其定義存檔並回寫 manifest 對應欄），再進循環；點名必附問題原文；③「小 prose item 合併」條改寫為合併審查放寬（同一連貫變更＋合計 diff ≤約 400 行→併一輪 checker，輸入集為各 item DoD／契約聯集；all-in 關）；④Tier 1 直寫捷徑條加「本捷徑適用全 tier（Tier 2 同守則）；all-in 關」；⑤formats.md manifest 欄位加 estimated_active_minutes／actual_active_minutes 一行（選填）
      契約: grep '點名' skills/eval-flow/SKILL.md 命中且同段含 'usage-analyzer'；grep '合併審查' 或 '併一輪' 命中；grep 'estimated_active_minutes' formats.md 命中；[邊界] AGENT_MIN_PHASE 零變更 → git diff 不含 .claude/hooks/；hook gate 相關句（'phase 仍須 decomposed'）原文仍命中
- [x] 5.3 test-strategy 豁免句同步＋全套測試  depends: 5.1  ~5行  files: skills/test-strategy/SKILL.md
      DoD: ①「豁免不改變 Tier 準入條件：高風險面照樣進不了 Tier 0／1」改為信任邊界／公開契約語彙（語義不變）；②`python3 -m unittest discover -s tests` 綠（docs 一致性測試涵蓋全部改動檔）
      契約: grep '高風險面照樣' skills/test-strategy/SKILL.md 零命中；grep '信任邊界' 命中；[邊界] 豁免窗口其餘句原文不動 → grep 'waived_by_user' 仍命中

---

# Task — 文件瘦身 sweep（#1–#9＋兩矛盾）  (created: 2026-09-06 13:40)

> run_id: 2026-09-06-doc-slimming（Tier 1）。九項清單與兩矛盾裁決全文見 manifest `spec_inline`（讀本檔＋manifest 即自足）。
> 測試指令：`python3 -m unittest discover -s tests`。
> 全域約束：只刪重述與死條款、只搬不改寫（兩層制三子彈逐字搬）；**不刪任何 R-NNN 標記規則本體**（反模式重述條目不算規則本體）；報告信封×7 與防注入段不動；`_deprecated` 不動。四 items 屬同一連貫變更，依 v2 合併審查併一輪 checker（合計 diff 預估 ~220 行 ≤400）。

## Task 6: 瘦身與矛盾修正
- 依賴: 無
- [x] 6.1 agent／skill 對去重＋四五要素矛盾  ~35行  files: .claude/agents/retro.md, .claude/agents/task-decomposer.md
      DoD: ①retro.md frontmatter 加 `skills: root-cause-table`，刪內文「根因分類參考」節（分類表＋分析框架複本），原位置留一句指向；②task-decomposer.md「拆分上限」節改指向 task-decomposition skill（單一枚舉點），刪硬上限／軟上限細節與「五要素」句（裁決：四要素為準，[P]／depends 依 skill Step 4 另判），保留「每情境至少一 item 對映」句
      契約: grep -c '知識不足' .claude/agents/retro.md → 0 且 skills/root-cause-table/SKILL.md → 1；grep '五要素' .claude/agents/task-decomposer.md → 零命中；[邊界] retro.md frontmatter `skills:` 存在（test_docs_consistency 的 frontmatter skills 測試綠）；decomposer 自檢四大範疇原文不動 → grep 'Clarity' 仍命中
- [x] 6.2 CLAUDE.md 瘦身＋eval-flow 死條款＋兩層制搬家  ~50行  files: CLAUDE.md, skills/bug-diagnosis/SKILL.md, skills/eval-flow/SKILL.md, retro/BUGLOG.md
      DoD: ①CLAUDE.md「bugfix retro 兩層制」三子彈逐字搬入 bug-diagnosis 新節「retro 兩層制」，CLAUDE.md 原位置壓一行指向；bug-diagnosis「預防問」的答案去向句改指本 skill 新節；BUGLOG.md 檔頭「規則見 CLAUDE.md」改指 bug-diagnosis；②CLAUDE.md Task Principle 五個目錄 bullet 壓一行指向（自足性、當天日期、subagent、[x]、拆分指向句保留）；③eval-flow 刪 step 3「生效時點」bullet、「task-verifier 定位」bullet 壓成只留回退機制 v3 句、step 7 適用範圍句去歷史框架留語義
      契約: grep '證據層' CLAUDE.md 零命中且 skills/bug-diagnosis/SKILL.md 命中；grep '生效時點' skills/eval-flow/SKILL.md 零命中；grep '回退機制' skills/eval-flow/SKILL.md 仍命中；[邊界] 搬移逐字：三子彈代表句（'不累積等於白修'、'升級判定是機械 grep'）在 bug-diagnosis grep 命中且 CLAUDE.md 零命中
- [x] 6.3 parallel-run／rare-paths 單向化  ~55行  files: skills/parallel-run/SKILL.md, skills/eval-flow/references/rare-paths.md
      DoD: ①parallel-run「被 Tier 2 fan-out 重用」節（重用清單＋分岔點）壓成 ~4 行單向指向（本 skill 步驟 5／6／7–10／11–13／14 為機制單一來源；分岔點住 rare-paths fan-out 節）；②rare-paths ③段刪「①-⑤」重點提示複本，留引用句（含「批次層 baseline 須手動帶 --cmd」可保留於 parallel-run 既有 8.4，不重列）
      契約: grep -c '重用清單' skills/parallel-run/SKILL.md → 0；grep '改任何一處時須對照另一端' → 零命中；rare-paths grep '重點提示' → 零命中；[邊界] parallel-run 步驟 5–14 本體原文不動 → grep '起手三步' 與 '原地凍結' 仍命中；rare-paths 對 parallel-run 步驟 7–10 的引用句仍在
- [x] 6.4 範例／反模式／reviewer 清單瘦身  ~80行  files: skills/usage-scenario-analysis/SKILL.md, skills/task-decomposition/SKILL.md, .claude/agents/code-reviewer.md
      DoD: ①usage-scenario-analysis 刪整節「範例」（task-decomposition 範例保留不動）；②兩 skill「反模式」節刪與前文硬性規則同義的重述條目、保留模式辨識型條目（保留清單見 spec_inline #8；規則本體與 R 標記規則零變動）；③code-reviewer 範疇 1、2 的通用 bullet 各壓成一行（範疇 3-5、Fowler 12 條、測試覆蓋節不動）
      契約: grep '部分沖銷' skills/usage-scenario-analysis/SKILL.md → 零命中且 skills/task-decomposition/SKILL.md 仍命中；grep 'SQL injection' .claude/agents/code-reviewer.md ≤1（壓縮後單行內）；[邊界] Fowler 12 條原文不動 → grep 'Mysterious Name' 與 'Refused Bequest' 仍命中；兩 skill 反模式節仍存在（非整節刪除）→ grep '反模式' 各 ≥1

---

# Task — _deprecated 歸檔目錄汰除  (created: 2026-09-06 13:52)

> run_id: 2026-09-06-deprecated-cleanup（Tier 1）。三分法分類與依據見 manifest `spec_inline`。
> 測試指令：`python3 -m unittest discover -s tests`。
> 全域約束：doctor.py／init.sh 的 `_deprecated` 排除邏輯零變更（防禦性保留，3c 合成測試覆蓋）；test_agent_refs.py 歷史註解不動。

## Task 7: 汰除
- 依賴: 無
- [x] 7.1 刪 skills/_deprecated/ 五目錄＋A 類測試隨刪  ~270行(刪)  files: skills/_deprecated/, tests/test_doctor.py
      DoD: ①`git rm -r skills/_deprecated`（五個歸檔 skill，248 行）；②tests/test_doctor.py 刪 `DeprecatedSkillsIntegrationTest` 整 class（A 類：主題＝真實目錄 E2E，依據＝spec_inline 作廢清單）；3c 兩個合成測試原文不動（B 類）；③全套測試綠
      契約: ls skills/_deprecated → 不存在；grep 'DeprecatedSkillsIntegrationTest' tests/test_doctor.py → 零命中；[邊界] grep -c 'test_deprecated_dir_not_reported_as_repo_only' tests/test_doctor.py → 1 且 doctor.py:49／init.sh:90 排除行原文仍在；全套 unittest 綠（測試數較 273 減少＝僅該 class 的案數）

## Task 8: skill 瘦身——eval-flow 家族（run: 2026-09-06-skill-prose-slimming）
- 依賴: 無
- 共通 DoD（8.*／9.* 全 item 適用，單一枚舉點）: ①item 範圍內 >400 字元物理行 → 0（`awk 'length($0)>400'` 驗；**YAML frontmatter 的 description 單行字串除外**——拆行會破壞 harness 解析，不動）②外移的每則敘事：原位留（R-NNN）引用、retro/RETRO.md 有對應條目（缺者依約束句式新增，R-011 起連號、append-only）③控制力不變：hook 指令／欄位名／閾值逐字保留、（R-NNN）標記總數不減、gate 條件語義不變④全套 unittest 無新增穩定失敗
- [x] 8.1 eval-flow/SKILL.md 去重＋「前置 0–3」節拆段外移  ~70行  files: skills/eval-flow/SKILL.md, retro/RETRO.md
      DoD: 資料格式節與 formats.md 逐字重複的兩段（helper 清單、set-review）改指向句，全文只住 formats.md；指向句保留三控制點（checker 輪填 0、升級輪 --dimensions 必填、commit gate 必填 🔴數）＋共通 DoD
      契約: grep -F '實測單一 run 手動 Edit 30+' skills/eval-flow/SKILL.md → 0 且 references/formats.md → 1；grep -F '五維詞彙' skills/eval-flow/SKILL.md → 0 且 formats.md ≥1；[邊界] 指向句三控制點字樣（固定 0、--dimensions、擋歸檔）各 ≥1 於 SKILL.md
- [x] 8.2 eval-flow/SKILL.md「循環 step 1–4」拆段外移  ~90行  files: skills/eval-flow/SKILL.md, retro/RETRO.md
      DoD: step 1 知識前置／契約前置、step 3 五類升級／引文核實、step 4 處置各條的內嵌子分支拆為巢狀子清單＋共通 DoD
      契約: awk 'length($0)>400' 該節範圍 → 0；[邊界] grep -F '硬湊綠燈才是違規' → 1（仲裁句原文在）
- [x] 8.3 eval-flow/SKILL.md「step 5–7＋Model／Subagent／憑據紀律／Tier 1／Tier B／Hotfix 節」拆段外移  ~80行  files: skills/eval-flow/SKILL.md, retro/RETRO.md
      DoD: 共通 DoD；本 item 收尾時全檔 >400 字元行 → 0
      契約: awk 'length($0)>400' skills/eval-flow/SKILL.md | wc -l → 0；[邊界] grep -F '≤2 tasks、合計 ≤8 items' → 1
- [x] 8.4 references/formats.md＋gates.md 拆段外移  ~85行  files: skills/eval-flow/references/formats.md, skills/eval-flow/references/gates.md, retro/RETRO.md
      DoD: 共通 DoD；formats.md 作為 8.1 指向目標的兩段全文完整保留
      契約: awk 兩檔 >400 → 0；[邊界] grep -F '五維詞彙' formats.md ≥1、grep -F 'set-review' formats.md ≥2
- [x] 8.5 references/rare-paths.md 拆段外移  ~70行  files: skills/eval-flow/references/rare-paths.md, retro/RETRO.md
      DoD: 共通 DoD
      契約: awk 'length($0)>400' rare-paths.md → 0；[邊界] grep -c 'Parent-Run-Id' rare-paths.md 計數不變

## Task 9: skill 瘦身——其餘五 skill（run: 2026-09-06-skill-prose-slimming）
- 依賴: 無（與 Task 8 不共檔；retro/RETRO.md 為 append-only 循序寫，不並行）
- [x] 9.1 test-strategy/SKILL.md 拆段外移  ~115行  files: skills/test-strategy/SKILL.md, retro/RETRO.md（>100 行 → 派 code-writer）
      DoD: 共通 DoD；「實測」型故事外移，判準內嵌參數（閾值、次數上限）保留
      契約: awk 'length($0)>400' → 0；[邊界] grep -F '2 次 sabotage 仍綠即停手' → 1
- [x] 9.2 task-decomposition/SKILL.md 拆段外移  ~95行  files: skills/task-decomposition/SKILL.md, retro/RETRO.md
      DoD: 共通 DoD；×2 校準倍率、行數預算數字為規則參數不外移
      契約: awk 'length($0)>400' → 0；[邊界] grep -c '×2' ≥ 改前值、grep -F '≤ **12 行**' 或同義預算句 → 1
- [x] 9.3 usage-scenario-analysis＋parallel-run＋bug-diagnosis 三檔拆段外移  ~95行  files: skills/usage-scenario-analysis/SKILL.md, skills/parallel-run/SKILL.md, skills/bug-diagnosis/SKILL.md, retro/RETRO.md
      DoD: 共通 DoD
      契約: awk 三檔 >400 → 0；[邊界] grep -F '起手三步' skills/parallel-run/SKILL.md ≥1、grep -F '[DEBUG-' skills/bug-diagnosis/SKILL.md ≥1

## Task 10: agent 定義補「不變量＋失敗模式」二節（run: 2026-09-06-agent-defs-invariants）
- 依賴: 無
- 共通 DoD（10.* 全 item）: ①兩節存在且每條目附指向（規則編號／節名），內容細則不重述（R-007 指向式）②兩節每條可對映檔內既有規則原文（不新增行為義務）③frontmatter 逐字不動④全套 unittest 無新增穩定失敗
- [x] 10.1 code-writer.md 補二節  ~45行  files: .claude/agents/code-writer.md
      DoD: 共通 DoD；不變量涵蓋：仲裁記錄、2 次上限、sabotage 證據、契約表上下限、範圍外零修改、報告信封
      契約: grep -c '## 不變量' → 1、grep -c '## 失敗模式' → 1；git diff 前 10 行（frontmatter）零變更；[邊界] grep '規則 4' 於不變量節命中（指向式非重述）
- [x] 10.2 task-verifier.md 補二節＋收斂工作守則既有重複  ~40行  files: .claude/agents/task-verifier.md
      DoD: 共通 DoD；工作守則中與五種升級節重複的斷言句（存在性缺席→②、不確定即升級④）搬入不變量節、原位不留複本
      契約: grep -c '存在性缺席 → 建議②' 全檔 → 1（搬移非複製）；grep -c '## 不變量' → 1；[邊界] 「不設第 6 類」原句仍命中
- [x] 10.3 code-reviewer.md 補二節  ~45行  files: .claude/agents/code-reviewer.md
      DoD: 共通 DoD；不變量涵蓋：兩節缺一無效、行號現查、引文當場複製、維度統計行、雙軸分離、信封
      契約: grep -c '## 不變量' → 1、'## 失敗模式' → 1；[邊界] Fowler 12 條唯一枚舉點宣告原句仍命中；grep '硬性節——缺此節報告無效' 仍命中

## Task 11: Tier 0／1 執行留痕＋時間戳（run: 2026-09-06-tier01-telemetry）
- 依賴: 無
- 共通 DoD（11.* 全 item 適用）: 全套 unittest（python3 -m unittest discover -s tests）無新增穩定失敗；既有 gate 行為不變（純新增記錄面，無 gate 消費新欄）
- [x] 11.1 eval_state.py 新增 `event` 子命令（不依賴 eval_state.json）＋單元測試  ~55行  files: .claude/hooks/eval_state.py, tests/test_eval_state.py
      DoD: `python3 .claude/hooks/eval_state.py event <run_id> <name> [--note <str>]` 經既有 append_event 寫 run/<run_id>.events.jsonl，不呼叫 load()（eval_state.json 不存在也成功）；事件行含 ts（ISO8601 UTC）／cmd=<name>／args.note；寫入失敗僅 stderr warning（旁路不得變主路，語義同 append_event）；共通 DoD
      契約: 無 eval_state.json 環境下跑 `event demo-tmp milestone --note x` → exit 0 且 run/demo-tmp.events.jsonl 尾行 json 含 "milestone" 與 "ts"；[邊界] eval_state.json 不存在時 exit 0；tests/test_eval_state.py 有 event 子命令測試 ≥2 條斷言
- [x] 11.2 eval_state.py 新增 `tier0` 子命令（run/tier0.jsonl 一行留痕）＋單元測試  ~50行  files: .claude/hooks/eval_state.py, tests/test_eval_state.py
      DoD: `tier0 --summary "<一句>" --files "<逗號清單>" --lines <int>` append 一行 `{"ts","summary","files":[...],"lines"}` 到 run/tier0.jsonl（append-only、ts ISO8601 UTC、run/ 不存在自動建）；共通 DoD
      契約: 跑一次後 `tail -1 run/tier0.jsonl` 可 json.loads 且含 ts/summary/files/lines 四鍵、files 為 list；[邊界] 連跑兩次 → 檔案兩行（不覆寫）
- [x] 11.3 stats.py 新增「Tier 0 留痕」節＋單元測試  ~45行  files: .claude/hooks/stats.py, tests/test_stats.py
      DoD: 讀 run/tier0.jsonl 輸出筆數／合計 lines／最近一筆 ts；檔不存在顯示「無記錄」；壞行寬容跳過（同 _parse_events 慣例）；Tier 1 事件由既有事件節自動涵蓋（消費端 keyed by manifest，免改）；共通 DoD
      契約: 造一行合法 tier0.jsonl 後 stats.py 輸出含 `Tier 0`；移除檔後輸出含 `無記錄`；[邊界] 壞行混入時不 crash 且筆數只計合法行
- [x] 11.4 文件投放：CLAUDE.md Tier 0 行＋SKILL.md Tier 1 事件呼叫點＋formats.md 已知限制解除  ~60行  files: CLAUDE.md, skills/eval-flow/SKILL.md, skills/eval-flow/references/formats.md
      DoD: ①CLAUDE.md Router Tier 0 路徑「直接改，不建任何檔」改為「直接改；收尾 append 一行 tier0 留痕（`eval_state.py tier0`，格式住 eval-flow formats.md）」；②SKILL.md Tier 1 精簡路徑增事件呼叫點（init 後／HITL 確認後／每 item 審查落檔後／step 5 驗證後／收尾 commit 前，各 `eval_state.py event <run_id> <節點名>`）；③SKILL.md step 6 ②的「（若存在；Tier 1 無 `eval_state.json` 故無此檔）」改「（若存在）」、formats.md events 節「已知限制：Tier 1 …故無此檔」改寫為 Tier 1 以 event 子命令寫入；④SKILL.md frontmatter「不建任何檔」同步改；外層 work/CLAUDE.md 不在 repo、由 init.sh 部署帶到，不動；共通 DoD
      契約: `grep -F '不建任何檔' CLAUDE.md skills/eval-flow/SKILL.md` → 0；`grep -c 'eval_state.py event' skills/eval-flow/SKILL.md` ≥1；`grep -rF '故無此檔' skills/` → 0；[邊界] formats.md events 節「旁路」語義行仍命中（grep -F '旁路' formats.md ≥1）
