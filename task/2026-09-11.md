# Task 2026-09-11

> run_id: 2026-09-11-retro-lifecycle（Tier 1 精簡路徑）
> spec_inline 見 `run/2026-09-11-retro-lifecycle.json`。目標：解決 retro 舊約束與新功能打架——加約束適用條件與前提錨點、retired 生命週期、貼入前衝突仲裁與鮮度檢查。
> 三 item 同源（同一設計決策）、合計 diff 一輪可讀 → **合併為一輪 checker 審**（輸入集為三 item DoD/契約聯集）。

## Task 1：retro 記憶生命週期與衝突防線

### Item 1：retro agent 輸出格式加適用條件與前提錨點
- **files**: `.claude/agents/retro.md`
- **做什麼**: 改「輸出格式」節的約束句式規範——(a) 約束句（粗體「約束：」之後）必須以**適用條件**開頭（句型如「當 <機制/檔案> 存在時」「做 <某類操作> 時」）；(b) 每條 bullet 尾端標**前提錨點** `［錨點: <檔案/helper/機制名>］`——約束所依賴、一旦不存在該約束即失效的實體；(c)「寫作檢驗」小節加一條：「這條約束在什麼情況下不適用？答不出來＝過度一般化，重寫。」
- **DoD**:
  - [ ] 輸出格式節出現「適用條件」要求，並給出至少一個條件開頭的句型範例
  - [ ] 輸出格式節出現「前提錨點」要求，格式標記 `［錨點: ...］` 明列，並說明錨點＝約束失效的判定依據（供消費端鮮度檢查）
  - [ ] 寫作檢驗小節含「在什麼情況下不適用／答不出來即重寫」該條
  - [ ] 明載「既有 R-001~R-017 條目不回改」（比照既有標籤慣例的不回改句）
- **契約**:
  | 情境 | 輸入 | 預期輸出 |
  |---|---|---|
  | 新約束句 | retro 產一條新教訓 | 句首為適用條件、bullet 尾有 `［錨點: X］`、通過「何時不適用」檢驗 |
  | 既有條目 | R-001~R-017 | 不被回改（格式不強制回溯） |

### Item 2：RETRO.md 檔頭加 retired 生命週期規則
- **files**: `retro/RETRO.md`
- **做什麼**: 檔頭（ID 規則段之後）新增 retired 規則段——(a) 條目可於行首 ID 後標 `［retired YYYY-MM-DD <run_id>］`；(b) retire 觸發時機：前提錨點失效、或約束已下沉為 hook/lint（prose 版與機械版並存會打架）；(c) **retire 必經使用者確認，agent 不可自行標**（比照驗證豁免/降級門防濫用原則）；(d) retired 條目保留在檔（append-only 不刪），但主 flow 知識前置 grep 篩選時排除。
- **DoD**:
  - [ ] 檔頭出現 retired 標記格式 `［retired YYYY-MM-DD <run_id>］` 定義
  - [ ] 明載 retire 兩種觸發時機（前提錨點失效／已下沉機械防線）
  - [ ] 明載「必經使用者確認、agent 不可自行標」
  - [ ] 明載 retired 條目不刪、但被知識前置 grep 排除（與 eval-flow step 1 呼應）
  - [ ] 30 條增長控制規則與 retired 的關係說明（retired 不佔活躍額度的計數口徑，或明載仍計入——擇一寫死避免歧義）
- **契約**:
  | 情境 | 輸入 | 預期輸出 |
  |---|---|---|
  | 前提失效條目 | 使用者確認 retire R-00X | 條目標 retired、留在檔、grep 前置不再選中 |
  | agent 自行判定 | agent 想 retire | 禁止——須回報使用者確認 |

### Item 3：eval-flow step 1 知識前置加衝突仲裁、retired 排除、鮮度檢查（含兩副本同步）
- **files**: `skills/eval-flow/SKILL.md`（**同步鏡像** `~/.claude/skills/eval-flow/SKILL.md`，內容須逐字一致）
- **做什麼**: 改循環 step 1「知識前置」的 retro 條目子項——(a) grep 篩選後**排除標 retired 的條目**；(b) 選中條目貼進 prompt **前**，對每條的**前提錨點**做鮮度檢查（grep 錨點檔案/函式是否仍存在）——不存在則**不貼該條**、列入「retire 候選」於收尾回報使用者；(c) 新增**衝突仲裁規則**：retro 約束與本 item 的 Spec/DoD/契約表衝突時 **Spec 優先**，但 writer/主 flow **不可靜默選邊**，須在交付報告寫仲裁記錄（R-NNN、衝突點一句、採用哪邊、理由一句），供主 flow 判「約束該 retire」或「Spec 該改」。
- **DoD**:
  - [ ] 知識前置 retro 子項出現「排除 retired 條目」指令
  - [ ] 出現前提錨點鮮度檢查步驟（錨點不存在→不貼+列 retire 候選回報）
  - [ ] 出現 retro 約束 vs Spec 衝突仲裁規則（Spec 優先、不可靜默選邊、寫仲裁記錄）
  - [ ] repo 與 ~/.claude 兩份 SKILL.md `diff -q` 無差異（憑據：貼 diff 結果）
- **契約**:
  | 情境 | 輸入 | 預期輸出 |
  |---|---|---|
  | 錨點失效條目被選中 | grep 命中 R-00X，但其錨點檔已不存在 | 不貼進 prompt、列 retire 候選回報 |
  | 約束與 Spec 衝突 | Spec 要求做 Y、retro 約束禁 Y | 採 Spec、writer 寫仲裁記錄（不靜默） |
  | retired 條目 | grep 命中已 retired 條目 | 排除、不貼 |
