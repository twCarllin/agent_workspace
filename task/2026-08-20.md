# Task — 觀測性強化四項（failure 留痕、事件日誌、死檔清理、session hooks）  (created: 2026-08-20 14:20)

> run_id: 2026-08-20-obs-hardening（Tier 2）。引用來源（讀本檔＋以下檔即自足，情境以 id、風險以節名指向，不重述）：
> Spec `spec/2026-08-20-obs-hardening.md`、usage `usage/2026-08-20-obs-hardening.md`、impact `impact/2026-08-20-obs-hardening.md`、risk `risk/2026-08-20-obs-hardening.md`。
> 全域約束：Spec「明確不做」五項排除不得出現在任何 item（不刪 gate、不做 status 推導版、不做 benchmark/CI、不動 settings.local.json 白名單、不攔 Write/Edit manifest 覆寫）。
> 測試指令：`python3 -m unittest discover -s tests`。新行為 item 的單元測試綁進該 item（同 diff）；整合測試 item 只做跨 item 驗證＋mutation self-check。
> MANIFEST_RE 單一判定點鐵律（impact §3、§5）：1b/1d/4a 判 manifest 身分一律重用既有 pattern，禁 endswith/startswith 補丁；session_start.py 以 import 重用（勿產生第三份，impact §5）。

## Task 1: 失敗／放棄 run 的留痕（項目 1）
- 依賴: 無
- [x] 1.1 `aborted` 語義入文件＋四消費點「非 in_progress」斷言測試（1a／1c 消費）  ~120行  files: skills/eval-flow/SKILL.md, skills/eval-flow-resume/SKILL.md, tests/test_eval_gates.py, tests/test_stats.py
      DoD: SKILL.md:144 格式節：status 枚舉加 aborted、failed_reason 說明擴及 aborted、明文 aborted／failed「永不清除」、明載 Write/Edit 覆寫 manifest 為已知不攔覆蓋限制（Spec 1a、B-edge2）；eval-flow-resume SKILL.md:12/:56 明文 aborted 不自動 resume（同 failed 待遇）；test_docs_consistency 綠  情境: A, A-edge1, B-edge2, C
      契約: aborted manifest → check_other_runs 不擋新 run；aborted tier-1 → 不被 `_find_unique_tier1_inprogress` 選為當前 run；含 aborted 的 run 集合 → stats status 分佈計入 aborted（無寫死枚舉，加斷言不改碼）；[邊界] failed → 與 aborted 同「非 in_progress」待遇（四消費點皆不誤判）  （風險 業務#2）
- [x] 1.2 防刪除 gate（1b）＋gate 清單重編號＋正反向測試  ~160行  files: .claude/hooks/eval_gates.py, skills/eval-flow/SKILL.md, tests/test_eval_gates.py
      DoD: commit gate 以 `git diff --cached --diff-filter=D`＋重用 MANIFEST_RE(:42) 偵測 manifest 刪除 → block(exit2)，訊息指示「改標 aborted」；SKILL.md:234 gate 區間宣稱句＋:234-243 清單同步重編號（subagent gate 位移）；test_docs_consistency 綠；既有 711 行 baseline 全綠  情境: B, B-edge1
      契約: staged 含 D 狀態 run/*.json → block；staged 含 D 狀態 *.eval.json → 放行；staged 含 D 狀態 *.test_baseline.json → 放行；[邊界] manifest 被修改（非刪除）→ 本 gate 不觸發（走既有判定）  （風險 業務#1）
- [x] 1.3 aborted／failed 留痕窄例外 gate（1d）＋正反向測試  depends: 1.2  ~200行  files: .claude/hooks/eval_gates.py, tests/test_eval_gates.py
      DoD: 於既有 intent gate 加窄例外，`git diff --cached --name-only` 判 staged 集合恰=一個 manifest（MANIFEST_RE）且 status∈{aborted,failed} 且 failed_reason 非空 → 放行並豁免 eval_state.json 存在攔截(:377)；任一不成立落回原判定；711 baseline 全綠  情境: N, N-err1, A-err1
      契約: 恰一 manifest＋status=aborted＋failed_reason 非空 → 放行(豁免 gate1)；混入其他檔 → block(原判定)；status=completed → 走原路(非窄例外)；[邊界] failed_reason 空 → block（即 1a「必填」機械強制點）  （風險 業務#1）
- [x] 1.4 整合測試（跨 gate）＋mutation self-check  ~120行  files: tests/test_eval_gates.py
      DoD: 以 subprocess 跑真實 `--hook`（仿 RunHookWorktreeRootTest:513 樣板）串接情境 B／N／N-err1；驗防刪除 gate 與窄例外 gate 共存不誤動既有判定；sabotage 防刪除偵測條件與窄例外三條件各一處 → 對應測試須 FAIL  情境: B, N, N-err1

## Task 2: run 事件日誌（項目 2）
- 依賴: 無（註：與 Task 1 共用 skills/eval-flow/SKILL.md、tests/test_stats.py 兩檔，循序執行避免衝突）
- [x] 2.1 事件 append 寫入端（2a）＋值截斷＋旁路容錯＋生命週期文件（2b）＋測試  ~200行  files: .claude/hooks/eval_state.py, tests/test_eval_state.py, skills/eval-flow/SKILL.md
      DoD: 10 個寫入子命令成功後 append 一行 `{"ts","cmd","args"}` 到 run/<run_id>.events.jsonl（append 在 save() 成功後、整段 try/except、失敗僅 warning 不改 exit code）；args 鍵全記值截斷（單值 ≤200 字元＋`…[truncated]`）；SKILL.md:94 step6 ②清單補 events.jsonl＋格式節註 Tier1 無此檔覆蓋限制  情境: D, D-err1, D-err2, D-edge1, D-edge2
      契約: 寫入子命令成功 → events.jsonl append 一行；list-files → 不 append；events 目錄不可寫 → 主命令仍成功(僅 warning)；缺 run_id → skip append＋warning；[邊界] arg 值 >200 字元 → 截斷加標記  （風險 技術#1、安全#1）
- [x] 2.2 stats 消費 events 指標（2c）＋測試  depends: 2.1  ~140行  files: .claude/hooks/stats.py, tests/test_stats.py
      DoD: 新增每 run 事件數＋首尾時距（依 ts 欄位取極值，非物理行序）與 set-step 重入次數（同 sub_task 同 step ≥2＝重試信號，依 cmd+step 計數）；無 events 檔的 run 顯示「無記錄」不 crash  情境: E, E-edge1
      契約: run 有 events → 輸出事件數/時距/重入；run 無 events → 「無記錄」；同 step 出現 2 次 → 重入計 1；[邊界] events 行序被打亂 → 時距/重入不變（依 ts＋cmd+step，不寫保序測試）  （usage 正確性假設 1、風險 §5 行序點）
- [x] 2.3 [P] .gitignore 移除 run/gate_hits.log 納入版控（2d）  ~5行  files: .gitignore
      DoD: 移除 .gitignore:5 的 run/gate_hits.log；`git check-ignore run/gate_hits.log` 無輸出（可 git add）；寫入端(eval_gates.py:72)/讀取端(stats.py:122) 不動  情境: F
- [x] 2.4 整合測試（跨 item）＋mutation self-check  depends: 2.1, 2.2  ~120行  files: tests/test_stats.py
      DoD: 跑真實 eval_state 子命令序列產 events.jsonl → stats collect/report 消費 → 事件數/時距/重入正確；含重入序列驗重試信號；sabotage 重入計數與 ts 取極值各一處 → 對應測試須 FAIL  情境: D, E

## Task 3: 死觀測產物清理（項目 3）
- 依賴: 無
- [x] 3.1 [P] 刪除 scout/ 與 subagents_record/（3a）  ~10行  files: scout/, subagents_record/
      DoD: git 刪除兩目錄；grep 確認無存活程式碼/CI 引用（impact §介面G：subagents_record 唯一引用 .claude/agents/task-verifier.md:74 為死引用、task-verifier 已退役）；manifest scout_report_path 相容條文不動  情境: G
- [x] 3.2 [P] 刪除 tests/check_worktree_isolation.sh（3b）  ~5行  files: tests/check_worktree_isolation.sh
      DoD: 刪除前斷言等價覆蓋 `RunHookWorktreeRootTest`(tests/test_eval_gates.py:513) 存在且通過（DoD 硬性前提）；grep 確認無程式碼/CI 呼叫（impact §介面G 已確認 0 命中）  情境: H
- [x] 3.3 [P] init.sh／doctor.py 排除 _deprecated（3c）＋測試  ~120行  files: init.sh, .claude/hooks/doctor.py, tests/test_doctor.py
      DoD: init.sh:86 skills 同步迴圈排除 skills/_deprecated/（維持「只覆蓋不刪除」語義）；doctor.py:43 list_skills 排除 _deprecated；兩端同步避免 repo_only 誤報  情境: I
      契約: repo 含 skills/_deprecated/ → 部署集合不含它；doctor 健檢 → 不計 _deprecated、不報 repo_only；[邊界] 僅一端排除（模擬漏改）→ doctor repo_only 誤報（反向斷言釘住兩端須同步）  （風險 部署#2）
- [x] 3.4 整合測試（跨 item）＋mutation self-check  depends: 3.1, 3.2, 3.3  ~60行  files: tests/test_doctor.py
      DoD: 刪除後 `unittest discover -s tests` 無 collection error（無殘留 import 指向已刪檔）；doctor.py 對含 _deprecated 的 repo 不誤報；sabotage doctor 的 _deprecated 排除 → repo_only 誤報測試須 FAIL  情境: I
- 收尾備註（非 code-writer item，情境 I-edge1，風險 部署#2）：run 收尾時由主 flow／使用者手動一次性清除目標端 `~/.claude/skills/_deprecated/`，`ls` 前後留痕；init.sh 不負責刪除。

## Task 4: SessionStart hook（項目 4）  depends: Task 3（共用 doctor.py、init.sh）
- [x] 4.1 doctor.py 新增 --brief 旗標（4b）＋測試  depends: 3.3  ~100行  files: .claude/hooks/doctor.py, tests/test_doctor.py
      DoD: main() 新增 argparse（原無，須從零加）；--brief 僅輸出異常行、全綠無輸出；預設模式（無旗標）行為不變；既有 check_skills_sync 測試仍綠  情境: K-brief
      契約: --brief 有異常 → 僅印異常行；--brief 全綠 → 無輸出；無 --brief → 行為不變（既有輸出）；[邊界] --brief 有異常時 exit code 維持既有非零約定  （風險 業務#3）
- [x] 4.2 session_start.py 新檔（4a）＋doctor HOOKS 納入＋假 payload 測試  depends: 4.1  ~220行  files: .claude/hooks/session_start.py, .claude/hooks/doctor.py, tests/test_session_start.py
      DoD: 新增 hook script，import 重用 MANIFEST_RE 掃 run/*.json 找殘留 in_progress manifest＋殘留 eval_state.json 輸出提示行、內嵌呼叫 doctor.py --brief 輸出異常行；stdin 解析失敗一律靜默 exit 0；doctor.py:21 HOOKS 加入 session_start.py（同 diff 建檔＋加清單，HOOKS 檢查 repo hooks_dir 存在故綠，採納 impact §5 待決策點）；假 payload 直跑測試  情境: K, K-edge1, K-err1, M
      契約: 有殘留 in_progress manifest → 輸出「有未收尾的 run…依 eval-flow-resume 恢復」提示；無殘留且 doctor 全綠 → 輸出空；payload 解析失敗 → 靜默 exit 0；[邊界] 輸出 >10 行 → 截至 ≤10 行  （風險 技術#2 已緩解：SessionStart payload 欄位已查證定案）
- [x] 4.3 部署接線（4c）：settings.json 註冊 SessionStart＋init.sh 合併擴充＋冪等測試  depends: 4.2  ~160行  files: .claude/settings.json, init.sh, tests/test_init_merge.py
      DoD: .claude/settings.json 加 SessionStart 事件（matcher startup|resume|compact → session_start.py）；init.sh:63 合併 Python 由「只合併 PreToolUse」擴充為同時合併 PreToolUse／SessionStart，維持冪等、不動 env/worktree 等其他鍵；合併後 PreToolUse 仍指向 gate-check.sh（doctor.py:116-120 守門）  情境: L, L-err1
      契約: 合併 → 部署層含 PreToolUse＋SessionStart 兩事件；跑兩次 → 結果相同（冪等）；[邊界] 既有 PreToolUse → 合併後仍指向 gate-check.sh（防 gate 防線靜默消失）、其他鍵不動  （風險 技術#3、部署#1）
- [x] 4.4 整合測試（跨 item）＋VERSION 升版＋全套一致性收尾  depends: 4.1, 4.2, 4.3  ~120行  files: tests/test_session_start.py, VERSION
      DoD: 假 payload 端到端跑 session_start.py（殘留提示＋doctor 異常行合流、全綠空、解析失敗 exit0、≤10 行）；doctor.py 驗 settings.json 兩事件內容；sabotage session_start 殘留偵測與 ≤10 行截斷各一處 → 測試須 FAIL；VERSION 升版；`unittest discover -s tests` 全綠含 test_docs_consistency（無文件漂移）  情境: K, K-edge1, M
      收尾提醒（風險 部署#1）：settings.json 改動須下次 session 載入才生效；收尾回報明告使用者「首次載入請驗證 hook 有跑」。
