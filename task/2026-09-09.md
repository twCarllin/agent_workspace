# 2026-09-09 Tasks

## Run: 2026-09-09-friction-fixes（Tier 1）

依據：`run/2026-09-09-friction-fixes.json` 的 `spec_inline`（外部專案實戰摩擦回報）。

### Task 1：修兩項 hook bug（信封假陽性、baseline 幻影欠帳）

- [x] Item 1：`report_envelope_check.py` 容忍 harness 前插 byline 行
  - 檔案：`.claude/hooks/report_envelope_check.py`、`tests/test_report_envelope_check.py`
  - DoD：
    - `check_envelope` 的首行戳記檢查改為：戳記行出現在**前 3 個非空行**內即合規（容忍 harness 自動前插 1–2 行 byline／日期）；前 3 個非空行均非戳記行 → 照舊記「首行戳記行」缺損退件
    - Self-check 終行檢查、task-verifier 兩節關鍵詞檢查不變
    - `tests/test_report_envelope_check.py` 新增 regression：戳記行前有 1 行 byline → exit 0；戳記行前有 2 行 → exit 0；整份無戳記行 → 仍 exit 2
  - 契約:
    | 情境 | 預期 |
    |---|---|
    | 報告第 1 個非空行即戳記行（現行合規報告） | exit 0（不迴歸） |
    | harness 前插 1 行 byline，戳記行為第 2 個非空行 | exit 0 |
    | harness 前插 2 行，戳記行為第 3 個非空行 | exit 0 |
    | 前 3 個非空行均無戳記行（戳記在第 4 行起或全無） | exit 2，stderr 含「戳記」 |
    | 缺 Self-check 終行 | exit 2（既有行為不變） |
- [x] Item 2：`test_baseline.py` baseline 直譯器錯配警示（不改行為；2026-09-09 HITL 裁決，否決自動綁 .venv）
  - 檔案：`.claude/hooks/test_baseline.py`、`tests/test_test_baseline.py`、`skills/test-strategy/SKILL.md`
  - DoD：
    - 新增 helper `venv_mismatch(cmd)`：cwd 存在 `.venv/bin`（次選 `venv/bin`）且 `cmd` 字串未引用該目錄名 → 回傳該目錄名；否則回傳 None。不改任何執行環境
    - `baseline` 子命令：stable 失敗數 > 0 且 `venv_mismatch(cmd)` 非 None → stderr 加印一行警示「專案有 <dir> 但 test_command 未用它，失敗可能是直譯器錯配的幻影欠帳，檢查 manifest test_command」
    - `tests/test_test_baseline.py` 新增 regression：有 `.venv/bin` 且 cmd 未引用 → 回傳 `.venv`；cmd 含 `.venv` → None；無虛擬環境目錄 → None
    - `skills/test-strategy/SKILL.md` baseline 節補一句：`test_command` 須指定專案直譯器（例 `.venv/bin/python3 -m pytest`），系統直譯器缺套件會產生幻影 stable failures
  - 契約:
    | 情境 | 預期 |
    |---|---|
    | cwd 有 `.venv/bin`，cmd=`pytest -q`，baseline stable > 0 | baseline 照常寫入，stderr 多一行錯配警示 |
    | cwd 有 `.venv/bin`，cmd=`.venv/bin/python3 -m pytest` | 無警示 |
    | cwd 無 `.venv` 也無 `venv` | 無警示，行為與現行完全相同 |
    | stable = 0（即使錯配） | 無警示（無欠帳即無雜訊） |
    | check／mine 子命令 | 不加警示、行為不變（警示只掛 baseline 建立點） |
