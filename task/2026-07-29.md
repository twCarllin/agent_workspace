# Task — 孤兒 skill 納入版控  (created: 2026-07-29)

> run_id: `2026-07-29-orphan-skills-vcs`（Tier 1 精簡路徑）。需求原文見 manifest 的 `spec_inline`。
> 現行全套測試基線：`python3 -m unittest discover -s tests` → **141 tests, OK**。

## 背景（自足，不依賴對話）

`~/.claude/skills/` 有 13 個 skill，repo 的 `skills/` 只有 7 個。以下 6 個**只存在於部署層、未納入版控**：

| skill | 行數 | 現況 |
|---|---|---|
| `eval-scoring` | 51 | 其 agent `eval-scorer` 已移除 → 死檔 |
| `report-format` | 96 | 四種報告模板；其 reviewer 模板缺「完成度節」，較 `.claude/agents/code-reviewer.md` 內嵌版陳舊 |
| `review-checklist` | 38 | 5 大範疇；缺「完成度節」與「維度標記」，較 agent 內嵌版陳舊 |
| `root-cause-table` | 27 | **活的**——`CLAUDE.md` 引用，用於 BUGLOG 根因分類 |
| `task-checklist` | 32 | 全 repo 零引用，用途不明 |
| `task-verify-checklist` | 31 | 內容已嵌於 `.claude/agents/task-verifier.md` |

這些檔案在 repo 外、無版控、無投放路徑（agent 無 `Skill` 工具、frontmatter 未宣告）。**刪掉即永久遺失、無 git 可回溯。**

**本 run 的目的只有一個：讓它們進入版控，使後續汰除成為可逆操作。**

## 明確不做的事（scope 邊界）

- 不刪除任何檔案
- 不修改任何 agent 定義（`.claude/agents/*.md`）
- 不修改 `CLAUDE.md`、`README.md` 或任何既有 skill
- 不改變任何引用關係、不新增 `skills:` frontmatter
- 不新增測試（本變更無行為面可測；驗證靠 diff 與全套無新增失敗）

## Task 1: 複製 6 個孤兒 skill 進 repo

- [x] 1.1 從 `~/.claude/skills/` 複製 6 個 skill 目錄進 `skills/`  ~275 行（純新增檔，內容為既有檔案的複本）  files: `skills/eval-scoring/SKILL.md`, `skills/report-format/SKILL.md`, `skills/review-checklist/SKILL.md`, `skills/root-cause-table/SKILL.md`, `skills/task-checklist/SKILL.md`, `skills/task-verify-checklist/SKILL.md`
      DoD（機械可驗）:
        1. 六個目錄各含一份 `SKILL.md`，且與部署層來源**逐位元相同**：對 6 者各跑 `diff skills/<n>/SKILL.md ~/.claude/skills/<n>/SKILL.md`，全部**空輸出**。
        2. `git status --short` 顯示這 6 個檔為新增（`??` 或 `A`），且**無任何既有檔被修改**（除本 run 的 manifest 與 task 檔外，不得出現其他 `M`）。
        3. 複製後 `ls ~/.claude/skills/` 仍為 13 個目錄——**來源未被移動或刪除**（本 run 是複製，非搬移）。
        4. 全套 `python3 -m unittest discover -s tests` → `Ran 141 tests ... OK`，無新增失敗（新增 skill 不應影響 `test_docs_consistency` 的 `skills/*/SKILL.md` glob 檢查）。
        5. 複製後 repo `skills/` 目錄數 = 13，與 `~/.claude/skills/` 一致。
      備註: 純機械複製，無邏輯判斷、無行為變更，故無行為契約表。DoD 4 是本 item 唯一的實質風險點——`tests/test_docs_consistency.py` 會 glob `skills/*/SKILL.md` 並檢查其中的 skill 互引與 hook 路徑引用；新進來的 6 份文件若含指向不存在對象的引用，該測試會轉紅。若轉紅，**不得為了讓測試過而修改新複製的檔案內容**（那會破壞 DoD 1 的逐位元相同）——改為停下回報，由使用者裁決。
