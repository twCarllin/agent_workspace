# Task — 2026-08-03

> run_id: `2026-08-03-verification-commands`（Tier 1 精簡路徑）
> manifest: `run/2026-08-03-verification-commands.json`
> 需求原文見 manifest 的 `spec_inline`（本檔不重述，避免兩處漂移）

## 背景（自足，不依賴對話上下文）

`local_test_evidence` 是自由文字欄位，承載 agent 的**推理留痕**（mutation self-check 的 sabotage 點與作廢探針理由、2 次停損結論、測試過時的三項依據、testlint 豁免理由、測試同步段的「測試檔↔對照表 row」對映）。這些內容有價值但不可查詢，導致「**每個 run 實際跑了幾條獨立驗證指令**」這個指標目前完全不可見。

本 run 採**加法**：新增 `verification_commands` 純記錄欄位補上該指標，`local_test_evidence` 的型別與用途**完全不動**。新欄位**不被任何 gate 消費**，`eval_gates.py` 零改動——這是本 run 判為 Tier 1 的前提，破壞它即須升級 Tier 2。

欄位形狀：
```json
"verification_commands": [
  {"command": "python3 -m unittest discover -s tests", "exit_code": 0}
]
```
放置位置：Tier 2 記在 `eval_state.json` 各 sub_task；Tier 1 記在 manifest 自身（與四欄憑據並列，因 Tier 1 不建 `eval_state.json`）。

## Task 1：verification_commands 欄位與遙測

- [x] 1.1 `eval_state.py` 新增欄位與 `add-verification` 子命令 ＋ eval-flow skill 同步 ＋ 單元測試  ~100行
      files: `.claude/hooks/eval_state.py`, `skills/eval-flow/SKILL.md`, `tests/test_eval_state.py`
      DoD:
      1. `cmd_add_subtask` 的 sub_task 骨架含 `"verification_commands": []`
      2. 子命令 `add-verification <id> --command "<指令>" --exit-code <int>` 存在；**重複呼叫為 append 累積**，不覆蓋既有內容
      3. `--command`（非空字串）與 `--exit-code`（整數，可為負）皆必填，缺任一由 argparse 擋下（exit 非 0）
      4. 對不存在的 sub_task id 報錯且 exit 非 0（沿用既有 `find_subtask` 行為）
      5. 舊 `eval_state.json`（sub_task 無此鍵）呼叫 `add-verification` 不 KeyError——以 `setdefault` 補上再 append（向後相容）
      6. `skills/eval-flow/SKILL.md` 兩處同步：eval_state.json 格式節的骨架含新欄位；操作規則節的 helper 子命令清單列出 `add-verification`（**受 `tests/test_docs_consistency.py::test_eval_state_subcommands_in_docs_exist` 強制**，未同步該測試會紅）
      7. `tests/test_eval_state.py` 新增案例涵蓋：append 累積（連兩次呼叫後長度為 2 且順序正確）、不存在的 id、舊檔無鍵時的向後相容
      8. `python3 -m unittest discover -s tests` 相對 baseline **無新增穩定失敗**

- [x] 1.2 `stats.py` 彙總指標 ＋ 單元測試 ＋ step 5 記錄敘述  ~70行
      files: `.claude/hooks/stats.py`, `tests/test_stats.py`, `skills/test-strategy/SKILL.md`, `skills/eval-flow/SKILL.md`
      DoD:
      1. `stats.py` 輸出新增一行彙總「驗證指令數」，資料來源兩處併計：`run/*.eval.json` 各 sub_task 的 `verification_commands`（Tier 2）與 `run/*.json` manifest 自身的同名欄位（Tier 1）
      2. **向後相容**：現存 25 個既有檔皆無此欄位，掃描不得拋例外；無記錄的 run 不計入平均的分母，輸出須能區分「0 條」與「無記錄」
      3. `tests/test_stats.py` 新增案例涵蓋：有欄位、無欄位、Tier 1／Tier 2 混合三種
      4. `skills/eval-flow/SKILL.md` 循環 step 5 與 `skills/test-strategy/SKILL.md` step 5 各加一句說明「驗證指令另記 `verification_commands`」——**明確與 `local_test_evidence` 並存、非取代**
      5. `eval_gates.py` 保持零改動（`git diff --stat` 不含該檔即為證據）
      6. `python3 -m unittest discover -s tests` 相對 baseline **無新增穩定失敗**

## 明確不做（scope 邊界）

- 不改 `local_test_evidence` 的型別、語義或任何既有用途
- 不改 `eval_gates.py`（新欄位不加任何 gate；加 gate 即為 Tier 2 變更）
- 不對 `run/` 內 25 個既有檔做 migration（新欄位為可選）
