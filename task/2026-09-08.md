# 2026-09-08 Tasks

## Run: 2026-09-08-prep1-skip-by-reason-code（Tier 1）

依據：`run/2026-09-08-prep1-skip-by-reason-code.json` 的 `spec_inline`。

### Task 1：前置 1 改理由碼推導的條件執行

- [x] Item 1：`skills/eval-flow/SKILL.md` 前置 1 節加「條件執行」規則
  - 檔案：`skills/eval-flow/SKILL.md`
  - DoD：
    - 前置 1 節開頭新增執行條件：`tier_rationale` 理由碼含「信任邊界」或「公開介面／落地資料契約」→ 必跑；不含 → 跳過，manifest 記 `risk_report_path: "skipped: 理由碼無邊界類"`、phase 直設 `"risk_done"`（hook 的 usage-analyzer 放行不受影響）
    - 明文：all-in 時本跳過關閉（照舊全跑）
    - 明文：跳過的判定由已留痕的 `tier_rationale` 理由碼機械推導，agent 不得臨場裁量
    - 循環開頭「升級逃生門」句補：前置 1 被跳過的 run 冒 🔴／歧義時，回頭補跑前置 1（含存檔與回寫 `risk_report_path`）
  - 契約:
    | 情境 | 預期 |
    |---|---|
    | Tier 2、理由碼僅「跨子系統協調」 | 前置 1 跳過，`risk_report_path` 記 `skipped: 理由碼無邊界類`，phase 直設 `risk_done` |
    | Tier 2、理由碼含「信任邊界」 | 前置 1 必跑，流程與現行相同 |
    | Tier 2、all-in | 前置 1 必跑（跳過捷徑關閉） |
    | 跳過後循環中冒 🔴 | 中止循環，補跑前置 1（升級逃生門既有路徑） |
- [x] Item 2：`skills/eval-flow/references/formats.md` 對齊兩處語義
  - 檔案：`skills/eval-flow/references/formats.md`
  - DoD：
    - phase 轉移句（「前置 1 無 🔴 → risk_done」）補跳過分支：理由碼無邊界類 → 前置 0 後直設 `risk_done`
    - `risk_report_path` 欄位語義補：Tier 2 亦可為 `"skipped: <理由>"`（跳過條件見 SKILL.md 前置 1）
- [x] Item 3：`skills/eval-flow-resume/SKILL.md` init 列對齊
  - 檔案：`skills/eval-flow-resume/SKILL.md`
  - DoD：
    - phase=`init` 列補：`risk_report_path` 為 `skipped: ...` 屬合法狀態——依 SKILL.md 前置 1 條件重derive；理由碼無邊界類則設 `risk_done` 續走前置 2，含邊界碼則跳過標記無效、照常跑前置 1

收尾另含：部署同步 `skills/` → `~/.claude/skills/`（doctor 既報不同步，本 run 收尾一併重新部署）。
