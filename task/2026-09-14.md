# Task 2026-09-14

> run_id: 2026-09-14-retro-citation-slimming（Tier 1 精簡路徑）
> spec_inline 見 `run/2026-09-14-retro-citation-slimming.json`。目標：四項框架治理補強——retro 引用鏈與「被引用即保留」判準、衝突成立判準句、skill 瘦身停損 checklist。
> Item 1、2 同源（retro 治理同一連貫變更）、合計 diff 一輪可讀 → **合併為一輪 checker 審**；Item 3（新 skill）獨立一輪。

## Task 1：retro 引用鏈與衝突成立判準

### Item 1：RETRO.md 檔頭 retired 節加引用鏈與「被引用即保留」判準
- **files**: `retro/RETRO.md`、`.claude/agents/retro.md`
- **做什麼**: (a) RETRO.md 檔頭 retired 節新增**引用鏈**規則——retire 候選提報時，主 flow 必須先跑 `grep -rn "R-NNN"`（全 repo，排除 `retro/RETRO.md` 自身與 `run/` 冷溯源檔）產出**引用清單**，隨候選一併回報使用者；(b) 同節新增**引用二分處置**——引用處屬 prose 規則文件（CLAUDE.md／`skills/`／`.claude/agents/` 定義）仍依賴該約束 → **不得列 retire 候選**，須先在同一提案中處理引用處（改寫或一併退役）；引用處僅為下沉機械版（hook／lint／測試）的來源標注 → 該引用是觸發時機②的**證據**、支持 retire；(c) 新增**下沉標注慣例**——條目下沉為機械防線時，機械版須在註解標注來源 `R-NNN`（反向鏈的建立端）；(d) retro agent「機械可防的往下沉」守則句尾加指向：下沉實作時機械版標注來源 R-NNN（規則住 RETRO.md 檔頭，指向式引用不重列）。
- **DoD**:
  - [x] RETRO.md 檔頭出現「retire 候選必附 grep 引用清單」規則，含 grep 範圍（全 repo、排除項）
  - [x] 出現引用二分處置：prose 規則文件引用→不得列候選（先處理引用處）；下沉標注引用→支持 retire 的證據
  - [x] 出現下沉標注慣例（機械版註解標 R-NNN）
  - [x] retro.md「機械可防的往下沉」守則含指向 RETRO.md 檔頭的一句（不重列規則——R-007 單一枚舉點）
  - [x] 既有 retired 節原文（兩觸發時機、必經使用者確認、不刪只標、活躍計數）不被改寫語義
- **契約**:
  | 情境 | 輸入 | 預期輸出 |
  |---|---|---|
  | 提報 retire 候選 | 錨點失效的 R-00X | 回報附全 repo grep 引用清單 |
  | prose 仍引用 | `skills/test-strategy` 條文引用 R-016 原文約束 | 不得列候選，先處理引用處 |
  | 已下沉條目 | hook 註解標 `R-00X` 且無 prose 依賴 | 引用即下沉證據，支持 retire |

### Item 2：eval-flow step 1 衝突仲裁加「衝突成立判準」
- **files**: `skills/eval-flow/SKILL.md`（**同步鏡像** `~/.claude/skills/eval-flow/SKILL.md`，內容須逐字一致）
- **做什麼**: 循環 step 1「retro 約束 vs Spec 衝突仲裁」條，於既有仲裁程序**之前**插入衝突成立判準——**僅題材重疊／同域不算衝突**；衝突成立須同時：①指認兩邊對撞的**條文原句**（R-NNN 的約束句 vs Spec／DoD／契約 row 原文）②寫出**具體後果**（照 A 做會違反 B 的哪一句、產生什麼可觀察的錯誤行為）。寫不出後果 → 不是衝突、不記仲裁記錄、照常兩者都遵守。既有仲裁程序（Spec 優先、不靜默選邊、仲裁記錄格式）不動。
- **DoD**:
  - [x] step 1 仲裁條出現「僅重疊不算衝突」判準與兩條件（對撞條文原句＋具體後果）
  - [x] 明載「寫不出後果→不是衝突、不記仲裁」
  - [x] 既有仲裁程序原文語義不變（Spec 優先、仲裁記錄四要素仍在）
  - [x] repo 與 ~/.claude 兩份 SKILL.md `diff -q` 無差異（憑據：貼 diff 結果）
- **契約**:
  | 情境 | 輸入 | 預期輸出 |
  |---|---|---|
  | 真衝突 | Spec 要求做 Y、R-NNN 約束句禁 Y，後果寫得出 | 記仲裁（既有程序不變） |
  | 僅同域 | R-NNN 講同一模組但條文與 Spec 不對撞 | 不記仲裁、兩者都守 |
  | 寫不出後果 | 疑似衝突但推不出可觀察錯誤 | 不成立、不記仲裁 |

## Task 2：skill 瘦身停損 checklist

### Item 3：新增 skill-slimming skill（含部署副本）
- **files**: `skills/skill-slimming/SKILL.md`（**部署副本** `~/.claude/skills/skill-slimming/SKILL.md`，內容須逐字一致）
- **做什麼**: 新增瘦身作業 skill，內容為 checklist（不是流程）：(a) **範圍界定**——只碰會進 LLM context 的 prose（SKILL.md、`.claude/agents/*.md`、CLAUDE.md、references/*.md）；script、tests、fixtures 不屬瘦身對象；(b) **不可弱化清單**——硬性 gate 語句、憑據要求、HITL／防濫用條款（「必經使用者確認」「agent 不可自行」句式）、標 `（R-NNN）` 的規則（改前先讀對應條目）——壓縮只改表述不得改語義；(c) **結構檢查**——單一枚舉點（R-007：重列收斂為指向引用，指向時不 inline 附推導結果）、投放路徑（R-002：內容搬動後確認執行者載入路徑仍讀得到）；(d) **停損判準**——預期省下的 token 低於逐條核對的成本即停，停時留痕「查了什麼、沒查什麼、為何停」；(e) **驗收**——逐條對照壓縮前後的硬規則語義保持（不整段重寫式壓縮）、兩份實體副本同 diff 同步；(f) frontmatter 觸發語（「瘦身 skill」「壓縮流程文件」「精簡 SKILL.md」）與不適用（新寫 skill、格式化不改字義的任務）。
- **DoD**:
  - [x] `skills/skill-slimming/SKILL.md` 存在，frontmatter 含 name／description（觸發語＋不適用）
  - [x] 內文含上述 (a)–(e) 五節，每節為可核對的 checklist 句（禁不可驗評價詞）
  - [x] 不可弱化清單明列「標 R-NNN 規則改前先讀對應條目」
  - [x] 停損節含留痕三要素（查了什麼／沒查什麼／為何停）
  - [x] repo 與 ~/.claude 兩份 `diff -q` 無差異（憑據：貼 diff 結果）
- **契約**:
  | 情境 | 輸入 | 預期輸出 |
  |---|---|---|
  | 瘦身任務進場 | 使用者說「精簡某 SKILL.md」 | 本 skill 被觸發、依 checklist 執行 |
  | 壓到硬規則 | 欲改寫帶「必經使用者確認」的句子 | 只改表述不改語義；語義要改須使用者裁決 |
  | 收益過低 | 剩餘段落省 token < 核對成本 | 停手並留痕三要素 |
