# Task 2026-09-21

> run_id: 2026-09-21-flow-rule-tuning（Tier 1 精簡路徑）
> spec_inline 見 `run/2026-09-21-flow-rule-tuning.json`。目標：四項流程規則——邊界直派 reviewer、信封缺損二分＋重取上限、verify 事件補指令原文＋全套次數統計、直寫捷徑改判斷加留痕。
> 四個 item 同一連貫變更（同 spec_inline）。審查分兩輪 checker：Item 1（prose）一輪、Item 2＋3＋4（script＋tests）合併一輪（合計 staged 預估 <400 行）。四個 item 皆主 flow 直寫（各 ≤100 行；本 run 同時 dogfood Item 1 的 executor_notes 留痕）。

## Task 1：流程規則（prose）

### Item 1：eval-flow SKILL.md／formats.md／gates.md／code-reviewer.md 四項規則落文
- **files**: `skills/eval-flow/SKILL.md`、`skills/eval-flow/references/formats.md`、`skills/eval-flow/references/gates.md`、`.claude/agents/code-reviewer.md`、鏡像 `~/.claude/skills/eval-flow/`（同 diff 同步）
- **做什麼**:
  - SKILL.md 循環 step 3 加「邊界直派」：manifest `tier_rationale` 理由碼含**信任邊界**或**公開介面／落地資料契約** → 該 run 全部 sub_task 跳過 checker，step 3 直接派 `code-reviewer` 全 diff 審（輸入照升級輪、step 2 用完整 file-scoped diff）；set-review `--checked-by reviewer:boundary`；審查落檔尾註 `checked_by: reviewer(boundary)`；不計入升級率；step 7 retro 條件視同升級輪；all-in 亦同。判定只依已留痕理由碼機械推導，agent 不臨場裁量（同前置 1 執行條件的慣例）。
  - SKILL.md 憑據紀律「信封缺損」句改為二分：缺 `Self-check:` 終行（或 task-verifier 缺兩節）＝疑似截斷，退件重取**最多 1 次**；第 2 次仍缺 → 主 flow 人工檢視報告內容、在回報留痕一句、不再重取。只缺戳記行 → hook 以 additionalContext 回傳警告、不退件，主 flow 在回報留痕一句。細節單一枚舉點在 gates.md 信封節，SKILL.md 指向。
  - gates.md 信封 lint 節改寫為二分（blocking 項 vs advisory 項、各自的 exit 與載荷）。
  - SKILL.md Tier 1 第 4 點「直寫捷徑」：刪 `≤100 行` 硬門檻，改為主 flow 判斷「交接是否划算」（考量：item 是否需要獨立 context、主 flow 目前 context 負載、item 邏輯是否簡單機械）；每 item 在 manifest 選填欄 `executor_notes` 記一句 `item <id>: 直寫｜派工 — <理由>`；寫審分離、知識前置自查留痕不變；all-in 關閉不變。
  - formats.md：manifest 加選填欄 `executor_notes`（list[str]，語義如上）；events 節補 `verify_cmd`／`add-verification` 事件的 `args.verify_command` 鍵語義（Item 3 產出的消費端說明）。
  - code-reviewer.md description 補「或理由碼含邊界類的 run 直派」一句。
- **DoD**:
  - [x] SKILL.md step 3 含「邊界直派」規則段，含 `reviewer:boundary` 字樣與「不臨場裁量」句
  - [x] SKILL.md 憑據紀律信封句含「最多 1 次」與「只缺戳記行」兩個分支，並指向 gates.md
  - [x] gates.md 信封節列出 blocking／advisory 兩類項目與對應 exit 碼
  - [x] SKILL.md Tier 1 第 4 點不再含 `≤100 行` 字樣，含 `executor_notes` 字樣
  - [x] formats.md 含 `executor_notes` 欄位說明與 `verify_command` 鍵說明
  - [x] code-reviewer.md description 含「邊界」字樣
  - [x] `diff -rq skills ~/.claude/skills` 除 .DS_Store 外無差異
  - [x] `python3 -m pytest tests/test_docs_consistency.py -q` 綠
- **契約**:
  | 情境 | 輸入 | 預期輸出 |
  |---|---|---|
  | 文件一致 | grep `reviewer:boundary` 全 repo | SKILL.md、eval_state.py、formats.md 或 stats.py 皆命中（單一枚舉點：eval_state.py VALID_CHECKED_BY；SKILL.md 指向） |
  | 舊字樣移除 | grep `≤100 行` skills/ | 零命中 |
  | 鏡像同步 | diff -rq skills ~/.claude/skills | 只剩 .DS_Store 差異 |

## Task 2：hook／script

### Item 2：report_envelope_check.py 信封缺損二分
- **files**: `.claude/hooks/report_envelope_check.py`、`tests/test_report_envelope_check.py`
- **做什麼**: `check_envelope` 改回傳 `(blocking, advisory)` 兩清單：`Self-check 終行`、`兩節關鍵詞（完成度／憑據）` 為 blocking；`首行戳記行` 為 advisory。main：blocking 非空 → exit 2、stderr 退件訊息（措辭沿用，加註「advisory 項一併列出」）；只有 advisory → stdout 印 PostToolUse JSON `{"hookSpecificOutput": {"hookEventName": "PostToolUse", "additionalContext": "[report-envelope] 警告（<agent>）：缺首行戳記行，不退件；主 flow 於回報留痕一句"}}`、exit 0；皆空 → exit 0 無輸出。fail-open 行為不變。
- **DoD**:
  - [x] 只缺戳記行 → exit 0、stdout 為合法 JSON 且 `additionalContext` 含「戳記」
  - [x] 缺 Self-check 終行 → exit 2、stderr 含「Self-check」
  - [x] task-verifier 缺兩節 → exit 2
  - [x] 同時缺戳記與 Self-check → exit 2、stderr 同時含「戳記」與「Self-check」
  - [x] 合規 → exit 0、stdout 與 stderr 皆空
  - [x] 既有 row 7–13 行為不變（fail-open、背景、非名單）
  - [x] tests/test_report_envelope_check.py 對應更新：row 2、row 14 改驗 exit 0＋additionalContext；新增「同時缺兩項」row
- **契約**:
  | 情境 | 輸入 | 預期輸出 |
  |---|---|---|
  | advisory only | 首行非戳記、其餘合規 | exit 0；stdout JSON.additionalContext 含「戳記」；stderr 空 |
  | blocking | 無 Self-check 行 | exit 2；stderr 含「Self-check」 |
  | blocking＋advisory | 首行非戳記且無 Self-check | exit 2；stderr 含「戳記」與「Self-check」 |
  | 邊界：戳記在第 4 非空行 | 前插 3 行 | exit 0（advisory）、additionalContext 含「戳記」 |
  | 合規 | golden 報告 | exit 0；stdout 空；stderr 空 |
  | 不驗 | subagent 不在名單 | exit 0；stdout 空 |

### Item 3：verify 事件補記指令原文＋stats 全套次數
- **files**: `.claude/hooks/eval_state.py`、`.claude/hooks/run_verify.py`、`.claude/hooks/stats.py`、`tests/test_eval_state.py`、`tests/test_run_verify.py`、`tests/test_stats.py`
- **做什麼**:
  - 根因：`append_event` 過濾 `command` 鍵（子命令 dest），而 `add-verification` 的 `--command` 與 run_verify 的 `Namespace(command=...)` 都用同名鍵 → 事件只剩 exit_code。修法：`cmd_add_verification` 與 run_verify 兩路徑改傳 `argparse.Namespace(..., verify_command=<指令>, exit_code=...)` 給 append_event；不改 append_event 過濾規則、不改 `--command` 參數名（CLI 相容）。
  - stats.py `_events_summary` 加 `full_suite`：計 cmd ∈ {`verify_cmd`, `add-verification`} 且 `args.verify_command` 含 `--strike-key full_suite` 的事件數；report 事件節每 run 尾綴 `／全套 N`。舊事件無 `verify_command` 鍵 → 計 0（不 crash）。
- **DoD**:
  - [x] `eval_state.py add-verification 1 --command "X" --exit-code 0` 後 events 尾行 `args.verify_command == "X"`（tests/test_eval_state.py 新測試）
  - [x] `run_verify.py --run-id r1 --cmd "X"`（Tier 1）事件 `args.verify_command == "X"`；Tier 2 路徑（--sub-task）事件亦含（tests/test_run_verify.py 兩條）
  - [x] stats：events 含 2 條 `verify_command` 帶 `--strike-key full_suite`、1 條不帶 → summary `full_suite == 2`、report 含「全套 2」（tests/test_stats.py）
  - [x] stats：舊事件無 verify_command 鍵 → `full_suite == 0`、不 crash
  - [x] 全套測試綠
- **契約**:
  | 情境 | 輸入 | 預期輸出 |
  |---|---|---|
  | add-verification 事件 | --command "pytest -q" | events 尾行 args 含 verify_command="pytest -q"、exit_code |
  | run_verify Tier 1 | --cmd "python3 -c pass" | verify_cmd 事件 args.verify_command == 指令 |
  | run_verify Tier 2 | --sub-task 1 --cmd ... | add-verification 事件 args.verify_command == 指令 |
  | stats 全套計數 | 2 條含 `--strike-key full_suite`、1 條不含 | full_suite=2；report 含「全套 2」 |
  | 邊界：舊事件 | args 無 verify_command | full_suite=0、report 含「全套 0」 |

### Item 4：checked_by 新值 reviewer:boundary
- **files**: `.claude/hooks/eval_state.py`、`.claude/hooks/stats.py`、`tests/test_eval_state.py`、`tests/test_stats.py`
- **做什麼**: `VALID_CHECKED_BY` 加 `reviewer:boundary`（註解與 `--checked-by` help 同步）。stats.py：`checked_by == "reviewer:boundary"` 歸新桶 `checked_by_boundary`（不入升級率分母、不入 checked_by_dist），report 升級率行尾綴 `　邊界直派：N 個 sub_task`（N=0 時不印）。
- **DoD**:
  - [x] `set-review 1 0 --checked-by reviewer:boundary` 寫入成功（test_eval_state）
  - [x] stats：sub_tasks [checker, reviewer:boundary, reviewer:①] → direct 1、escalated 1、boundary 1；report 含「checker 升級率：50%（1/2）」與「邊界直派：1」（test_stats）
  - [x] 無 boundary 時 report 不含「邊界直派」字樣（既有測試不動即驗）
- **契約**:
  | 情境 | 輸入 | 預期輸出 |
  |---|---|---|
  | 合法值 | --checked-by reviewer:boundary | 檔案寫入、exit 0 |
  | stats 分桶 | 三種值各 1 | direct 1／escalated 1／boundary 1；升級率 50%（1/2） |
  | 邊界：零 boundary | 只有 checker | report 無「邊界直派」 |
