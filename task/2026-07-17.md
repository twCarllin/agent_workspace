# Task 2026-07-17

## Task 1：flow 延遲優化——並行審查、Tier 1 直寫捷徑、baseline 重用（run: 2026-07-17-flow-latency-opt）

- 建立時間：2026-07-17 00:49
- Tier：1（spec_inline 見 `run/2026-07-17-flow-latency-opt.json`）
- 背景：2026-07-16 三個真實 run 的耗時實測——每個 subagent 呼叫 60–180 秒且大半是重建 context 的固定稅，一個 item 的序列鏈（writer → reviewer → verifier）≈ 6–8 分鐘；baseline 把全套測試跑兩次，對測試慢的專案是固定痛點。優化原則：**只砍重複與序列化開銷，不削任何防線**——reviewer／verifier 皆唯讀互不依賴（可並行）；「寫的人 ≠ 審的人」才是獨立性防線，主 flow 直寫不損之（reviewer 照常獨立審）；HEAD 未變時 baseline 結果是確定性的（可重用）。
- 驗證方式：全套測試 `python3 -m unittest discover -s tests` 綠。

### Items（序列執行——item 1 為 prose、item 2 為 code＋prose，檔案不相交但序列成本低）

- [x] **Item 1：review ∥ verify 並行化＋Tier 1 主 flow 直寫捷徑（prose）**（預估 ~30 行 diff）
  - `skills/eval-flow/SKILL.md` 循環步驟 3／4 改寫：
    - step 3 開頭改為「**同一訊息並發呼叫** `code-reviewer` 與 `task-verifier`（兩者皆唯讀、讀同一份 staged diff、互不依賴輸出——序列跑是純浪費）」；step 4 改為處理兩者結果的匯合點：reviewer 零 🔴 **且** verifier 通過 → 進 step 5；reviewer 有 🔴 → 該輪 verifier 結果作廢，走既有 fixing 迴圈（審查報告落檔、重裁條款、set-review 均不變），修正後**兩者一併重跑**；verifier 不通過（reviewer 零 🔴）→ 修正後回 step 3 重跑兩者
    - 並行的取捨註記一句：🔴 時 verifier 白跑一次，🔴 率低時期望值為正
    - `step` 欄位語義：並行階段記 `reviewing`（`verifying` 值保留供舊 run resume 相容，新路徑不再單獨使用）
  - 同檔「Tier 1 精簡路徑」加一條**主 flow 直寫捷徑**：Tier 1 且單 item 預估 ≤100 行 → 主 flow 可直接寫 code、不 spawn `code-writer`（省一次全新 agent 重建 context 的稅）；條件與守則——「寫的人 ≠ 審的人」防線不變（code-reviewer 照常獨立審 staged diff）、retro 前置改由主 flow 自查相關條目並在回報留痕、超過 ≤100 行或跨多檔複雜 item 仍派 code-writer；hook 對 code-writer 的 phase gate 不受影響（直寫路徑不經該 gate，phase 仍須 decomposed 才動工——由輕量 HITL 保證）
  - `skills/eval-flow-resume/SKILL.md` Step 3 表格同步：`reviewing` 列改為「並行審查／驗證中被斷 → 重跑步驟 3（並發呼叫兩者）」；`verifying` 列註明「（舊版序列 run 的現場）重跑步驟 4」
  - `CLAUDE.md`「呼叫 subagent 完成任務」條加括號註記「（例外：Tier 1 小 item 的主 flow 直寫捷徑，見 eval-flow skill）」
  - DoD：`python3 -m unittest tests.test_docs_consistency` 綠
- [x] **Item 2：baseline 重用（code＋tests＋prose 同步）**（預估 ~90 行 diff）
  - `.claude/hooks/test_baseline.py`：
    - `baseline` 建立時寫入 `head_sha`（`git rev-parse HEAD`）與所用 `cmd` 到 baseline 檔（若現行格式已存 cmd 則沿用）
    - `baseline` 執行時先掃 `run/*.test_baseline.json`：存在「`head_sha` == 目前 HEAD 且 cmd 相同」的既有檔 → **沿用**（複製 stable_failures／flaky 名單為本 run 的 baseline 檔，附 `reused_from: <run_id>` 欄，log 標明「沿用 <run_id> 的 baseline（HEAD 未變），免跑兩次全套」），不重跑測試
    - 新增 `--fresh` 旗標：強制重建、跳過重用（測試環境變過但 HEAD 沒變時的手動出口）
    - HEAD 取不到（非 git repo）→ 照舊全新建立，不重用
  - `tests/test_test_baseline.py` 補案例：同 HEAD＋同 cmd → 重用（不執行測試指令、reused_from 正確）；HEAD 不同 → 不重用；cmd 不同 → 不重用；`--fresh` → 不重用
  - `skills/test-strategy/SKILL.md`「Baseline」節同步一句：HEAD 未變且 cmd 相同時自動沿用前一 run 的 baseline（`--fresh` 強制重建）
  - DoD：`python3 -m unittest tests.test_test_baseline` 綠；全套 `python3 -m unittest discover -s tests` 綠

## Task 2：第二輪文件修剪——尾節／CLAUDE.md／規則去重／report-format 拆解（run: 2026-07-17-docs-prune-2）

- 建立時間：2026-07-17 09:16
- Tier：1（spec_inline 見 `run/2026-07-17-docs-prune-2.json`）
- 背景：精簡檢視（CLAUDE.md＋8 skills 共 1,106 行）結論，使用者四組全選。原則：**只刪重複與死條文，不動範例與實測 rationale**。判準與上次 prune run 相同——單一消費者的間接層砍掉、single source 以外的重述砍掉。
- 驗證方式：全套 `python3 -m unittest discover -s tests` 綠（含 test_docs_consistency 引用一致性）。

### Items（序列執行；檔案不相交，但 item 2 涉及跨檔搬移、序列較穩）

- [x] **Item 1：A＋C＋D＋E 純刪減**（預估 ~60 行 diff，全為刪除與小改寫；主 flow 直寫捷徑執行）
  - A：刪五檔尾部「## 適用範圍」整節（`skills/eval-flow/SKILL.md`、`skills/eval-flow-resume/SKILL.md`、`skills/test-strategy/SKILL.md`、`skills/task-decomposition/SKILL.md`、`skills/usage-scenario-analysis/SKILL.md`）——內容與各自 frontmatter description 的「不適用於」重複；frontmatter 一字不動
  - C：`CLAUDE.md`——刪「舊的 task.md…」行（死條文，repo 無此檔）；刪「task 所有子任務完成後，必須呼叫 task-verifier…」條（hook verify_passed 已強制＋eval-flow step 3/4 已明文）；刪「拆分品質由…把關」兩行（task-decomposition／eval-flow 已有）；刪「標記任務創建時間」「可以平行化的任務…[P]」「task 完成後…[x]」三行（task-decomposition 輸出格式已規範）；「Subagent Principle」整節刪除、其內容以一行併入 Task Principle：「sub_task 收尾後，若任務來自 task 檔，須將對應 item 標記完成」；「部署準備」節兩行併入「部署規則」節、刪原節標題
  - D：`skills/eval-flow/SKILL.md` 操作規則的「quality_score < 10…」條（含三個子彈）壓縮為一句「每輪 round 的扣分細則（points_lost 加總 == 10 − quality_score、每筆四欄位）由 helper／hook 驗證，定義住 eval-scorer agent」
  - E：`skills/task-risk-analysis/SKILL.md`「輸出原則」節刪與 eval-flow 前置 1 重複的流程後果（🔴 不可進入執行、🟡 記錄帶入、🟢 提醒的**處置**部分），保留「有風險標等級＋對策」與「無風險也要明確標註」兩條分析側原則，加一句「等級的流程後果住 eval-flow 前置 1」
  - DoD：`grep -n "## 適用範圍" skills/ -r` 無殘留；`python3 -m unittest tests.test_docs_consistency` 綠
- [x] **Item 2：B report-format 四模板內聯拆解**（預估 ~200 行 diff，多為搬移；派 code-writer）
  - 搬移對應（模板 code block 原樣搬進 agent 定義的「輸出格式」小節；agent 定義原「依 report-format skill 的 X 模板」指涉改為檔內小節；frontmatter `skills:` 欄若列 report-format 則移除）：
    - A. 程式碼審查報告 → `.claude/agents/code-reviewer.md`
    - B. 工作報告 → `.claude/agents/code-writer.md`
    - C. Retro 記錄 → `.claude/agents/retro.md`（「消費方式」說明句與 eval-flow step 1 重複者不搬，僅搬條目格式與寫作檢驗）
    - D. 任務完成度驗證報告 → `.claude/agents/task-verifier.md`
  - 刪除 `skills/report-format/` 整個目錄
  - `grep -rn "report-format" .claude/ skills/ CLAUDE.md README.md tests/` 全面清點引用並同步（以 grep 結果為準；TODO.md 歷史紀錄不改）
  - DoD：`skills/report-format/` 不存在；grep 僅剩 TODO.md／歷史 task 檔／subagents_record；全套 `python3 -m unittest discover -s tests` 綠

## Task 3：大專案適應——影響面盤點＋知識分層（run: 2026-07-17-impact-knowledge-layering）

- 建立時間：2026-07-17 11:19
- Tier：1（spec_inline 見 `run/2026-07-17-impact-knowledge-layering.json`，含完整設計決策）
- 背景：萬行以上專案的主要失敗模式從「code 寫錯」變成「不懂系統就動手」（重複造輪、違反模組慣例、漏改呼叫端）——diff-only review 天生看不到。落地平衡建議 1（影響面盤點前置）與 5（知識分層）。
- 驗證方式：全套 `python3 -m unittest discover -s tests` 綠。

### Items（序列執行——item 1 與 3 均動 skills/eval-flow/SKILL.md，不可 [P]）

- [x] **Item 1：impact-analyzer agent＋前置 2.5 接線**（預估 ~160 行 diff，多為新 agent 定義；派 code-writer）
  - 新建 `.claude/agents/impact-analyzer.md`：唯讀（Read/Grep/Glob/Bash）、frontmatter model 比照 usage-analyzer；指令內聯（無獨立 skill）——讀 manifest 的 spec_path／usage_report_path 與既有程式碼，產出 `impact/<run_id>.md` 五節：觸及模組清單、各模組既有慣例（命名／錯誤處理／測試慣例，含出處檔:行）、可重用既有元件（防重複造輪）、被改介面的呼叫端清單（grep 完整、含測試）、跨模組風險點；報告自足（不指涉對話）；產出後回寫 manifest.impact_report_path
  - `skills/eval-flow/SKILL.md` 新增「前置 2.5：影響面盤點」節（前置 2 與 3 之間）：預設執行；skip 條件＝全新模組／無既有呼叫端（manifest 記 `impact_report_path: "skipped: <理由>"`）；不新增 phase 值；前置 3 的 task-decomposer 呼叫 prompt 須含 impact report 路徑（拆分依模組邊界切）。manifest 格式範例加 `impact_report_path` 欄＋說明行；Tier 1 精簡路徑註明固定 skipped
  - `.claude/agents/task-decomposer.md`：輸入區加 impact report（讀模組邊界與呼叫端，對映到 item 的 files／DoD）
  - `skills/eval-flow-resume/SKILL.md` Step 2 表 `usage_confirmed` 列：補「檢查 impact_report_path：null 且非 Tier 1 → 先跑前置 2.5」
  - `CLAUDE.md` Task Principle 資料夾慣例加一行：影響面報告放 `impact/<run_id>.md`
  - DoD：`python3 -m unittest tests.test_docs_consistency` 綠
- [x] **Item 2：hook gate＋測試**（預估 ~20 行 diff；主 flow 直寫捷徑）
  - `.claude/hooks/eval_gates.py` AGENT_MIN_PHASE 加 `"impact-analyzer": "usage_confirmed"`（註解比照既有）
  - `tests/test_eval_gates.py` 補案例：phase < usage_confirmed 呼叫 impact-analyzer → block；達 usage_confirmed → 放行
  - DoD：`python3 -m unittest tests.test_eval_gates` 綠
- [x] **Item 3：知識前置擴充＋retro 標籤規範**（預估 ~25 行 diff；主 flow 直寫捷徑）
  - `skills/eval-flow/SKILL.md` step 1 的「retro 前置」擴為「知識前置（硬性步驟）」：貼進 writer prompt 硬性約束區的內容從 retro 條目擴為三源——①retro 相關條目（改為先以本 item files 的模組路徑 grep 標籤篩選、主 flow 再補判）②該 item 觸及模組的 conventions（模組子目錄 CLAUDE.md 存在則摘錄）③impact report 的對應模組慣例段（前置 2.5 有跑時）；無相關內容照舊留痕「無相關條目」
  - `.claude/agents/retro.md` 標籤規範：條目標籤第一段固定為模組路徑或域名（如 `hooks／eval_gates` 或 `流程設計`），供機械 grep；既有條目格式已符合，不回改
  - DoD：`python3 -m unittest tests.test_docs_consistency` 綠；全套綠

## Task 4：移除 eval-scorer 獨立階段與 threshold 閘門（run: 2026-07-17-remove-eval-scorer）

- 建立時間：2026-07-17 16:40
- Tier：1（spec_inline 見 `run/2026-07-17-remove-eval-scorer.json`）
- 背景：前次修剪（2026-07-16-prune-redundant-layers）將 eval-scorer 留待 5-run 審計；10-run 遙測結論到位——scorer 獨立貢獻 0%（0/7）、quality_score 平均 9.7（22 rounds）貼天花板無鑑別力、threshold 47 次未 bind、scorer 反被 gate 擋 4 次（純摩擦）。設計原則（使用者確認）：**每閘門一個正交問題、輸出決策不輸出分數；分數只留給遙測**。scorer 沒有自己的正交失效模式（品質問題 reviewer 管、完成度 verifier 管、退化測試管）→ 移除獨立階段。
- 設計決策：
  - 循環改為 7 步：writer → (review ∥ verify) → 修至零 🔴 → 本地測試 → 收尾（原 step 6 評分、step 7 threshold 判斷刪除）
  - **修正迭代上限**：同一 sub_task 修正 2 輪後 reviewer 仍有 🔴 → `status: "failed"`、回報使用者（承接原「rounds == 2 回報」的迭代上限語義）
  - **rework 遙測訊號**：`review_reds >= 1`（首輪就有 🔴 ＝ 需要第二輪）取代原 `len(rounds) >= 2`
  - **維度訊號降格為遙測**：code-reviewer 每條問題（🔴／🟡）標五維詞彙之一（Clarity／Completeness／Testability／Non-functional／Technical_constraints）；主 flow 於 set-review 時以 `--dimensions` 一併記入 sub_task 的 `review_dimensions`（維度→問題數）；stats.py 據此輸出維度分佈（回答「writer prompt 該往哪補」）
  - **向後相容**：run/ 既有歸檔檔含 rounds／quality_score，stats.py 照讀（legacy 路徑）；hook 對舊格式寬容（rounds 存在不驗品質不變量），部署目標專案有 in_progress run 時新 hook 不可打斷其收尾
  - retro 觸發條件不變（reviewer 有 🔴 才跑）；「scorer 獨立貢獻」指標問題已回答，自 stats.py 移除
- 驗證方式：全套 `python3 -m unittest discover -s tests` 綠。

### Items（序列執行——item 1／3 均動 skills/eval-flow/SKILL.md 的關聯敘述與 hooks，檔案雖不相交但語義耦合高，序列較穩）

- [x] **Item 1：流程文件面（prose）**（預估 ~120 行 diff；跨多檔，派 code-writer）
  - `skills/eval-flow/SKILL.md`：刪循環 step 6（評分）與 step 7（threshold 判斷），收尾順序（全套測試 → 歸檔 → commit）併入新 step 6；step 4 匯合點加修正迭代上限（2 輪仍有 🔴 → failed、回報使用者）；原 step 8 retro 條件改引用 reviewer 🔴（刪 score < threshold 分支）；`eval_state.json` 格式範例刪 `threshold`／`rounds`，加 `review_dimensions`；操作規則刪「每輪評分後」「扣分細則」「score < threshold」「評分跳過路徑」四條，set-review 條補 `--dimensions`；「Gate 的硬性執行」刪 gate 5 的評分不變量與 gate 6 的 eval-scorer 條；「Subagent 呼叫原則」的 eval-scorer 引用刪除；Tier 1 精簡路徑的「score → commit」字樣同步
  - `CLAUDE.md`：「循環 1–8（code-writer → review → verify → 本地測試 → score → commit）」改為「循環 1–7（code-writer → review ∥ verify → 本地測試 → commit）」
  - `skills/eval-flow-resume/SKILL.md`：`scoring` step 列改為舊 run 相容註記；`skills/test-strategy/SKILL.md`、`skills/task-decomposition/SKILL.md` 各 1 處 scorer 引用同步
  - DoD：`python3 -m unittest tests.test_docs_consistency` 綠
- [x] **Item 2：agent 定義面**（預估 ~40 行 diff；主 flow 直寫捷徑）
  - 刪除 `.claude/agents/eval-scorer.md`
  - `.claude/agents/code-reviewer.md`：輸出格式加「維度標記」——每條 🔴／🟡 問題標五維詞彙之一（詞彙表內聯，含一行定義），報告末尾附 `維度統計：{維度: 問題數}` 供主 flow 轉錄
  - `.claude/agents/retro.md`（2 處）、`.claude/agents/code-writer.md`（1 處）scorer 引用同步
  - DoD：`grep -rn "eval-scorer" .claude/ skills/ CLAUDE.md` 僅剩歷史檔（task／subagents_record／TODO）
- [x] **Item 3：hooks（code）**（預估 ~120 行 diff；派 code-writer）
  - `.claude/hooks/eval_gates.py`：AGENT_MIN_PHASE 刪 eval-scorer；刪 eval-scorer 呼叫 gate 區塊；`check_rounds_invariant` 改為寬容（rounds 存在時不驗 quality／dimensions／Floor——舊格式歸檔照過）；commit gate 的「rounds 空 ↔ review_reds == 0」耦合檢查刪除（review_reds 為 int 且 >= 0、verify_passed、測試兩欄的檢查不變）
  - `.claude/hooks/eval_state.py`：刪 `append-round` 子命令與 `--threshold` 參數（init 只寫 run_id ＋空 sub_tasks）；`set-review` 加選用 `--dimensions '<json>'`（維度→問題數，寫入 `review_dimensions`，驗證鍵 ∈ 五維詞彙、值為非負 int）；archive 驗證同步（不再驗 rounds）
  - `.claude/hooks/stats.py`：刪「scorer 獨立貢獻」指標；rework 改「`review_reds >= 1` 的 sub_task 比例」（legacy 歸檔無頂層 review_reds 時 fallback `len(rounds) >= 2`）；維度分佈改讀 `review_dimensions`（legacy `deduction_reasons` 照算、標註 legacy）；docstring 同步
  - DoD：`python3 -m unittest tests.test_eval_gates tests.test_eval_state tests.test_stats` 可跑（紅燈由 item 4 收）
- [x] **Item 4：tests 同步＋VERSION**（預估 ~150 行 diff；派 code-writer）
  - `tests/test_eval_gates.py`：刪 scorer gate 與評分不變量案例；補「舊格式歸檔（含 rounds）照過」「review_reds 缺失仍擋」案例
  - `tests/test_eval_state.py`：刪 append-round 案例；補 `set-review --dimensions` 案例（合法寫入、非法維度鍵擋）
  - `tests/test_stats.py`：rework 新舊格式各一案例、`review_dimensions` 統計案例
  - `.claude/hooks/VERSION` bump（建議 `2026.07.17.2`——同日第二次規則變更，manifest 的 framework_version 需可區分）
  - DoD：全套 `python3 -m unittest discover -s tests` 綠
- [x] **Item 5：部署待辦註記（不在本 run 執行）**：`init.sh` 部署到外層專案前，須確認該專案無 in_progress run（遙測顯示有 1 個）；`~/.claude/skills/eval-scoring/`（前次修剪的殘留）與外層 `.claude/agents/eval-scorer.md` 為 init.sh 不會刪除的孤兒，部署時手動清——記入 TODO.md，不動 init.sh
