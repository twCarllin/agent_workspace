# Task — 2026-08-07

> run_id: `2026-08-07-bug-diagnosis-skill`（Tier 1 精簡路徑）
> manifest: `run/2026-08-07-bug-diagnosis-skill.json`
> 需求原文見 manifest 的 `spec_inline`（本檔不重述，避免兩處漂移）

## 背景（自足，不依賴對話上下文）

CLAUDE.md「工作型態前判」規定 bugfix 走「診斷先行：重現 → 定位根因 → 三行診斷結論」，但**「診斷」本身怎麼做沒有紀律**——沒有完成判準、沒有防錨定機制、沒有插樁清理規則。實測的常見失敗模式：agent 讀 code 建第一個假設就開修，修錯方向來回多輪。

本 run 新增 `skills/bug-diagnosis/SKILL.md`，內容改編自 mattpocock/skills 的 diagnosing-bugs（原文備份在 session scratchpad `mp/diagnosing-bugs/SKILL.md`），核心紀律：**先建 feedback loop 才准提假設**。並與本框架既有機制接線：三行診斷結論格式沿用 CLAUDE.md、預防一問接 BUGLOG 兩層制（引用、不重述規則）。

分工原則：CLAUDE.md 管**流程時序**（診斷先行、判級在後、BUGLOG 兩層制），本 skill 管**診斷怎麼做**（loop 判準、假設紀律、插樁紀律）。兩邊不重述對方內容，各留一行指向。

## Task 1：bug-diagnosis skill 與接線

- [x] 1.1 新建 `skills/bug-diagnosis/SKILL.md`（診斷紀律）  ~120行
      files: `skills/bug-diagnosis/SKILL.md`
      DoD:
      1. frontmatter 依本專案慣例：`name: bug-diagnosis`、`description` 含觸發語（「診斷這個 bug」、需求是修 bug 且進入診斷先行時）與不適用情境（根因已明的微調、非 bug 類需求）
      2. 含「Phase 1 feedback loop」節，**完成判準為硬性 checklist**：能指名**一條命令**（測試呼叫／curl／script 路徑），已實跑至少一次（附輸出），且滿足四條——會紅（驅動實際 bug 路徑、斷言使用者描述的症狀，非「沒炸就算過」）／確定性（每次跑同判定；非確定性 bug 改為「已拉高且釘住重現率」）／快（秒級）／agent 可自跑。**此命令不存在前，禁止進入假設階段**，條文明載
      3. 含「重現與最小化」節：loop 轉紅後逐項削減（輸入、呼叫端、設定、資料），每削一項重跑 loop，完成判準是「每個殘留元素都承重（再削任一項就轉綠）」
      4. 含「假設」節：一次列 3–5 個、逐一排名；每個必須可否證，固定格式「若 X 是根因，則改 Y 後 bug 消失／改 Z 後惡化」；寫不出預測句的假設不算假設。排名先回報使用者再開驗（使用者不在場則按自排名續行，不阻塞）
      5. 含「插樁」節：一次只變一個變數；每個探針對應一個具體假設；debug log 一律帶唯一前綴（格式 `[DEBUG-<4位隨機碼>]`），並明載理由：收尾清理是一次 grep
      6. 含「修法與收尾」節：修之前先在正確 seam 寫回歸測試（紅→修→綠）；**找不到正確 seam 時「無 seam 本身就是發現」**，寫入診斷結論、不硬塞假信心測試。收尾 checklist：原始（未最小化）情境重跑 loop 轉綠、grep debug 前綴清空、拋棄式 harness 已刪、正確假設寫進 commit message
      7. 收尾節含「什麼能預防這個 bug」一問，答案的去向**引用** CLAUDE.md 的 BUGLOG 兩層制與（架構性答案時）RETRO 升級判定，不重述其規則
      8. 產出格式與 CLAUDE.md 對齊：診斷結論固定三行（根因、影響面、修法），並註明它就是後續判級的 `spec_inline`
      9. 全文不重述 CLAUDE.md 的判級規則與 BUGLOG append 格式（各以一行指向）；`python3 -m unittest discover -s tests` 相對 baseline 無新增穩定失敗

- [x] 1.2 CLAUDE.md 指向行 ＋ VERSION bump  ~4行
      files: `CLAUDE.md`, `.claude/hooks/VERSION`
      DoD:
      1. CLAUDE.md「工作型態前判」步驟 1（診斷先行）句尾加指向：診斷的執行紀律（feedback loop 判準、假設可否證、插樁與清理）依 `bug-diagnosis` skill
      2. 該節其他文字零改動（`git diff` 可驗：CLAUDE.md 變更行數 ≤3）
      3. `.claude/hooks/VERSION` 內容改為 `2026.08.07.1`
      4. `python3 -m unittest discover -s tests` 相對 baseline 無新增穩定失敗（`tests/test_docs_consistency.py` 對 CLAUDE.md／VERSION 若有斷言須仍綠）

## 明確不做（scope 邊界）

- 不改 BUGLOG／RETRO 兩層制的任何規則（skill 只引用）
- 不動 `.claude/hooks/*.py` 任何可執行邏輯（VERSION 是純文字檔，不含邏輯）
- 不引入 mattpocock 的 grilling／CONTEXT.md／improve-codebase-architecture 機制（不在本 run 範圍）
- 不為本 skill 新增 hook gate（診斷紀律靠 prose 約束，與對方原設計一致）

---

# Run 2 of 3 — 雙軸審查與 smell 基準

> run_id: `2026-08-07-two-axis-review`（Tier 1 精簡路徑）
> manifest: `run/2026-08-07-two-axis-review.json`
> 需求原文見 manifest 的 `spec_inline`

## 背景（自足）

`code-reviewer.md` 的報告已有完成度節（＝mattpocock 的 Spec 軸）與品質節（＝Standards 軸），但缺兩件：**分離紀律**（總結不分軸統計，一軸的重問題會遮蔽另一軸）與**固定品質基準**（可維護性範疇只有 4 條泛則，無具名的 smell 基準可引用）。本 run 移植 mattpocock code-review 的雙軸分離原則與 Fowler 12 smell 基準表（Refactoring ch.3），基準表唯一枚舉點放 `code-reviewer.md`，eval-flow 只指向。

## Task 2：雙軸分離與 smell 基準層

- [x] 2.1 `code-reviewer.md`：雙軸分離紀律 ＋ Fowler smell 基準表  ~55行
      files: `.claude/agents/code-reviewer.md`
      DoD:
      1. 報告模板的總結改為**分軸統計**：完成度軸（缺席數＋最重缺席項）與品質軸（🔴／🟡 數＋最重問題）各自一行；明載**不跨軸合併排名、不挑單一 winner**，並附一句理由（合規但做錯事 → 品質過、完成度炸；做對事但違慣例 → 反之；合併排名會讓一軸遮蔽另一軸）
      2. 可維護性範疇（Checklist 第 4 節）加入 Fowler smell 基準層：12 條（Mysterious Name／Duplicated Code／Feature Envy／Data Clumps／Primitive Obsession／Repeated Switches／Shotgun Surgery／Divergent Change／Speculative Generality／Message Chains／Middle Man／Refused Bequest），每條格式「是什麼 → 怎麼修」，各一行
      3. smell 基準層三規則明載：(a) 全部判斷級——smell 只標 🟡、永不標 🔴，且須具名（例「possible Feature Envy」）；(b) repo 已文件化的慣例優先——衝突時壓掉 smell 不報；(c) 工具鏈（linter／formatter）已強制者略過
      4. 既有內容零破壞：完成度節規則、五維維度標記、通過即短、行號現查、測試變更需有依據等既有守則原文不動（diff 不觸及該些段落）
      5. 維度統計行不變（smell 發現照舊掛五維詞彙，多數落 Clarity／Technical_constraints）
      6. 全套測試相對 baseline 無新增穩定失敗
- [x] 2.2 eval-flow skill step 3 同步 ＋ VERSION bump  ~6行
      files: `skills/eval-flow/SKILL.md`, `.claude/hooks/VERSION`
      DoD:
      1. 循環 step 3 的兩節敘述後補一句：兩節即雙軸（完成度＝Spec 軸、品質＝Standards 軸），總結分軸統計、不跨軸合併排名；品質節含 Fowler smell 基準層（判斷級 🟡、repo 慣例優先、工具已強制者略過），基準表住 code-reviewer 定義（指向式，不重列 12 條）
      2. `.claude/hooks/VERSION` 改為 `2026.08.07.2`
      3. eval-flow 其他段落零改動；全套測試相對 baseline 無新增穩定失敗（gate 條數測試不受影響）

## 明確不做（scope 邊界）

- 不拆兩個平行 reviewer subagent（單 reviewer 雙節結構不變，拆分是 token 稅且失去單一報告的完成度—品質交叉視野）
- 不動 `eval_gates.py`、`review_reds`／`set-review` 語義、五維遙測詞彙
- smell 12 條只列在 `code-reviewer.md`（單一枚舉點），eval-flow 與其他文件一律指向
