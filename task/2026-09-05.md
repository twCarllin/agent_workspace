# Task — agentflow 借鑑兩項：model 政策表＋devlog 唯讀敘事 render  (created: 2026-09-05 21:20)

> run_id: 2026-09-05-model-policy-devlog（Tier 1）。需求全文見 manifest `spec_inline`（讀本檔＋manifest 即自足）。
> 測試指令：`python3 -m unittest discover -s tests`。單元測試綁進各實作 item（同 diff）。
> 全域約束：兩項均不動 init.sh（hooks 目錄整批複製自動涵蓋 devlog.py）、不動 settings.json、不動 eval_gates.py 任何判定式、不新增 gate、不新增手寫狀態檔。

## Task 1: model 政策表＋devlog render
- 依賴: 無（兩 item 無共用檔，可循序或 [P]）
- [x] 1.1 [P] MODEL_POLICY.md 政策表＋一致性測試＋eval-flow 指向更新  ~90行  files: MODEL_POLICY.md, tests/test_model_policy.py, skills/eval-flow/SKILL.md
      DoD: repo 根新增 MODEL_POLICY.md——機械可 parse 的 markdown 表（欄：agent、model、指派理由一句），收錄 .claude/agents/ 全部 7 個 agent 現值（code-writer=claude-sonnet-5、code-reviewer=claude-opus-4-8、impact-analyzer=claude-opus-4-8、retro=claude-sonnet-4-6、task-decomposer=claude-opus-4-8、task-verifier=claude-haiku-4-5-20251001、usage-analyzer=claude-opus-4-8），表後一段明文「去相關化約束：code-writer 與 code-reviewer 必須異族（model 家族名不同）」與「改 model 只改此表＋對應 frontmatter，兩處同 diff」；tests/test_model_policy.py 斷言 (a) 表列 agent 集合 == .claude/agents/*.md 檔案集合 (b) 每個 agent frontmatter model == 表值 (c) code-writer 與 code-reviewer 的 model 家族（model id 去掉 claude- 前綴後的首段，如 sonnet／opus）相異；skills/eval-flow/SKILL.md「Model 指派原則」第一條的 single-source 句改為指向 MODEL_POLICY.md（frontmatter 為執行端、政策表為審計端，測試強制兩者一致）；`python3 -m unittest tests.test_model_policy` 綠、`tests.test_docs_consistency` 綠
      契約: 任一 agent frontmatter model 改了但表沒改 → test_model_policy FAIL；表多列或漏列 agent → FAIL；writer 與 reviewer 同家族 → FAIL；[邊界] frontmatter model 行帶行內註解（現況：`model: claude-opus-4-8  # 說明`）→ 解析須取註解前的值、測試 PASS
- [x] 1.2 [P] devlog.py 唯讀敘事 render＋測試  ~260行  files: .claude/hooks/devlog.py, tests/test_devlog.py
      DoD: 新增 `.claude/hooks/devlog.py`（比照 stats.py：argparse、無第三方依賴、唯讀），用法 `python3 .claude/hooks/devlog.py <run_id> [--dir run]`，無參數時列出 run/ 下可用 run_id 清單；輸出 markdown 到 stdout，節次固定——①run 概要（run_id、tier、created_at、status/phase、tier_rationale、spec_path 或 spec_inline 摘句）②前置軌跡（risk/usage/impact/task_file 路徑與 hitl_confirmed_at、hitl_rejections）③sub_task 逐一（來源 `run/<run_id>.eval.json`；Tier 1 無此檔則改列 manifest 四憑據欄 local_test_passed/local_test_evidence/review_reds/verify_passed）——name、status、review_reds、rounds 數、local_test_evidence、verification_commands 條數④時間線（來源 `run/<run_id>.events.jsonl`，逐行 ts+cmd 摘要；無檔顯示「無事件記錄」）；一切缺漏欄位顯示 n/a、壞 JSON 行寬容跳過（比照 stats.py `_parse_events`），不猜不虛構；不寫任何檔案、不 import eval_gates/eval_state 的寫入路徑；tests/test_devlog.py 用 tmp 目錄造 manifest＋eval＋events fixture，斷言 (a) Tier 2 run 四節齊、sub_task 欄位正確 (b) Tier 1 run（無 eval.json）走 manifest 憑據 fallback (c) 無 events 檔顯示無事件記錄不 crash (d) run_id 不存在 → 非零 exit＋stderr 訊息；`python3 -m unittest tests.test_devlog` 綠
      契約: 存在完整 Tier 2 產物 → stdout 含四節；Tier 1 無 eval.json → 第③節 fallback 到 manifest 四憑據；events 壞行混入 → 跳過該行其餘照渲染；[邊界] run_id 不存在 → exit code 非 0、stderr 提示、stdout 無輸出
