# Task — Tier 2 [P] item worktree 並行（fan-out）  (created: 2026-07-25 00:00)

> run_id: `2026-07-25-tier2-p-worktree`（Spec: `spec/2026-07-25-tier2-p-worktree.md`；usage: `usage/2026-07-25-tier2-p-worktree.md`；impact: `impact/2026-07-25-tier2-p-worktree.md`）
> 本 task 檔自足可讀，不依賴對話上下文。
>
> **本 run 執行模式（硬性，HITL 裁示①／情境 J）**：本 run 改的是自己正在跑的 eval-flow／parallel-run／task-decomposition skill，改完即生效。**本 run 自身序列跑、不觸發自己的 fan-out**——下方所有 item **一律不標 `[P]`**（本 run 無真正並行意圖；改的是「未來 Tier 2 run 如何 fan-out」的 prose 規則）。全部由主 flow 循序派 code-writer 或（小 prose item）主 flow 直寫，逐 item `git add`→review∥verify→step5→step6 收尾單一原子 commit（退回單 run 語義）。
>
> **契約表適用性（拆分者註記）**：本 run 產出以 prose（skill／agent 定義的散文規則）為主，prose item **無「輸入→可觀察副作用」的執行期行為可列契約表**——這些 item 改的是「未來 run 的執行指南文字」，其正確性驗收基準是**文字一致性與 grep 可驗的關鍵句存在／消除**，故以 DoD（含 grep 斷言）替代契約表，並在該 item 註明「prose item：以 DoD／grep 斷言替代契約表」。唯一有執行期行為的是 Task 3 的 hook code item（`parent_run_id` 相容、`MANIFEST_RE` 對子 manifest 匹配），該 item 附行為契約表。
>
> **無「有意的行為變更／作廢舊行為清單」需求（拆分者核對結論）**：本 run 對 hook 是**純新增可選欄位**（`parent_run_id`，codebase 零讀取端，impact 介面 A 已證），不改任何既有 hook 行為；agent／skill prose 改寫是「新增 file-scoped 收斂範圍＋改寫 [P] 併發語義」，被改寫的 [P] 共用樹段是**尚未實戰的規則描述**、非「既有可觀察行為」，且 file-scoped 是既有 `git diff --cached -- <files>` 用法的延伸（impact §3、eval-flow:233 先例）。故無既有測試需同步、無測試同步段。若 code-writer 實作 Task 3 hook 時發現任何既有測試斷言需被改動（非純新增 case）→ 觸發升級逃生門停下回報（代表相容假設破，超出本 run 範圍）。

---

## Task 1: file-scoped diff 底層修法（循序退回路徑的驗證收斂）  對映情境 C／C-edge1，DoD 2  （2 items：1.1、1.2；原 1.3 一致性驗證已併入 1.2 (e)）
- 依賴: 無（獨立可驗收：與 fan-out 無關的底層必需，Spec 4.3）
- 說明: 循序 sub_task 的 staging 累積污染，即使沒有並行也存在。把 code-reviewer／task-verifier 的取 diff 從全域 `git diff --cached` 改為 file-scoped `git diff --cached -- <files>`（`<files>` 取自 `eval_state.py list-files` helper 的輸出）。**守則不可破**：staging 命中為空時仍走「空即停、不 fallback 全域」（task-verifier.md:23、code-reviewer.md:17）；不使用 unstaged `git diff`（:24／:18）。**只改 4 處目標，避讓其餘 8 處既有 `git diff --cached` 引用**（impact 介面 B：resume:233、eval-flow-resume:35、test-strategy:84/87/103、task-decomposition:25、test_eval_gates.py:214）。

- [x] 1.1 兩 agent 定義改 file-scoped diff（保留空即停守則）  ~90行  files: `.claude/agents/task-verifier.md`（行 21 區塊）, `.claude/agents/code-reviewer.md`（行 15 區塊）
      DoD:
      (a) task-verifier.md:21「固定使用 `git diff --cached`」改為「使用 `git diff --cached -- <files>`，`<files>` 為本次 sub_task 的涉及檔案清單（由主 flow 派工 prompt **依當前 sub_task 的 `files` 欄位**提供——主 flow 讀 `eval_state.json` 該 sub_task 的 `files`；`eval_state.py list-files` 回傳全 sub_task 聯集、**不可**當單一 sub_task 來源）」；code-reviewer.md:15 同步同義改寫。〔r1 修正：原稿誤把 list-files 標為來源，reviewer 🟡 抓出，list-files 是聯集〕
      (b) 兩處「staging area 為空即停止並回報、不自行 fallback」守則（task-verifier:23、code-reviewer:17）**原文保留**，並補一句「`-- <files>` 命中為空時同樣走空即停，不 fallback 全域 diff」。
      (c) 兩處「不使用 unstaged `git diff`」守則（:24／:18）保留。
      (d) 補「同檔跨 sub_task 邊界」註（Spec 4.3／情境 C-edge1）：一句「兩個 sub_task 改到同一檔案時，`-- <file>` 會把兩者變更一起帶出，此時以 task 描述判當前 sub_task 的歸屬」。
      (e) grep 驗證：`grep -n "git diff --cached -- " .claude/agents/task-verifier.md .claude/agents/code-reviewer.md` 各 ≥1 命中；`grep -n "空即停\|staging area 為空" ...` 守則仍在。
      情境: C, C-edge1
      契約: prose item（agent 定義散文）——以上 DoD／grep 斷言替代契約表（無執行期輸入→副作用可列）。

- [x] 1.2 eval-flow 循環 step 3 派工描述 ＋「Subagent 呼叫原則」明文同步 file-scoped  ~110行  files: `skills/eval-flow/SKILL.md`（step 2-3 派工區 :80 附近、`Subagent 呼叫原則` :110）
      DoD:
      (a) step 2（:80）`git add` 描述處補一句 file-scoped 派工指示，沿硬約束區體例（附可直接貼進 prompt 的指令）：「派 code-reviewer／task-verifier 時，prompt 硬性指示改用 `git diff --cached -- <files>`，`<files>`＝**當前 sub_task 的 `files`**（主 flow 讀 `eval_state.json` 該 sub_task 的 `files` 欄帶入；收斂到當前 sub_task 涉及檔，避免跨 sub_task staging 累積污染）。**注意**：`eval_state.py list-files` 是全 sub_task 聯集，不是單一 sub_task 來源、不可用於此。」
      (b) `Subagent 呼叫原則`（:110）「必須在 prompt 中指示使用 `git diff --cached`」同步為 `git diff --cached -- <files>`，與 (a)／agent 定義三處表述一致，**不得殘留光禿的全域 `git diff --cached`**（否則 skill 說全域、agent 說 file-scoped，自相矛盾——impact §5 風險）。
      (c) 與情境 C 對齊：明文「此收斂為退回主 worktree 循序時的污染修法（與 fan-out 無關、底層必需）」。
      (d) grep 驗證：`grep -n "git diff --cached -- <files>\|list-files" skills/eval-flow/SKILL.md` 於 step 區與呼叫原則區各命中；確認未把 :233（resume 還原）等既有 file-scoped 用途改動。
      (e) **併入原 1.3 的 Task 1 尾端一致性驗證**（本 item 為 Task 1 最後一個內容 item，1.1 的兩 agent 改動此時已 staged，可一併驗）：跑 4 目標點一致性 grep 全通過才算完成——
          (1) `grep -rn "git diff --cached -- <files>" .claude/agents/task-verifier.md .claude/agents/code-reviewer.md skills/eval-flow/SKILL.md` ≥4 命中（兩 agent＋eval-flow step＋呼叫原則）；
          (2) `grep -rn "空即停\|staging area 為空\|不.*fallback" .claude/agents/task-verifier.md .claude/agents/code-reviewer.md` 守則仍在（DoD 2 正確性核心：file-scoped 不得把「空即停」改成 fallback 全域——impact §5）；
          (3) 抽查 `skills/eval-flow/SKILL.md` 的 :233（resume 還原）等既有非目標 file-scoped 引用未被波及、仍為原語義。
          mutation self-check（prose 適配）：故意在其中一處還原成全域 `git diff --cached` 應被 (1) 檢出缺項；還原後綠。證據記入本 sub_task 的 `local_test_evidence`。
      DoD（自足性）: 派工指令句可被未參與對話者直接複製使用，檔案路徑寫全。
      情境: C, C-edge1
      契約: prose item（skill 派工散文＋Task 1 尾端一致性驗證）——以 DoD／grep 斷言替代契約表。

---

## Task 2: 三段式 fan-out 主體 ＋ 三 skill／[P] 語義一致性改寫  對映 A／B／D／D-err1-3／E／F／F-err1-2／G／H／J  depends: Task 1
> depends 理由: fan-out 的「退回門檻不滿足→循序」路徑（情境 C）引用 Task 1 的 file-scoped 修法；Task 1 先落地，Task 2 的退回路徑描述才有可引用的機制，避免重複定義。

- [x] 2.1 eval-flow 三段式 fan-out 主體（prep run→completed 才 fan-out／fan-out 段迷你 run／rolling merge 重用）＋門檻與退回  ~280行  files: `skills/eval-flow/SKILL.md`（新增「Tier 2 [P] fan-out（worktree 並行）」節，接於循環章與 273 併發節之間或其後）
      DoD:
      (a) 明列**三段式執行**（Spec 4.1／情境 A/B/F）：①循序前置段（prep run）——depends 型 item 在主 worktree 循序做完，照現行 step 6 正常歸檔＋commit、**父 manifest 標 `completed` 才 fan-out**（附機械依據：`eval_gates.py:169 check_other_runs` 對同工作區另一 in_progress manifest 直接 block subagent，父 run 保持 in_progress 會擋 item worktree 的 code-writer；歸檔 gate 對未歸檔 eval_state 擋 commit——D4／假設 3）；②Fan-out 段——每個符合門檻的 [P] item 從當前 run HEAD 切 `feat/<run_id>-item-<id>` worktree，以**獨立 sibling 迷你 run** 跑完整 Tier 2 循環，子 manifest `run/<run_id>-item-<id>.json`（`spec_path` 指回父 Spec、`parent_run_id`=父 run_id）、自己 eval_state、自己歸檔、自己 branch commit（trailer 帶自己 Run-Id）；③Rolling merge 段——**直接引用 `skills/parallel-run/SKILL.md` 收尾序列**（不重寫），一句話點名重用①測試只增不改②實際交集重驗③後合者先 `git merge main`④全套 baseline gate⑤BUGLOG 帶回統一 append＋兩層制升級判定。
      (b) **門檻與退回**（Spec 4.2／D2／HITL③／情境 C）：fan-out 僅在「[P] item ≥2 且各估計 ≥150 行（以 task-decomposer 的 `~<行數>行` ×2 校準估計為準）」時啟動；估不準即不 fan-out（保守偏循序）；不滿足 → 該批 [P] 退回主 worktree **循序**，配 Task 1 的 file-scoped diff 修法（引用 Task 1，不重述）。
      (c) **本 run 自身序列跑**的操作約束（情境 J／HITL①）：新機制首次執行不用在改它自己的 run 上；一句註明「改此 skill 的 run 自身序列、不 fan-out」。
      (d) grep 驗證：`grep -n "feat/<run_id>-item-<id>\|parent_run_id\|prep\|rolling merge\|≥2 且各.*150\|150 行" skills/eval-flow/SKILL.md` 命中三段式關鍵句。
      DoD（大 item 註記／軟上限）: 本 item ~280 行**貼近但未破 300 行硬拆線**；不再拆的理由——三段式（prep→fan-out→merge）是**單一內聚的執行協定**，切開會讓「prep 必須先 completed 才 fan-out」這條跨段不變量散落兩 item、接手者拼不回完整協定（impact §5 一致性殘留風險）。此為 task-decomposition skill 允許的「內聚不宜切開」保留大 item，理由成立。
      情境: A, B, D, F, J
      契約: prose item（skill 執行協定散文）——以 DoD／grep 斷言替代契約表（改的是未來 run 的執行指南，無本 run 執行期行為）。
      註（情境 D 對映）: fan-out 段 (a) 須含「背景 agent 迷你 run 執行細節」——在自己 worktree init 子 eval_state→code-writer（mine 模式復活，乾淨 diff）→review∥verify→step5→自己歸檔→自己 branch commit；**BUGLOG 條目寫進回報內容、不 append 檔案**（沿 parallel-run，兩 worktree 各 grep 自己快照會漏看對方）；禁碰主工作區與他 worktree。

- [x] 2.2 eval-flow 舊「[P] 共用樹」段（273–279）改寫＋殘留清除  ~150行  files: `skills/eval-flow/SKILL.md`（「單一 run 原則與併發」節 273–279、:69 引 279 段、:77 末句、:82 末句「各開 worktree」）
      DoD:
      (a) 現行「單一 run 內 [P] 併發 code-writer 共用同一棵樹」＋測試 barrier＋「mine 不適用」註（:69／:77／:82／279 段）改寫為 fan-out 語義：[P] item 各開 worktree（門檻滿足）或循序退回（門檻不足），**不再有「共用同一棵樹的併發 writer」語義**（Spec §1 根因、情境互動點 115）。
      (b) 「單一 worktree 同時只一個 in_progress run」「插單凍結」等既有正確段落**保留不動**（非本變更範圍）。
      (c) **殘留清除（DoD 5 核心、impact §5 風險）**：grep 關鍵句 `grep -rn "共用同一棵樹\|共用樹\|併發 code-writer\|平行寫作的測試 barrier" skills/eval-flow/SKILL.md` → 改寫後應**無殘留舊語義**（0 命中，或僅在「已改為 worktree 隔離」的對照說明中出現）；避免接手者拿到自相矛盾的兩套 [P] 併發規則。
      情境: J（互動點：eval-flow 273–279 改寫）
      契約: prose 改寫／殘留清除 item——以 DoD／grep 斷言替代契約表。

- [x] 2.3 parallel-run skill 標明 Tier 2 fan-out 重用點（機制共用、不重複）  ~120行  files: `skills/parallel-run/SKILL.md`
      DoD:
      (a) 在 parallel-run 標明「Tier 2 [P] fan-out 重用本 skill 的：worktree 開設（`feat/<run_id>-item-<id>` 為 `feat/<run_id>` 格式延伸）、背景 agent spawn、rolling merge 收尾序列①-⑤、機械檢查①②、卡住協定、批次中斷恢復」（Spec 4.4／impact 互動點 116）。
      (b) 標明**分岔點**（不同於跨獨立 Tier 1 需求並行）：parallel-run 前置是「逐一判級＋批次 HITL」，Tier 2 fan-out 換成「已過父 run 前置 0–3」；「既有測試只增不改」前提沿用（含此類 item 不進 fan-out 批，留循序段——情境 D-err3／DoD 3）。
      (c) 不改 parallel-run 既有跨 Tier 1 語義（避免 impact 互動點 116「既有語義被誤改」風險）：grep `grep -n "Tier 2\|fan-out\|parent_run_id" skills/parallel-run/SKILL.md` 命中新增重用節，且既有 rolling merge 序列原文未被改弱。
      情境: A, F, D-err3
      契約: prose 重用標註 item——以 DoD／grep 斷言替代契約表。

- [x] 2.4 task-decomposition skill 補 [P] worktree 門檻註記＋卡住／溯源／退回輔助 prose  ~140行  files: `skills/task-decomposition/SKILL.md`（Step 4 [P] 標註段、item 行數估計格式段）, `.claude/agents/task-decomposer.md`（[P] 描述一致性）
      DoD:
      (a) Step 4 [P] 標註段補門檻註記（D2／HITL③）：「[P] item **≥2 個且各估計 ≥150 行**（以本欄 `~<行數>行` ×2 校準值為準）才觸發 Tier 2 worktree fan-out；否則循序退回。估計不準時保守處理——估不準即不 fan-out。」；disjoint 檔案前提**不動**（Spec Non-goal）。
      (b) 行數估計格式段點明 150 下限與 300 上限並存（下限觸發 fan-out、上限觸發再拆——impact task-decomposition 慣例 :37）。
      (c) `.claude/agents/task-decomposer.md` 內 [P] 描述與改後 skill 一致，不留舊語義（impact 介面 C：task-decomposer.md 命中 [P]）。
      (d) grep 一致性（DoD 5）：`grep -rn "\[P\]" skills/task-decomposition/SKILL.md .claude/agents/task-decomposer.md` 逐處確認語義一致；`grep -n "150 行\|≥150\|fan-out\|worktree" skills/task-decomposition/SKILL.md` 命中新門檻註記。
      情境: A（門檻判定的行數來源），C（退回門檻）
      契約: prose 門檻註記 item——以 DoD／grep 斷言替代契約表。

- [x] 2.5 fan-out 情境的錯誤路徑／溯源／中斷恢復 prose（卡住協定命名、Parent-Run-Id trailer、批次 resume）＋三 skill 跨檔一致性驗證  ~180行  files: `skills/eval-flow/SKILL.md`（fan-out 節錯誤路徑子段）, `skills/parallel-run/SKILL.md`（卡點命名／trailer 引用點）；驗證證據寫入 `local_test_evidence`
      DoD:
      (a) **卡住協定**（情境 D-err1/2/3、HITL⑤）：item 背景 agent 卡住（2 次真失敗／升級逃生門／需裁決）→ 停前落盤：子 manifest 標 `status: blocked`＋blocked_reason，寫卡點報告 **`run/<子 run_id>-blocked.md`**（＝`run/<run_id>-item-<id>-blocked.md`，沿 parallel-run 慣例、worktree 隔離下不衝突）；blocker 根因在 main 既有 code → 禁在 worktree 修，回主 session 走 bugfix、修後 `git merge main` 同步（D-err2）；漏篩「需改既有測試」item → 退出並行改循序（D-err3／DoD 3）。
      (b) **溯源**（情境 H、HITL②／Spec 開放問題 2）：子 run commit trailer 同附 `Parent-Run-Id: <parent_run_id>`（＋各自 `Run-Id`），一次 `git log --grep "Parent-Run-Id: <parent_run_id>"` 撈全族；prep run 帶父自身 Run-Id。派 code-writer 給 fan-out item 時 trailer 格式須含此句。
      (c) **編排錯誤兜底**（情境 E）：門檻／切批是 prose 驅動、hook 不硬檢（HITL⑥）；skill 寫成機械可查檢核步驟（比照 parallel-run 兩兩比對），兜底靠子 run gate＋merge 機械檢查②（實際交集非空→停）。
      (d) **批次中斷恢復**（情境 G）：`git worktree list`＋各 manifest 狀態＋main `git log` grep Parent-Run-Id＋`run/parallel-merge-*.test_baseline.json`；列「已 merge 但 BUGLOG 未 append」遺失窗口為機械重建檢查項。
      (e) **三 skill 跨檔一致性總驗（DoD 5）**：`grep -rn "共用同一棵樹\|共用樹" skills/` 應無殘留舊 [P] 併發語義（跨 eval-flow／parallel-run／task-decomposition＋task-decomposer.md／test-strategy.md 五處——impact 介面 C）；`grep -rln "\[P\]" skills/ .claude/agents/` 四檔逐一確認語義互不矛盾（含 test-strategy.md 的 mine×[P] 互動描述與改後共用樹段一致）。此為 Task 2 尾端的跨 prose 一致性驗證 item（本 task 全 prose、無執行期行為，mutation self-check 以「反向 grep：故意保留一句舊語義應被 (e) 檢出」替代）。
      DoD（自足性）: 所有命名慣例、trailer 格式、grep 指令寫全，未參與對話者可據以驗收。
      (f) **rolling merge 錯誤路徑**（情境 F-err1/F-err2）：merge gate 全套測試紅燈（跨兩支語意衝突）→ 由 bugfix 走既有「診斷先行」在**主 session（main）**做（單一 worktree 視野只有一半）；機械檢查②實際交集非空（disjoint 前提被破）→ 停下回報、該支退出並行改序列。此二路徑沿用 parallel-run 收尾序列，本 item 只須在 fan-out 節點名引用、不重寫機制。
      情境: D-err1, D-err2, D-err3, E, F-err1, F-err2, G, H
      契約: prose 錯誤路徑／一致性驗證 item——以上 DoD／grep 斷言替代契約表。

---

## Task 3: hook parent_run_id 相容 ＋ 迴歸測試 ＋ worktree 隔離實測  對映情境 I／假設 3、4，DoD 4  depends: Task 2  （3 items：3.1、3.2、3.3；原 3.4 降為收尾驗證清單）
> depends 理由: 隔離實測（3.3）驗證的是 Task 2 定義的 fan-out 期間 hook 行為（子 manifest 不洩漏主 worktree、`check_other_runs` 不誤擋）；hook 相容（3.1）對應 Task 2 引入的 `parent_run_id` 欄位語義。Task 2 的 prose 規則先定，Task 3 才有可實測的對象。

- [x] 3.1 `eval_gates.py` 支援 `parent_run_id` 向後相容（純新增可選欄、零現存讀取端）＋子 manifest `MANIFEST_RE` 匹配確認  ~120行  files: `.claude/hooks/eval_gates.py`（`manifest_phase` @135、`check_other_runs` @155、`MANIFEST_RE` @22 相關）
      DoD:
      (a) `parent_run_id` 為**可選欄位**：舊 manifest（無此欄）不炸，視同無父 run（比照 `scout_report_path` 舊欄相容慣例 eval_gates.py:135-144）。**不新增任何強制讀取**——impact 介面 A 已證 codebase 零讀取端；本 item 只確保「若 manifest 含此欄，既有 gate 行為完全不變」，必要時僅加防禦性容錯，**不改既有 gate 判定邏輯**（Spec Non-goal 4「不降低既有 gate 效力」、HITL④「hook 零改動」）。
      (b) 確認子 manifest 命名 `run/<run_id>-item-<id>.json` 被 `MANIFEST_RE`（@22-24 負向後查 `.eval`／`.test_baseline`）正確識別為合法獨立 manifest、不被誤排除（impact 介面 D 已對 pattern 求值確認匹配；本 item 以測試固化該行為）。
      (c) 〔主 flow 執行時裁定〕主 flow 讀 eval_gates.py 確認相容**零邏輯改動**即成立（所有 manifest 讀取皆 `.get()`、`MANIFEST_RE` `[^/]+?` 已匹配子 manifest 且負向後查排除 `.eval`）。零 diff 會卡死 review∥verify，故本 item 走「補最小防禦性改動」路徑：加**防禦性文件註記**——在 `MANIFEST_RE` 定義處註明「子 manifest `run/<run_id>-item-<id>.json`（Tier 2 fan-out）為合法 manifest、由本 pattern 匹配」，並註明 `parent_run_id` 為 gate 忽略的可選欄。**不改任何 gate 判定邏輯**（DoD 守則），把隱性契約 explicit 化、防未來誤改；實際固化由 3.2 迴歸測試負責。
      (d) **升級逃生門**：若實作中發現需改動任何既有 gate 判定邏輯才能相容（非純新增）→ 停下回報（代表相容假設破、超出本 run「hook 零改動」範圍）。
      情境: I
      契約（行為契約表，hook code item）:
        - 輸入: manifest 含 `parent_run_id: "<父run>"` → 可觀察效果: `manifest_phase()` 回傳值與不含此欄時**完全相同**（phase 推導不受影響）；`check_other_runs` 對其 in_progress 判定行為不變。
        - 輸入: 舊 manifest 無 `parent_run_id` 欄 → 效果: 照常運作、視同無父 run，不拋例外（KeyError 不發生）。
        - 輸入: 子 manifest 路徑 `run/2026-07-25-x-item-1.json` → 效果: `MANIFEST_RE.match` 命中、`run_id`=`2026-07-25-x-item-1`（視為合法獨立 manifest）。
        - [邊界] 輸入: 衍生檔 `run/2026-07-25-x-item-1.eval.json` → 效果: `MANIFEST_RE` **不**匹配（負向後查排除，與子 manifest 走不同分支——確認加 `-item-<id>` 前綴不破壞既有排除）。
        - [邊界] 輸入: 子 manifest commit 時（`check_manifest`）→ 效果: 要求 `run/<run_id>-item-<id>.eval.json` 已 staged，子 run 各自歸檔即滿足（gate 行為不變，impact 介面 D）。

- [x] 3.2 hook 相容迴歸測試（`parent_run_id`＋子 manifest 命名固化為綠燈）  ~200行  files: `tests/test_eval_gates.py`（比照 `ManifestPhaseTest:113-129`／`ManifestRegexTest:132-147` 體例新增 case）
      DoD: 依 impact §5「相容無測試即無效」與 DoD 4——把 3.1 的相容行為從斷言變綠燈：
        (1) `manifest_phase` 讀含 `parent_run_id` 的 manifest 回傳與不含時相同（新增 test，assert 相等）；
        (2) `check_other_runs`／`check_manifest` 對含 `parent_run_id` 的 manifest 行為不變（比照既有 `assertRaises(SystemExit)` / 回傳斷言體例）；
        (3) `MANIFEST_RE` 對 `run/<run_id>-item-<id>.json` 匹配且 `run_id` group 正確、對 `-item-<id>.eval.json`／`.test_baseline.json` **不**匹配（擴充 `ManifestRegexTest`，含 3.1 契約表的兩條邊界 row）。
      DoD（測試品質硬要求）: 測試為 `unittest.TestCase`、class 名 `<主題>Test`（沿慣例 impact §2）；**不引入 fixture 檔**（本測試直接構造 manifest dict／路徑字串斷言，無外部 producer 輸出需對齊，故 fixture producer 行號依據規則不適用——註明「無 fixture、直接構造輸入」）；測試為 3.1 相容行為的 single source。
      情境: I
      契約: 本 item 為 3.1 的測試載體，契約表即 3.1 的行為契約表（同 diff、同 writer——測試隨實作）。

- [ ] 3.3 worktree 隔離實測驗證（子 manifest 不洩漏主 worktree＋`check_other_runs` 不誤擋）  ~140行  files: `tests/check_worktree_isolation.sh`（**可重跑實測腳本，durable artifact——有可審 diff、不是無檔驗證步驟**；腳本自動化下方 (1)-(5) 步驟，執行輸出摘要另記 `local_test_evidence`）
      DoD（DoD 3／假設 4、假設 3 的「不只信推理」實測——HITL④「仍寫 item DoD 實測驗證」）:
        (1) 在測試 worktree（`git worktree add` 一個臨時 worktree＋branch）內建一個 in_progress 子 manifest `run/<run_id>-item-<id>.json`；
        (2) 回主 worktree 執行 `ls run/` 與 `git status`，**確認主 worktree 的 `run/` 不含該子 manifest**（子 manifest 落在子 worktree 的工作目錄／未合回 main，故主 worktree 掃不到——假設 4 的機械根據 `check_other_runs:157` 只 glob 當前工作區 `run/*.json`）；
        (3) 在主 worktree 觸發一次 subagent 呼叫路徑的 gate 檢查（或直接呼叫 `check_other_runs`），**確認不被子 worktree 的 in_progress 子 manifest 誤擋**（gate 不誤報「本工作區已有另一個 in_progress run」）；
        (4) 反向確認：**同一 worktree 內**存在 in_progress 子 manifest 時，`check_other_runs` **會**擋（符合設計——D4「同 worktree 內子 manifest in_progress 應擋」，隔離只跨 worktree 生效）；
        (5) 實測後 `git worktree remove` 清理臨時 worktree，不留孤兒。
      DoD（實測留痕）: `local_test_evidence` 逐條記 (1)-(4) 的實際輸出（`ls run/` 結果、gate 回傳／是否 block），證明假設 4「worktree 天然隔離、hook 零改動」**由實測而非推論成立**（impact §5「hook 零改動假設須實測佐證」）。
      情境: 對映 usage 正確性假設 4（item worktree 彼此隔離）＋假設 3（父 completed 才 fan-out 的 gate 機械根據）
      契約（行為契約表，有執行期 gate 行為）:
        - 輸入: 子 worktree 內存在 in_progress 子 manifest，主 worktree 掃 `run/` → 效果: 主 worktree `ls run/` 不含該子 manifest（跨 worktree 不可見）。
        - 輸入: 主 worktree 觸發 subagent gate 檢查、他 worktree 有 in_progress 子 manifest → 效果: **不 block**（`check_other_runs` 只掃當前工作區）。
        - [邊界] 輸入: **同一** worktree 內有 in_progress 子 manifest 再觸發 subagent → 效果: **block**（設計行為，隔離不跨同一 worktree——與上一條走相反分支）。

> **原 3.4 已降為「run 收尾驗證清單」**（見本檔末「收尾驗證」節）——它全是無 diff 的 grep／全套測試檢查，屬 step 6 ⓪＋主 flow commit 前的收尾動作，非 code-writer sub_task（無可審 diff 會卡死 review∥verify）。故不列為 sub_task。

---

## 收尾驗證（step 6 收尾，主 flow 執行，非 sub_task；原 3.4）

全部 sub_task passed、進 step 6 收尾時，主 flow 於 commit 前逐項確認（皆為無 diff 的 grep／全套測試，故不走 review∥verify）：

1. **一致性總驗（DoD 5）**：`grep -rn "共用同一棵樹\|共用樹" skills/`＝0 殘留；`grep -rln "\[P\]" skills/ .claude/agents/` 四檔語義一致（eval-flow／task-decomposition／test-strategy／task-decomposer.md）；file-scoped `git diff --cached -- <files>` 在 4 目標點一致（Task 1）；fan-out 三段式關鍵句、`Parent-Run-Id` trailer、`≥2 且各 ≥150` 門檻、卡點命名 `run/<子 run_id>-blocked.md` 各在對應 skill 命中。
2. **非目標未觸犯（Spec §6）**：確認未新增「原子 squash commit」語義、未改 `stats.py`（`git diff --stat` 不含 stats.py）、未改 `[P]` disjoint 前提、未削弱任一既有 hook gate（3.2 全綠即證）。
3. **全套測試 baseline gate（step 6 ⓪）**：`python3 .claude/hooks/test_baseline.py check --cmd "<全套指令>" --strike-key full_suite` 相對 baseline 無新增失敗（DoD 6）。

---
---

# Task — 流程去儀式化五條（deregulation）  (created: 2026-07-25 16:10)

> run_id: `2026-07-25-flow-deregulation`（Tier 1，無 Spec 檔；需求原文見 manifest `run/2026-07-25-flow-deregulation.json` 的 `spec_inline`）
> 本 task 檔自足可讀，不依賴對話上下文。
>
> **背景**：現行流程對「模型作弊」的防線已足夠（引文核實、mine_log 指紋、hook gate），但累積了一批**每次都要做、卻幾乎從不改變結論的儀式**。本 run 砍五處儀式，**不動任何攔截條件**——🔴 攔截、intent gate、高風險排除清單、引文核實、mine_log 指紋全部原樣保留。
>
> **Tier 判定備註（稽核用）**：item 2.2 須改 `.claude/hooks/eval_gates.py` 的 gate 判定式，依 Router 排除清單屬高風險面、本應 Tier 2。**使用者於 2026-07-25 明示裁示「五條全走 Tier 1」**，覆寫 Router 的高風險排除；agent 未自行降級。此為既有防濫用規則的一次明示例外，已記入 manifest `tier_rationale`。
>
> **本 run 的自舉注意（硬性）**：本 run 改的是自己正在跑的 eval-flow／hook，改完即生效。**本 run 自身依「舊規則」收尾**——已建 `eval_state.json`，照現行 step 6 歸檔為 `run/<run_id>.eval.json` 再 commit。item 2.2 的 hook 改動**必須向後相容**（歸檔檔存在時照舊驗），否則本 run 自己 commit 不過。
>
> **契約表適用性**：item 1.1／2.1 為 prose（skill／agent 定義散文），無執行期「輸入→可觀察副作用」，以 DoD＋grep 斷言替代契約表。item 2.2 動 hook 判定式、有執行期行為，附行為契約表。
>
> **無既有測試需改（拆分核對）**：item 2.2 對 hook 是**新增 tier==1 分支**，既有分支行為不變 → 既有 `tests/test_eval_gates.py` 斷言應全數續綠，只新增 case。若實作時發現任何既有斷言須改動 → 相容假設破，觸發升級逃生門停下回報。

---

## Task 1: 文件層放寬（三處純 prose，合併一輪審）
- 依賴: 無
- 說明: 三條都是「砍掉儀式性的全量要求」同一設計決策的多處投放，且各檔改動 ≤30 行、無 code → 依 eval-flow「Tier 1 小 prose item 合併」規則**合併為一個 sub_task 一輪審**。

- [x] 1.1 Tier 0 門檻放寬 ＋ 風險六面向改觸及才寫 ＋ mutation 降為每 run 抽樣  ~65行  files: `CLAUDE.md`, `skills/task-risk-analysis/SKILL.md`, `skills/eval-flow/SKILL.md`（前置 1 段）, `skills/test-strategy/SKILL.md`（Mutation self-check 段）
      DoD:
      (a) **Tier 0 門檻**（`CLAUDE.md` Router 表 Tier 0 列）：「1~2 個檔案、每檔 ≤30 行」改為「**≤3 個檔案、合計 ≤80 行**（以 `git diff` 增＋刪計）」；機械式跨檔重複改動的既有例外條款保留（每檔 ≤50 行那條）。**風險軸原文一字不動**——「不觸及高風險面（同 Tier 1 的排除清單，1 行也不例外）」必須仍在。
      (b) **風險六面向**（`skills/task-risk-analysis/SKILL.md` ＋ `skills/eval-flow/SKILL.md` 前置 1）：從「六大面向逐一標註等級」改為「**只寫觸及的面向；其餘以一行 `不適用：<面向清單>` 帶過**」。**攔截條件原文不動**：「有 🔴 → 不可進入任何後續步驟、須先改 Spec」必須仍在，且須補一句「面向未寫不等於已評估為無風險——判為不適用者仍須列名，漏列即視同未做」。
      (c) **mutation self-check**（`skills/test-strategy/SKILL.md`）：從「每個整合測試 item 都做」改為「**每 run 抽一個整合測試 item 做完整版（含主 flow 獨立重放），其餘 item 記『沿用本 run mutation 結論』**」。停損條款（2 次 sabotage 仍綠即停手回報、禁止加壓硬湊 FAIL）與「第一步先質疑需求」原文保留。
      (d) grep 驗證：`grep -n "≤3 個檔案\|合計 ≤80 行" CLAUDE.md` ≥1 命中且 `grep -n "1 行也不例外" CLAUDE.md` 仍 ≥1；`grep -rn "不適用：" skills/task-risk-analysis/SKILL.md skills/eval-flow/SKILL.md` ≥1 且 `grep -rn "不可進入任何後續步驟" skills/eval-flow/SKILL.md` 仍 ≥1；`grep -n "沿用本 run mutation 結論" skills/test-strategy/SKILL.md` ≥1 且 `grep -n "2 次 sabotage" skills/test-strategy/SKILL.md` 仍 ≥1。
      契約: prose item——以上 DoD／grep 斷言替代契約表。

---

## Task 2: 機制層（砍 scout ＋ Tier 1 豁免 eval_state）
- 依賴: 無（2.1、2.2 互不相干，但同 run 循序執行，不標 [P]）

- [x] 2.1 砍掉前置 1.5 scout（職責併回 impact-analyzer／usage-analyzer 自掃）  ~80行  files: `skills/eval-flow/SKILL.md`（前置 1.5 全節、前置 2／2.5 的 scout 引用、Subagent 呼叫原則 :113、manifest 格式 :143／:159、Tier 1 精簡路徑 :246）, `.claude/agents/impact-analyzer.md`（「證據來源優先序」節）, `.claude/agents/usage-analyzer.md`（「證據來源優先序」節）, `.claude/agents/scout.md`（刪除）, `CLAUDE.md`（:58 scout 資料夾條）, `skills/task-decomposition/SKILL.md`（:31 scout 引用）
      說明: scout 的立意是省 usage／impact 兩隻 opus 的掃碼 token，但代價是多一個 agent 呼叫＋一份報告＋一次主 flow 回寫＋一組 skip／降級規則，且 impact 的呼叫端清單本就被要求自行重跑 Grep（省不到）。淨效益為負 → 整步移除。
      DoD:
      (a) `skills/eval-flow/SKILL.md` 刪除整個「### 前置 1.5：scout 蒐證」節；前置 2（:50）與前置 2.5（:55）中所有「`scout_report_path` 有值時附證據檔路徑／改讀證據檔＋抽查」的條件句移除，回復為「自行掃 codebase」的單一路徑；`Subagent 呼叫原則` :113 的「retro / scout」改為「retro」。
      (b) manifest 格式：移除 `scout_report_path` 範例欄（:143）與其說明條（:159）；Tier 1 精簡路徑 :246 的 `scout_report_path: "skipped"` 一併移除。**加一句向後相容註**：「舊 manifest 仍有 `scout_report_path` 欄者不需回填移除，hook 無此欄依賴」。
      (c) `.claude/agents/impact-analyzer.md` 與 `.claude/agents/usage-analyzer.md` 各自的「## 證據來源優先序（scout 證據檔存在時）」整節刪除；兩檔中「prompt 未附證據檔路徑（scout skipped）→ 照原方法自行掃碼」所描述的**原方法即成為唯一方法**，確認刪節後兩檔仍完整說明各自的掃碼職責（缺則補一句）。
      (d) 刪除 `.claude/agents/scout.md`；`CLAUDE.md:58` 的「scout 蒐證檔放在 `scout/` 資料夾」條移除。**`scout/` 目錄與既有報告檔保留**（歷史 run 的產出，屬冷溯源，不刪）。
      (e) `skills/task-decomposition/SKILL.md:31` 的「以 scout／…」引用改為不提 scout 的等義敘述。
      (f) grep 驗證：`grep -rn "scout" CLAUDE.md skills/ .claude/agents/` 命中數 **≤1**，且唯一容許的命中為 `skills/eval-flow/SKILL.md` manifest 格式節的**向後相容註**（(b) 明文要求保留，該註必然含 `scout_report_path` 欄名）；除該註外任何 scout 引用皆須清空（`run/`、`impact/`、`risk/`、`usage/`、`task/`、`scout/` 等歷史產出目錄不計）。`ls .claude/agents/scout.md` 不存在。
      〔拆分瑕疵修正（主 flow，執行中發現）：原 (f) 寫「0 命中」與 (b) 的「加向後相容註」內部矛盾——註中必然出現欄名。改為 ≤1 且明列容許項，非為湊綠而放寬：向後相容註是 (b) 有意要求的產出。〕
      契約: prose item——以上 DoD／grep 斷言替代契約表。

- [x] 2.2 Tier 1 豁免 eval_state.json（hook 三處 ＋ 流程 prose）  ~150行  files: `.claude/hooks/eval_gates.py`, `tests/test_eval_gates.py`, `skills/eval-flow/SKILL.md`（Tier 1 精簡路徑、manifest 格式、Gate 的硬性執行節）
      說明: Tier B 與 hotfix 都已豁免 eval_state（不走循環評分），Tier 1 反而要跑全套 set-step／set-review／set-verify／set-test／archive。Tier 1 的 run 短、中斷恢復價值低，這是 Tier 1 剩下最重的稅。改為：**Tier 1 不建 `eval_state.json`，測試與審查憑據改記 manifest 自身欄位**。
      **關鍵約束（拆分者盤點，不可漏）**：`eval_gates.py` 有三處假設 eval_state 必然存在，只改 commit gate 會讓 Tier 1 **完全無法派 code-writer**：
      DoD:
      (a) **gate 6 共通前提**（`eval_gates.py` :227 附近，subagent 呼叫攔截）：`eval_state.json` 不存在時，改為先找當前工作區**唯一一個** `tier == 1` 且 `status == "in_progress"` 的 manifest；找到 → 以該 manifest 做 phase 檢查並放行；找不到（或找到多個）→ 維持原 block 行為與原訊息語義。
      (b) **`check_other_runs`（單一 run 原則）**：無 `eval_state.json` 時，以 (a) 找到的 tier 1 manifest 的 `run_id` 作為「當前 run」比對基準；其餘 in_progress manifest 照舊擋。**不得**因為無 eval_state 就跳過本檢查。
      (c) **`check_manifest` 測試 gate**：`m.get("tier") == 1` 分支——若 `run/<run_id>.eval.json` **已 staged** → 走現行歸檔驗證路徑（**向後相容，本 run 自己走這條**）；未 staged → 改驗 manifest 自身四欄 `local_test_passed is True`／`local_test_evidence` 非空字串／`review_reds` 為非負 int／`verify_passed is True`，缺任一 block（訊息比照既有體例，指出該補什麼），通過則 return（豁免歸檔檔要求）。**四項憑據一項都不能少**——豁免的是檔案載體，不是證據本身。
      (d) `tests/test_eval_gates.py` 新增 case（既有斷言不得改動）：①Tier 1 無 eval_state ＋ manifest 四欄齊全 → 放行；②Tier 1 無 eval_state ＋ 缺任一欄 → block（四欄各一 case 或參數化）；③Tier 1 **有** staged 歸檔檔 → 走舊路徑（相容性回歸）；④無 eval_state ＋ 存在唯一 tier 1 in_progress manifest → subagent gate 放行；⑤無 eval_state ＋ 無任何 tier 1 in_progress manifest → 照舊 block；⑥無 eval_state ＋ 有其他 in_progress manifest → `check_other_runs` 仍擋。
      (e) `skills/eval-flow/SKILL.md`：Tier 1 精簡路徑步驟 1 改為「**不建 `eval_state.json`**」，並說明四項憑據改記 manifest；步驟 5 收尾改為「不歸檔、直接 `git add` manifest／task 檔並 commit」；manifest 格式節新增 `local_test_passed`／`local_test_evidence`／`review_reds`／`verify_passed` 四欄說明（標明 Tier 1 專用）；「Gate 的硬性執行」節的 gate 3、gate 6 描述同步標注 Tier 1 分支。
      (f) 全套測試 `python3 -m unittest discover -s tests` 相對 baseline 無新增失敗。
      契約（行為契約表）:
        - 輸入: staged manifest `tier:1`、無 `run/<id>.eval.json`、manifest 四欄齊全（`local_test_passed:true`／`local_test_evidence:"..."`／`review_reds:0`／`verify_passed:true`）→ 效果: commit gate **放行**。
        - 輸入: 同上但 `local_test_evidence` 為 `""` → 效果: **block**，訊息指出須記錄驗證證據。
        - 輸入: 同上但 `review_reds` 為 `null` → 效果: **block**，訊息指出須留痕 🔴 數。
        - 輸入: staged manifest `tier:1` ＋ `run/<id>.eval.json` 已 staged → 效果: 走**現行歸檔驗證路徑**（逐 sub_task 驗 status／四欄），行為與改動前完全一致。
        - 輸入: `tier:2` manifest ＋ 無歸檔檔 → 效果: **block**（現行行為不變，Tier 2 不受豁免）。
        - 輸入: 無 `eval_state.json`、工作區有唯一 `tier:1` `in_progress` manifest 且 `phase:"decomposed"`、呼叫 code-writer → 效果: **放行**。
        - 輸入: 無 `eval_state.json`、該 tier 1 manifest `phase:"init"`、呼叫 code-writer → 效果: **block**（phase 未達 decomposed）。
        - 輸入: 無 `eval_state.json`、工作區**無**任何 tier 1 in_progress manifest、呼叫 code-writer → 效果: **block**（前置 0 未完成，原行為）。
        - [邊界] 輸入: 無 `eval_state.json`、工作區有**兩個以上** tier 1 in_progress manifest → 效果: **block**（無法判定當前 run，走原 block 路徑）。
        - [邊界] 輸入: 無 `eval_state.json`、有唯一 tier 1 in_progress manifest A、另有 in_progress manifest B（tier 2）→ 效果: `check_other_runs` **block**（單一 run 原則不因豁免而失效）。
