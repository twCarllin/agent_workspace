# Task 2026-07-16

## Task 1：架構修剪——移除冗餘層（run: 2026-07-16-prune-redundant-layers）

- 建立時間：2026-07-16 10:56
- Tier：1（spec_inline 見 `run/2026-07-16-prune-redundant-layers.json`）
- 背景：L6 架構檢視結論——task-reviewer 是 sonnet 審 opus 產出（方向反了，硬上限已由 task-decomposition skill＋升級逃生門把關）；五個 skill 各只有一個 agent 消費者，是純間接層；`eval_state.json` 頂層 `status` 與 manifest `status` 重複且無任何 gate 讀取。三項皆為刪減，不新增規則。
- 驗證方式：全套測試 `python3 -m unittest discover -s tests` 綠（含 `tests/test_docs_consistency.py` 的引用一致性檢查）。

### Items（序列執行，不可 [P]——item 1/2/3 均觸及 agent 定義或 eval-flow SKILL，檔案相交）

- [x] **Item 1：移除 task-reviewer 層，審查職責併入 task-decomposer 交付前自檢**（預估 ~150 行 diff）
  - 刪除 `.claude/agents/task-reviewer.md`、`skills/task-checklist/`（整個目錄，含 SKILL.md——它是 task-reviewer 的專屬 skill，隨之消失）
  - `.claude/agents/task-decomposer.md`：新增「交付前自檢」小節，吸收 task-checklist 的 4 大範疇（描述清楚程度、拆分必要性、技術限制與前置條件、驗收標準／DoD）作為自檢清單；原文中三處「交給／呼叫 task-reviewer 審查」「由 task-reviewer 打回／判斷」改為自檢語義（frontmatter description 也要改）；「phase 不由你更新——task-reviewer 審查通過後由主 flow 設 decomposed」改為「自檢通過交付後由主 flow 設 decomposed」
  - `skills/eval-flow/SKILL.md`：L48「並接 task-reviewer 審查」、L50「（含 task-reviewer 審查通過）」、L95「retro / task-reviewer 等」、L98「task-decomposer 後接 task-reviewer 審查」四處改為自檢語義（L95 直接刪 task-reviewer 例子、L98 改「task-decomposer 交付前自檢通過」）
  - `skills/eval-flow-resume/SKILL.md` L23：「卡在 task-reviewer 審查，補審查」改為「task 檔已產出但 phase 未達 decomposed → 主 flow 核對 task-decomposer 自檢結論後設 phase」
  - `skills/task-decomposition/SKILL.md`：三處「task-reviewer 打回」改為「交付前自檢不通過即重拆」語義（L27、L107 附近）
  - `CLAUDE.md`（repo 本身這份）：Task Principle 的「task 檔案有新增任務後，必須呼叫 task-reviewer subagent 審查」整條改為「task-decomposer 交付前自檢（Tier 2）／主 flow 輕量 HITL（Tier 1）把關拆分品質」；其下「拆分合理性依 task-decomposition skill」子項保留
  - `README.md` L39：「拆完由 task-reviewer 審過才開工」改為「拆完經交付前自檢才開工」
  - `skills/report-format/SKILL.md`：刪除「D. 任務審查報告（task-reviewer 用）」模板整節（消費者已不存在），其後模板編號若有連動一併調整
  - DoD：repo 內 `grep -rn "task-reviewer\|task-checklist"` 僅剩 `TODO.md`（歷史紀錄，不改）；`python3 -m unittest tests.test_docs_consistency` 通過
- [x] **Item 2：內聯四個單一消費者 skill 進 agent 定義，刪除 skill 目錄**（預估 ~300 行 diff，多為內容搬移）
  - 搬移對應（原 SKILL.md 的 body 內容整段搬進 agent 定義檔的對應小節，去掉 frontmatter；agent 定義原本「按照 **xxx** skill／的 N 大範疇」的指涉改為指向檔內小節；frontmatter `skills:` 欄移除該項）：
    - `skills/review-checklist/SKILL.md` → `.claude/agents/code-reviewer.md`
    - `skills/task-verify-checklist/SKILL.md` → `.claude/agents/task-verifier.md`
    - `skills/root-cause-table/SKILL.md` → `.claude/agents/retro.md`
    - `skills/eval-scoring/SKILL.md` → `.claude/agents/eval-scorer.md`
  - 刪除上述四個 skill 目錄
  - 外部散文引用更新：`skills/test-strategy/SKILL.md` frontmatter description 的「eval 評分（走 eval-scoring）」與 L120「住在 `eval-scoring` skill」改為指向 eval-scorer agent 定義
  - 注意：`report-format` 仍是多 agent 共用，**不動**；`task-risk-analysis` 由主 flow 使用，**不動**
  - DoD：四個 skill 目錄不存在；`grep -rn "review-checklist\|task-verify-checklist\|root-cause-table\|eval-scoring"` 僅剩 `TODO.md`；`python3 -m unittest tests.test_docs_consistency` 通過
- [x] **Item 3：移除 eval_state.json 頂層 status 欄位（manifest.status 為唯一權威）**（預估 ~60 行 diff）
  - `.claude/hooks/eval_state.py`：`init` 不再寫入頂層 `status`（L64）；`archive` 不再設 `state["status"] = "completed"`（L153，歸檔檔也不含此欄——歸檔通過的判準本來就是全 sub_task passed）
  - `tests/test_eval_state.py` L112：`archived["status"] == "completed"` 斷言改為斷言歸檔檔**不含**頂層 `status` 鍵
  - `skills/eval-flow/SKILL.md`：eval_state.json 格式範例刪頂層 `"status"` 行；操作規則「全部完成且通過」條刪「頂層 status 設為 completed」、「有任一 failed」條改為「manifest 的 status 設為 failed」（不再寫兩邊）
  - `skills/eval-flow-resume/SKILL.md` 若有引用 eval_state 頂層 status，一併同步（以 grep 為準）
  - 注意：sub_task 層級的 `status`（passed/failed/in_progress）**不動**；`set-status` 子命令**不動**（操作的是 sub_task）
  - DoD：`python3 -m unittest discover -s tests` 全綠；`grep -n '頂層 status\|state\["status"\]' .claude/hooks/ skills/ -r` 無殘留

## Task 2：hook 不變量補強＋review/verify 審計欄位（run: 2026-07-16-floor-audit-gates）

- 建立時間：2026-07-16 15:37
- Tier：1（spec_inline 見 `run/2026-07-16-floor-audit-gates.json`）
- 背景：L6 檢視結論——Floor 規則（工作區既有未 commit 的 prose diff）只有文字沒有 hook 防線，違反「實際防線以 hook 為準」原則；「五維度加總 == quality_score」不變量從未被驗證；循環 step 3–4（code-reviewer 零 🔴、task-verifier 通過）只活在對話裡，跳過評分路徑（rounds 留空）事後無法區分「reviewer 一次過」與「根本沒跑 reviewer」。本 task 把三者落成 hook／helper 可稽核的欄位與檢查。既有未 commit 的 Floor prose（`.claude/agents/eval-scorer.md`、`skills/eval-flow/SKILL.md` 各 +1 行）屬同一功能，隨本 run 一併 commit。
- 驗證方式：全套測試 `python3 -m unittest discover -s tests` 綠。
- 相容性註記：新檢查只作用於 commit 時 staged 的歸檔檔與新 run 的 eval_state；已 commit 的舊 `run/*.eval.json` 不會被重新驗證，不需遷移。

### Items（序列執行，不可 [P]——item 2 的 helper 預設欄位是 item 1 歸檔檢查的配套，item 3 文件須以落地後的程式行為為準）

- [x] **Item 1：`eval_gates.py` 不變量與 gate 補強＋測試**（預估 ~120 行 diff）
  - `check_rounds_invariant()`：既有扣分總和檢查後新增——round 的 `dimensions` 必須是 dict 且五維度加總 == `quality_score`，否則 block（訊息含 sub_task 名、round 編號、加總值與 score）
  - `validate_state(require_passed=True)` 對每個 sub_task 新增四項檢查（僅 require_passed 路徑，不影響 `--validate` 對進行中 state 的自檢）：
    - `review_reds` 必須是非負 int（`None`＝reviewer 未留痕 → block；注意排除 bool）
    - `verify_passed` 必須是 `True`（task-verifier 未留痕 → block）
    - 一致性：`rounds` 為空但 `review_reds != 0` → block（曾有 🔴 必須有評分 round）；`rounds` 非空但 `review_reds == 0` → block（首輪零 🔴 應走跳過評分路徑，不該有 round）
    - **Floor 規則**：`rounds` 非空時，最終 round 的任一維度為 0 → block（0 分缺失不得被總分補償，對應 eval-scorer 定義的 Floor 規則）
  - `check_task_gate()` 的 `eval-scorer` 分支：對每個 in_progress sub_task 新增 `review_reds` 須為 int 且 >= 1（`None` → reviewer 未跑不可評分；`0` → 零 🔴 走跳過評分路徑，不該呼叫 scorer），置於既有 `local_test_passed` 檢查旁
  - `tests/test_eval_gates.py` 新增案例：dimensions 缺失／加總不符 → block；passed sub_task 缺 review_reds、缺 verify_passed、rounds↔review_reds 兩向不一致、最終 round 含 0 分維度 → block；合法跳過路徑（rounds 空＋review_reds 0＋verify_passed true）→ 通過；eval-scorer gate 對 review_reds None／0 → block、>=1 → 放行。既有測試 fixture 若因新必填欄位而失敗，補上欄位（這是預期的行為變更，不是弱化測試）
  - DoD：`python3 -m unittest tests.test_eval_gates` 綠
- [x] **Item 2：`eval_state.py` helper 欄位與子指令＋測試**（預估 ~80 行 diff）
  - `add-subtask` 預設欄位新增 `"review_reds": None`、`"verify_passed": False`（落盤為 JSON `null`／`false`）
  - 新增子指令 `set-review <id> <reds>`（`type=int`，負值 fail exit 1）：寫入該 sub_task 的 `review_reds`（記首輪 code-reviewer 的 🔴 數，修正前的原始數）
  - 新增子指令 `set-verify <id>`：將該 sub_task 的 `verify_passed` 設為 `True`（task-verifier 通過時呼叫；未通過不呼叫，欄位維持 false）
  - 檔頭 docstring 用法區同步兩個新指令
  - `tests/test_eval_state.py` 新增案例：add-subtask 預設值、set-review 正常／負值、set-verify、archive 對缺 review/verify 欄位的 state → exit 2（驗證與 item 1 的 validate_state 串接）
  - DoD：`python3 -m unittest tests.test_eval_state` 綠
- [x] **Item 3：文件同步（`skills/eval-flow/SKILL.md` 為主）**（預估 ~30 行 diff）
  - eval_state.json 格式範例的 sub_task 加 `"review_reds": null`、`"verify_passed": false`（放 `local_test_evidence` 附近）
  - 操作規則新增兩條：step 3 首輪審查結果出來後 `set-review <id> <🔴數>`（記修正前原始數）；step 4 task-verifier 通過後 `set-verify <id>`——兩者為 commit gate 必填，缺一擋歸檔
  - 「評分跳過路徑」條補充：`review_reds` 記 0 是跳過評分的合法性依據，hook 憑 `rounds 空 ↔ review_reds==0` 一致性稽核
  - 「Gate 的硬性執行」清單同步：gate 3 補 review/verify 留痕檢查、gate 5 補 dimensions 加總與 Floor、gate 6 的 eval-scorer 條補 `review_reds >= 1`
  - `grep -rn "review_reds" skills/ .claude/agents/ README.md` 檢查其他文件（如 eval-flow-resume、README）是否需同步，以 grep 結果為準
  - DoD：`python3 -m unittest tests.test_docs_consistency` 綠；全套 `python3 -m unittest discover -s tests` 綠
